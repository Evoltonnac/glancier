---
phase: quick
plan: 260330-q40
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - ui-react/pnpm-lock.yaml
  - ui-react/package-lock.json
  - ui-react/package.json
  - Makefile
  - scripts/test_frontend_core.sh
  - scripts/build.sh
  - .github/workflows/ci.yml
  - README.md
  - docs/build-path-contract.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "开发与 CI 都通过 PNPM 完成依赖安装和脚本执行，不再依赖 npm/package-lock。"
    - "根入口（Makefile）与现有测试/构建脚本在迁移后保持可用。"
    - "README 与 build contract 中的命令说明与实际实现一致。"
  artifacts:
    - path: "ui-react/pnpm-lock.yaml"
      provides: "前端依赖锁文件（PNPM）"
    - path: "ui-react/package.json"
      provides: "脚本链路不再硬编码 npm 调用"
    - path: ".github/workflows/ci.yml"
      provides: "CI 使用 pnpm 缓存与 frozen lockfile 安装"
    - path: "Makefile"
      provides: "根命令入口迁移到 PNPM"
    - path: "docs/build-path-contract.md"
      provides: "迁移后的命令契约文档"
  key_links:
    - from: "Makefile"
      to: "ui-react/package.json"
      via: "pnpm --dir ui-react run ..."
      pattern: "pnpm --dir ui-react run (dev:all|tauri:dev:all|tauri:build:mac|tauri:build:win|tauri:build)"
    - from: ".github/workflows/ci.yml"
      to: "ui-react/pnpm-lock.yaml"
      via: "setup-node cache + install command"
      pattern: "cache: pnpm|pnpm install --frozen-lockfile"
    - from: "scripts/build.sh"
      to: "ui-react/src-tauri"
      via: "pnpm exec tauri build"
      pattern: "pnpm exec tauri build"
---

<objective>
将仓库主路径从 NPM 迁移到 PNPM（per D-01：用户明确要求“将 NPM 迁移至 PNPM”），覆盖本地开发、测试、构建与 CI，避免 npm/pnpm 混用。

Purpose: 统一包管理器与锁文件来源，降低安装与脚本行为漂移风险。
Output: PNPM 锁文件、命令入口、CI、文档全部同步完成。
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@README.md
@docs/build-path-contract.md
@Makefile
@scripts/test_frontend_core.sh
@scripts/build.sh
@.github/workflows/ci.yml
@ui-react/package.json
@ui-react/package-lock.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 PNPM 锁文件与脚本基础</name>
  <files>pnpm-workspace.yaml, ui-react/pnpm-lock.yaml, ui-react/package-lock.json, ui-react/package.json</files>
  <action>创建工作区配置（仅纳入 `ui-react`），将 `ui-react/package.json` 中硬编码 `npm run ...` 的脚本链路改为 `pnpm run ...`，并将安全审计脚本改为 PNPM 等价命令。生成并提交 `ui-react/pnpm-lock.yaml`，移除 `ui-react/package-lock.json`。本任务仅做包管理器迁移，不引入功能性依赖升级（per D-01）。</action>
  <verify>
    <automated>corepack enable && pnpm --dir ui-react install --lockfile-only && test -f ui-react/pnpm-lock.yaml && test ! -f ui-react/package-lock.json</automated>
  </verify>
  <done>前端锁文件来源切换到 PNPM，脚本内部不再依赖 npm 调用。</done>
</task>

<task type="auto">
  <name>Task 2: 迁移执行入口与 CI 到 PNPM</name>
  <files>Makefile, scripts/test_frontend_core.sh, scripts/build.sh, .github/workflows/ci.yml</files>
  <action>将 `Makefile` 与 shell 脚本中的 `npm --prefix`、`npm run`、`npx tauri build` 迁移到 PNPM 等价命令（例如 `pnpm --dir ui-react run ...`、`pnpm run ...`、`pnpm exec tauri build`）。在 CI 中将 Node 缓存策略切换为 pnpm 锁文件，并使用 `pnpm install --frozen-lockfile` 与 `pnpm run ...` 执行前端测试、typecheck、Tauri 构建，保持现有 job 结构和矩阵不变（per D-01）。</action>
  <verify>
    <automated>pnpm --dir ui-react install --frozen-lockfile && make test-frontend && make test-typecheck</automated>
  </verify>
  <done>本地入口与 CI 均可在不依赖 npm 的情况下完成前端核心流程。</done>
</task>

<task type="auto">
  <name>Task 3: 同步命令文档契约并做迁移回归检查</name>
  <files>README.md, docs/build-path-contract.md</files>
  <action>将 README 与 Build Path Contract 中的 npm 安装/运行命令迁移为 PNPM 等价写法，明确推荐入口仍为 `make`，并保留必要的高级直连命令说明。确保文档中的依赖安装、测试、构建示例与已迁移命令一致，避免出现“文档还是 npm、实现是 pnpm”的漂移（per D-01）。</action>
  <verify>
    <automated>rg -n "pnpm --dir ui-react|pnpm install --frozen-lockfile|pnpm run tauri build|pnpm exec tauri build" README.md docs/build-path-contract.md .github/workflows/ci.yml Makefile scripts/test_frontend_core.sh scripts/build.sh</automated>
  </verify>
  <done>开发与 CI 文档路径完成统一，迁移后的命令可以被直接复现。</done>
</task>

</tasks>

<verification>
- `pnpm --dir ui-react install --frozen-lockfile`
- `make test-frontend`
- `make test-typecheck`
- `make build-backend`（仅确认迁移未影响 Python sidecar 预构建入口）
</verification>

<success_criteria>
- `ui-react/pnpm-lock.yaml` 存在且 `ui-react/package-lock.json` 已移除。
- Makefile、脚本、CI 的前端命令链路均使用 PNPM。
- README 与 `docs/build-path-contract.md` 的命令契约与迁移后实现一致。
- 前端核心测试与 typecheck 在 PNPM 链路下通过。
</success_criteria>

<output>
After completion, create `.planning/quick/260330-q40-npm-pnpm/260330-q40-SUMMARY.md`
</output>
