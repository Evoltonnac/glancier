---
must_haves:
  - BaseSourceCard widget container uses sdui-card-shell class
  - WidgetRenderer adds sdui-widget-shell wrappers based on widget type
  - TextBlock, FactSet, Badge, etc. are structural
  - Chart.*, List are content components with flex-grow properties
  - List container implements min-h-0 constraint for internal scrolling
---
# Quick Task Plan: SDUI Widget Size Refactoring

## 1. Update BaseSourceCard.tsx Shell
**Action**: Modify `BaseSourceCard` content container to apply the Card Shell logic (`sdui-card-shell`) for proper "rigid bottom line" flexible layout as defined in `index.css`.
**Files**:
- `ui-react/src/components/BaseSourceCard.tsx`
**Verify**: The `.sdui-card-shell` class replaces `min-h-0` inside the content area.
**Done**: [ ]

## 2. Introduce Widget Wrapper in WidgetRenderer.tsx
**Action**: Extract a `WIDGET_LAYOUT_META` map, and wrap the returned component inside `WidgetRendererImpl` with the layout wrappers `.sdui-widget-shell--structural` or `.sdui-widget-shell--content`, setting `--widget-weight` and `--widget-min-height-rows` appropriately.
**Files**:
- `ui-react/src/components/widgets/WidgetRenderer.tsx`
**Verify**: Each rendered widget sits inside an `sdui-widget-shell-*` div wrapper except in contexts that require inline behavior (maybe the top level wraps are enough).
**Done**: [ ]

## 3. Implement Internal Scroll Priority for List/Table components
**Action**: Update the inner components or `ListWidgetRenderer` to include `min-h-0` and `overflow-y-auto overscroll-contain` on their scrollable areas.
**Files**:
- `ui-react/src/components/widgets/WidgetRenderer.tsx`
- `ui-react/src/components/widgets/charts/ChartTable.tsx`
**Verify**: Components can scroll internally without pushing out the grid.
**Done**: [ ]
