---
phase: quick
plan: 260317-0uf
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-react/src-tauri/Cargo.toml
  - ui-react/src-tauri/tauri.conf.json
  - ui-react/src-tauri/capabilities/default.json
  - ui-react/src-tauri/src/lib.rs
  - ui-react/package.json
  - ui-react/src/pages/Settings.tsx
  - ui-react/src/i18n/messages/en.ts
  - ui-react/src/i18n/messages/zh.ts
  - .github/workflows/ci.yml
  - README.md
  - docs/build-path-contract.md
autonomous: true
requirements: []
---

<objective>
Enable Tauri v2 update detection end-to-end for Glanceus desktop, including updater plugin wiring, signing key generation workflow, release pipeline env wiring, and user-visible "Check Updates" behavior in Settings.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Wire Tauri updater runtime and capabilities</name>
  <files>ui-react/src-tauri/Cargo.toml, ui-react/src-tauri/src/lib.rs, ui-react/src-tauri/tauri.conf.json, ui-react/src-tauri/capabilities/default.json</files>
  <action>
Add `tauri-plugin-updater` dependency and register plugin in Rust app bootstrap. Enable updater artifact generation and add updater plugin config (`active`, `endpoints`, `pubkey`) in `tauri.conf.json`. Grant updater permission in the default capability.
  </action>
  <verify>
    <manual>rg -n "tauri-plugin-updater|plugin\(tauri_plugin_updater|createUpdaterArtifacts|\"updater\"|updater:default" ui-react/src-tauri</manual>
  </verify>
  <done>Desktop runtime can invoke updater check APIs and builds can emit updater artifacts with signing.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend check-update behavior and i18n copy</name>
  <files>ui-react/package.json, ui-react/src/pages/Settings.tsx, ui-react/src/i18n/messages/en.ts, ui-react/src/i18n/messages/zh.ts</files>
  <action>
Install frontend updater plugin package and replace About tab "Check Updates" button with real updater check logic (Tauri-only). Show loading state and localized toasts for: checking, update available, latest version, and error fallback.
  </action>
  <verify>
    <automated>npm --prefix ui-react run typecheck</automated>
  </verify>
  <done>Users can click "Check Updates" and receive clear localized feedback from Tauri updater status.</done>
</task>

<task type="auto">
  <name>Task 3: Create signing keys and align release docs/pipeline</name>
  <files>.github/workflows/ci.yml, README.md, docs/build-path-contract.md, .planning/STATE.md</files>
  <action>
Generate updater signing key pair (private key kept local, public key injected to Tauri config). Update release workflow to pass signing secrets for updater artifacts. Update docs to remove the "updater disabled" policy and document required secrets/commands.
  </action>
  <verify>
    <manual>CI workflow contains `TAURI_SIGNING_PRIVATE_KEY` and `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`; docs mention updater enabled and key setup.</manual>
  </verify>
  <done>Key creation is completed and release pipeline/documentation are synchronized with updater-enabled builds.</done>
</task>

</tasks>

<success_criteria>
- Tauri updater plugin is enabled in Rust, config, and capabilities
- Public key is configured and private key is generated locally (not committed)
- Settings "Check Updates" executes real updater check in Tauri runtime
- CI release workflow is ready for signed updater artifacts
- README/build contract no longer claim updater is disabled
</success_criteria>

<output>
After completion, create `.planning/quick/260317-0uf-tauri/260317-0uf-SUMMARY.md`
</output>
