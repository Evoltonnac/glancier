---
phase: 09-sql-runtime-and-integration-data-normalization
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - tests/core/test_sql_output_channel_compat.py
  - docs/flow/02_step_reference.md
  - docs/flow/04_step_failure_test_inputs.md
  - docs/flow/05_refresh_scheduler_and_retry.md
autonomous: true
requirements:
  - DATA-04
must_haves:
  truths:
    - "User can map SQL outputs to existing flow `outputs` and `context` channels without frontend logic changes."
    - "User-facing flow docs describe canonical SQL output paths and compatibility aliases with deterministic field names."
  artifacts:
    - path: "tests/core/test_sql_output_channel_compat.py"
      provides: "Regression coverage for `sql_response` path mapping through existing executor output/channel rules."
    - path: "docs/flow/02_step_reference.md"
      provides: "Canonical SQL output contract documentation for integration authors."
    - path: "docs/flow/04_step_failure_test_inputs.md"
      provides: "Failure and metadata expectations updated for normalized SQL outputs."
  key_links:
    - from: "tests/core/test_sql_output_channel_compat.py"
      to: "core/executor.py"
      via: "_resolve_output_path and step outputs/context mapping"
      pattern: "outputs={\"...\": \"sql_response"
    - from: "docs/flow/02_step_reference.md"
      to: "core/steps/sql_step.py"
      via: "Documented runtime output envelope keys"
      pattern: "sql_response.fields"
---

<objective>
Prove that normalized SQL responses remain consumable through existing executor template channels, then update flow contracts/docs to match shipped behavior.

Purpose: Complete DATA-04 with executable compatibility checks and synced documentation.
Output: New channel-compat regression tests + docs reflecting canonical SQL output paths and alias policy.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sql-runtime-and-integration-data-normalization/09-CONTEXT.md
@.planning/phases/09-sql-runtime-and-integration-data-normalization/09-RESEARCH.md
@core/executor.py
@core/steps/sql_step.py
@docs/flow/01_architecture_and_orchestration.md
@docs/flow/02_step_reference.md
@docs/sdui/03_template_expression_spec.md

<interfaces>
From core/executor.py mapping contract:
```python
for target_var, source_path in step.outputs.items():
    resolved = self._resolve_output_path(output, source_path)
```

From Flow channel rules (docs/flow/01_architecture_and_orchestration.md):
```text
outputs: persisted/public
context: in-memory downstream vars
secrets: encrypted persistence
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SQL output-channel compatibility regression tests (outputs + context)</name>
  <files>tests/core/test_sql_output_channel_compat.py</files>
  <read_first>
    tests/core/test_sql_step.py
    core/executor.py
    core/steps/sql_step.py
    docs/flow/01_architecture_and_orchestration.md
  </read_first>
  <action>
    Create `tests/core/test_sql_output_channel_compat.py` with executor-driven tests proving existing mapping channels work with normalized SQL keys.

    Include at least two test cases:
    1. `outputs` mapping resolves canonical keys:
       - map `sql_rows` from `sql_response.rows`
       - map `sql_fields` from `sql_response.fields`
       - map `sql_duration_ms` from `sql_response.duration_ms`
    2. compatibility alias path remains readable:
       - map `sql_columns` from `sql_response.columns`
       - map `sql_execution_ms` from `sql_response.execution_ms`
       - assert `sql_execution_ms == sql_duration_ms` when both mapped.

    Keep assertions backend-only and deterministic; do not add UI or frontend logic.
  </action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_output_channel_compat.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/core/test_sql_output_channel_compat.py` exists.
    - `rg -n "sql_response\\.fields" tests/core/test_sql_output_channel_compat.py` returns at least one match.
    - `rg -n "sql_response\\.duration_ms" tests/core/test_sql_output_channel_compat.py` returns at least one match.
    - `rg -n "sql_response\\.execution_ms" tests/core/test_sql_output_channel_compat.py` returns at least one match.
    - Pytest command in `<verify>` passes with exit code 0.
  </acceptance_criteria>
  <done>DATA-04 compatibility is proven by automated tests for canonical and alias `sql_response` paths through existing output-channel mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Update SQL flow docs to canonical envelope + compatibility aliases</name>
  <files>docs/flow/02_step_reference.md, docs/flow/04_step_failure_test_inputs.md, docs/flow/05_refresh_scheduler_and_retry.md</files>
  <read_first>
    docs/flow/02_step_reference.md
    docs/flow/04_step_failure_test_inputs.md
    docs/flow/05_refresh_scheduler_and_retry.md
    core/steps/sql_step.py
    .planning/phases/09-sql-runtime-and-integration-data-normalization/09-CONTEXT.md
  </read_first>
  <action>
    Update docs to match shipped SQL runtime contract exactly:

    In `docs/flow/02_step_reference.md`, set SQL output envelope list to include canonical keys:
    - `sql_response.rows`
    - `sql_response.fields`
    - `sql_response.row_count`
    - `sql_response.duration_ms`
    - `sql_response.truncated`
    - `sql_response.statement_count`
    - `sql_response.statement_types`
    - `sql_response.is_high_risk`
    - `sql_response.risk_reasons`
    - `sql_response.timeout_seconds`
    - `sql_response.max_rows`
    and compatibility aliases:
    - `sql_response.columns`
    - `sql_response.execution_ms`

    In `docs/flow/04_step_failure_test_inputs.md`, keep deterministic `runtime.sql_*` failure matrix and add explicit normalized success-envelope metadata checks (`fields`, `duration_ms`, `truncated`) in SQL regression guidance.

    In `docs/flow/05_refresh_scheduler_and_retry.md`, keep SQL retry policy note (no auto-retry for deterministic guardrail failures) and reference canonical metadata naming (`duration_ms`, `truncated`) instead of legacy-only timing wording.
  </action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_output_channel_compat.py tests/core/test_sql_step_connectors.py tests/core/test_sql_step.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "sql_response\\.fields" docs/flow/02_step_reference.md` returns one or more matches.
    - `rg -n "sql_response\\.duration_ms" docs/flow/02_step_reference.md` returns one or more matches.
    - `rg -n "sql_response\\.truncated" docs/flow/02_step_reference.md` returns one or more matches.
    - `rg -n "sql_response\\.execution_ms" docs/flow/02_step_reference.md` returns one or more matches as alias documentation.
    - `rg -n "runtime\\.sql_connect_failed|runtime\\.sql_auth_failed|runtime\\.sql_query_failed" docs/flow/04_step_failure_test_inputs.md` returns matches.
    - Pytest command in `<verify>` passes with exit code 0.
  </acceptance_criteria>
  <done>Flow docs and failure guidance are synchronized with canonical SQL Integration Data paths while preserving deterministic error-code references.</done>
</task>

</tasks>

<verification>
Run compatibility/doc-adjacent backend checks:
- `python -m pytest tests/core/test_sql_output_channel_compat.py tests/core/test_sql_step_connectors.py tests/core/test_sql_step.py -q`
- `make test-backend`
</verification>

<success_criteria>
- Existing `outputs/context` channel mapping works with canonical SQL response paths and compatibility aliases.
- SQL step reference and failure docs reflect canonical field names and alias policy.
- DATA-04 is covered by passing backend tests without frontend business logic changes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-sql-runtime-and-integration-data-normalization/09-03-SUMMARY.md`
</output>
