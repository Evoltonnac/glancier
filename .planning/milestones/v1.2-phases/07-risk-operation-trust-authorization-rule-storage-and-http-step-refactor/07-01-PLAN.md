---
phase: 07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/storage/contract.py
  - core/storage/sqlite_connection.py
  - core/storage/sqlite_trust_rule_repo.py
  - core/network_trust/models.py
  - core/network_trust/policy.py
  - core/settings_manager.py
  - main.py
  - tests/core/test_storage_contract_sqlite.py
  - tests/core/test_settings_manager.py
autonomous: true
requirements: [STORAGE-TRUST-01, SEC-DB-01]
user_setup: []
must_haves:
  truths:
    - "Trust rules persist in SQLite under the storage contract domain and are not stored in ad-hoc JSON files."
    - "Trust rule identity is deterministic by capability/scope/target, preventing duplicate conflicting rows."
    - "Source-scoped trust rules are cleaned automatically when source rows are deleted."
  artifacts:
    - path: "core/storage/sqlite_trust_rule_repo.py"
      provides: "SQLite repository for trust rule CRUD and precedence-ready queries."
    - path: "core/network_trust/policy.py"
      provides: "Deterministic policy evaluator for source/global rules plus default fallback."
    - path: "tests/core/test_storage_contract_sqlite.py"
      provides: "Regression coverage for trust-rule table bootstrap, uniqueness, and source-delete cascade."
  key_links:
    - from: "core/storage/sqlite_connection.py"
      to: "core/storage/sqlite_trust_rule_repo.py"
      via: "Schema bootstrap creates table/index consumed by trust repository."
    - from: "core/settings_manager.py"
      to: "core/network_trust/policy.py"
      via: "Default trust behavior reads `http_private_target_policy_default` from system settings."
    - from: "main.py"
      to: "core/network_trust/policy.py"
      via: "App bootstrap wires trust repository + settings into shared policy service."
---

<objective>
Build Phase 7 foundation: durable trust-rule persistence plus deterministic policy evaluation before touching runtime `http` step behavior.

Purpose: Deliver STORAGE-TRUST-01/SEC-DB-01 with low blast radius by landing storage and policy primitives first.
Output: SQLite trust-rule schema/repository, evaluator module, settings default hook, and regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-CONTEXT.md
@.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-RESEARCH.md
@docs/flow/http_local_network_policy_proposal.md
@core/storage/contract.py
@core/storage/sqlite_connection.py
@core/storage/sqlite_resource_repo.py
@core/settings_manager.py
@main.py
@tests/core/test_storage_contract_sqlite.py
@tests/core/test_settings_manager.py

<interfaces>
Target storage rule identity:
```text
capability + scope_type + source_id (nullable) + target_type + target_value
```

Required decisions:
```text
allow | deny
```

Default policy setting:
```text
http_private_target_policy_default: prompt | allow | deny
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trust-rule SQLite schema and repository in storage contract domain</name>
  <files>core/storage/contract.py, core/storage/sqlite_connection.py, core/storage/sqlite_trust_rule_repo.py, tests/core/test_storage_contract_sqlite.py</files>
  <read_first>docs/flow/http_local_network_policy_proposal.md, core/storage/contract.py, core/storage/sqlite_connection.py, tests/core/test_storage_contract_sqlite.py</read_first>
  <behavior>
    - Test 1: Storage bootstrap creates `connection_trust_rules` table, deterministic unique index, and FK to `stored_sources(source_id)` with cascade.
    - Test 2: Repository supports upsert/query/delete for source/global rules with deterministic match keys.
    - Test 3: Deleting a source row removes source-scoped trust rules automatically via FK cascade.
  </behavior>
  <action>Extend storage bootstrap DDL with `connection_trust_rules` and unique identity index. Add repository class exposing minimal CRUD and match-query methods for trust decisions. Keep schema additive/idempotent and align repository error mapping with existing storage error taxonomy. Extend storage contract tests to assert table/index creation, upsert/read behavior, uniqueness enforcement, and source-delete cascade semantics.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_contract_sqlite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_contract_sqlite.py -q` exits 0.
    - `rg -n "connection_trust_rules" core/storage/sqlite_connection.py core/storage/sqlite_trust_rule_repo.py` returns schema + repo usage.
    - `rg -n "ON DELETE CASCADE" core/storage/sqlite_connection.py` confirms lifecycle cleanup behavior.
  </acceptance_criteria>
  <done>Trust-rule persistence primitives are available in SQLite with deterministic identity and source lifecycle cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trust policy evaluator and settings default contract</name>
  <files>core/network_trust/models.py, core/network_trust/policy.py, core/settings_manager.py, main.py, tests/core/test_settings_manager.py</files>
  <read_first>core/settings_manager.py, main.py, .planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Policy evaluator resolves `allow|deny|prompt` by deterministic precedence (source > global > default).
    - Test 2: Settings model persists/loads `http_private_target_policy_default` with fallback `prompt`.
    - Test 3: App bootstrap wires one trust-policy instance with storage-backed repository and settings-backed default resolver.
  </behavior>
  <action>Create a focused trust-policy module with typed rule/decision models and precedence logic. Extend `SystemSettings` and compatibility normalization to include `http_private_target_policy_default` while preserving existing settings behavior. Wire trust-policy dependencies in `main.py` for later runtime/API usage without yet changing `http` execution behavior.</action>
  <verify>
    <automated>python -m pytest tests/core/test_settings_manager.py tests/core/test_storage_contract_sqlite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_settings_manager.py tests/core/test_storage_contract_sqlite.py -q` exits 0.
    - `rg -n "http_private_target_policy_default" core/settings_manager.py` confirms persisted settings contract.
    - `rg -n "network_trust|trust_rule|policy" main.py core/network_trust/policy.py` confirms bootstrap wiring.
  </acceptance_criteria>
  <done>Deterministic trust policy and settings default contract exist and are wired for phase-7 runtime integration.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_storage_contract_sqlite.py -q
- [ ] python -m pytest tests/core/test_settings_manager.py -q
- [ ] make test-backend
</verification>

<success_criteria>
- SQLite-backed trust-rule model exists under storage contract ownership with deterministic identity and source cascade cleanup.
- Trust policy precedence is codified and testable before runtime behavior changes.
- Settings default policy (`prompt|allow|deny`) is persisted and backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-01-SUMMARY.md`
</output>
