---
phase: 07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - core/steps/http_step.py
  - core/executor.py
  - core/api.py
  - ui-react/src/api/client.ts
  - ui-react/src/components/auth/FlowHandler.tsx
  - ui-react/src/components/auth/FlowHandler.test.tsx
  - ui-react/src/i18n/messages/en.ts
  - ui-react/src/i18n/messages/zh.ts
  - docs/flow/02_step_reference.md
  - docs/flow/06_storage_contract_and_migration.md
  - tests/core/test_http_step.py
  - tests/api/test_source_delete_api.py
  - tests/api/test_settings_api.py
  - tests/api/test_oauth_interaction_api.py
autonomous: true
requirements: [SEC-HTTP-01, SEC-HTTP-02, STORAGE-TRUST-01]
user_setup: []
must_haves:
  truths:
    - "Private/loopback HTTP targets no longer hard-fail silently; missing trust decision yields deterministic interaction-required state."
    - "User trust decision actions (`allow_once`, `allow_always`, `deny`) are explicit and auditable."
    - "Runtime and UI diagnostics use stable new error codes without leaking credentials."
  artifacts:
    - path: "core/steps/http_step.py"
      provides: "Target classification + trust-gate execution path replacing hard private-target block."
    - path: "core/api.py"
      provides: "Trust-decision interaction handling without routing trust payloads into secrets storage."
    - path: "ui-react/src/components/auth/FlowHandler.tsx"
      provides: "Interaction UI rendering and submission for trust decision actions."
    - path: "tests/core/test_http_step.py"
      provides: "Regression coverage for trust-required/allow/deny/public/invalid-target behavior."
  key_links:
    - from: "core/steps/http_step.py"
      to: "core/executor.py"
      via: "Structured trust exceptions map to deterministic runtime status + interaction payload."
    - from: "core/executor.py"
      to: "core/api.py"
      via: "Interaction payload/action protocol consumed by `/sources/{source_id}/interact`."
    - from: "core/api.py"
      to: "ui-react/src/components/auth/FlowHandler.tsx"
      via: "Backend-provided trust actions drive frontend decision buttons and submission payloads."
---

<objective>
Apply Phase 7 runtime behavior: replace `http` private-target hard block with trust-gate policy, integrate interaction decision flow, and align docs/i18n/test contracts.

Purpose: Deliver SEC-HTTP-01/SEC-HTTP-02 with deterministic interaction + error-code contracts and no frontend/business-boundary regressions.
Output: Runtime trust-gate behavior, interaction decision handling, UI contract updates, and synced docs/tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-CONTEXT.md
@.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-RESEARCH.md
@.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-01-PLAN.md
@docs/flow/http_local_network_policy_proposal.md
@core/steps/http_step.py
@core/executor.py
@core/api.py
@ui-react/src/components/auth/FlowHandler.tsx
@ui-react/src/api/client.ts
@tests/core/test_http_step.py
@tests/api/test_oauth_interaction_api.py
@tests/api/test_source_delete_api.py

<interfaces>
Required stable runtime error codes:
```text
runtime.network_trust_required
runtime.network_target_denied
runtime.network_target_invalid
```

Required trust decision protocol (interaction submit):
```json
{
  "type": "confirm",
  "decision": "allow_once | allow_always | deny",
  "scope": "source | global",
  "target_key": "<normalized-host-or-host_port>"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace HTTP hard private-target block with trust-gate runtime path</name>
  <files>core/steps/http_step.py, core/executor.py, tests/core/test_http_step.py</files>
  <read_first>core/steps/http_step.py, core/executor.py, docs/flow/http_local_network_policy_proposal.md, tests/core/test_http_step.py</read_first>
  <behavior>
    - Test 1: Private/loopback target without trust decision raises trust-required path (`runtime.network_trust_required`) and does not perform outbound request.
    - Test 2: Allow decision path permits request execution; deny decision path blocks with `runtime.network_target_denied`.
    - Test 3: Invalid scheme/target emits deterministic `runtime.network_target_invalid`; public target behavior is unchanged.
  </behavior>
  <action>Refactor `http` target validation into structured classification/policy evaluation. Replace `RuntimeError(http_target_blocked_*)` with typed runtime exceptions carrying deterministic codes and interaction metadata (source/step/method/url/target classification). Ensure executor maps trust-required exception to suspended interaction state and denied/invalid paths to deterministic error states without credential-bearing details.</action>
  <verify>
    <automated>python -m pytest tests/core/test_http_step.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_http_step.py -q` exits 0.
    - `rg -n "runtime\\.network_trust_required|runtime\\.network_target_denied|runtime\\.network_target_invalid" core/steps/http_step.py core/executor.py` returns deterministic-code paths.
    - Existing public target success tests remain green.
  </acceptance_criteria>
  <done>HTTP step behavior is policy-gated for private/loopback targets with deterministic trust-required/deny/invalid contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trust-decision interaction handling in API and frontend flow dialog</name>
  <files>core/api.py, ui-react/src/api/client.ts, ui-react/src/components/auth/FlowHandler.tsx, ui-react/src/components/auth/FlowHandler.test.tsx, ui-react/src/i18n/messages/en.ts, ui-react/src/i18n/messages/zh.ts, tests/api/test_oauth_interaction_api.py</files>
  <read_first>core/api.py, ui-react/src/components/auth/FlowHandler.tsx, ui-react/src/components/auth/FlowHandler.test.tsx, ui-react/src/i18n/messages/en.ts, ui-react/src/i18n/messages/zh.ts</read_first>
  <behavior>
    - Test 1: Trust interaction accepts only allowed keys/actions and rejects malformed/extra payload with deterministic API errors.
    - Test 2: `allow_once` stores ephemeral grant (non-persisted), `allow_always` persists source/global rule, `deny` persists deny decision.
    - Test 3: Frontend renders action buttons for trust interactions and submits explicit decision payload; i18n copy resolves in both `en` and `zh`.
  </behavior>
  <action>Add explicit trust-decision branch in `interact_source` that consumes validated protocol fields and bypasses generic secrets-write flow. Extend `FlowHandler` confirm interaction rendering to support backend-specified trust actions (three buttons with deterministic payloads). Keep interaction source-binding and payload-validation safeguards intact. Add i18n keys for new runtime error-code copy and trust-action labels/messages in both locale files.</action>
  <verify>
    <automated>python -m pytest tests/api/test_oauth_interaction_api.py -q</automated>
    <automated>npm --prefix ui-react run test -- --run src/components/auth/FlowHandler.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Trust-decision payloads do not write into source secrets.
    - `npm --prefix ui-react run test -- --run src/components/auth/FlowHandler.test.tsx` exits 0.
    - `rg -n "runtime\\.network_trust_required|runtime\\.network_target_denied|runtime\\.network_target_invalid" ui-react/src/i18n/messages/en.ts ui-react/src/i18n/messages/zh.ts` returns both locale mappings.
  </acceptance_criteria>
  <done>Trust decision interaction flow works end-to-end through API + frontend with deterministic payload validation and i18n diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sync flow/storage docs and add regression checks for source lifecycle and settings surfaces</name>
  <files>docs/flow/02_step_reference.md, docs/flow/06_storage_contract_and_migration.md, tests/api/test_source_delete_api.py, tests/api/test_settings_api.py</files>
  <read_first>docs/flow/02_step_reference.md, docs/flow/06_storage_contract_and_migration.md, tests/api/test_source_delete_api.py, tests/api/test_settings_api.py</read_first>
  <behavior>
    - Test 1: Source deletion semantics still hold and include trust-rule cascade expectations.
    - Test 2: Settings API round-trips `http_private_target_policy_default`.
    - Test 3: Step reference docs reflect trust-gate behavior and error-code outcomes for `http` step private targets.
  </behavior>
  <action>Update flow docs so `http` step contract explains target classification + trust-gate interaction path and deterministic error codes. Update storage contract doc with trust-rule table ownership/cascade behavior. Extend API tests for source deletion and settings persistence to include trust-policy-specific assertions.</action>
  <verify>
    <automated>python -m pytest tests/api/test_source_delete_api.py tests/api/test_settings_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/api/test_source_delete_api.py tests/api/test_settings_api.py -q` exits 0.
    - Docs mention new trust-related runtime codes and persistence semantics.
    - No regression in existing source-delete cleanup contract fields.
  </acceptance_criteria>
  <done>Phase 7 behavior/docs/tests are synchronized, and source lifecycle + settings contracts remain deterministic.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_http_step.py tests/api/test_oauth_interaction_api.py -q
- [ ] python -m pytest tests/api/test_source_delete_api.py tests/api/test_settings_api.py -q
- [ ] npm --prefix ui-react run test -- --run src/components/auth/FlowHandler.test.tsx
- [ ] make test-backend
- [ ] make test-frontend
- [ ] make test-typecheck
</verification>

<success_criteria>
- `http` private/loopback targets use explicit trust gate instead of hard block, with deterministic runtime codes.
- Trust decision actions are explicit, validated, and auditable across backend + frontend.
- Trust-rule persistence/lifecycle behavior is documented and verified alongside existing source-delete/settings contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-risk-operation-trust-authorization-rule-storage-and-http-step-refactor/07-02-SUMMARY.md`
</output>
