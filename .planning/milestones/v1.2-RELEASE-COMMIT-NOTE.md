# v1.2 Release Candidate Commit Note (Draft)

## Candidate Range

- Base: `origin/1.2`
- Candidate: `HEAD` (`1.2`)
- Ahead commits:
  - `a23b2ef chore: remove phase 11 (Authoring Usability and Diagnostics Hardening)`
  - `c0f82fc feat(trust): split db risk prompts and stabilize allow-once gating`

## Change Summary

1. Risk-operation trust architecture expanded for database connectors
- Added database target classification and trust-gate enforcement for SQL/MongoDB/Redis private or loopback targets.
- Added dedicated DB trust helper module and policy integration plumbing.
- Extended API/executor/FlowHandler interaction data to support capability-specific trust prompts and allow-once/allow-always semantics.

2. Runtime behavior and contract updates
- SQL/MongoDB/Redis execution path now routes through common trust-evaluation logic before connector execution.
- Updated error-surface and interaction handling tests/docs around network trust prompts and decisions.

3. Frontend and i18n updates
- `FlowHandler` updated for split DB risk prompts.
- Added/updated translation keys in both `en` and `zh`.

4. Planning scope adjustment
- Removed planned Phase 11 from active milestone planning state.

## Risk Points

1. Release blocker: full regression suite is red
- `python -m pytest tests -q` currently fails with 13 tests.
- Main impact clusters:
  - startup resilience/storage contract monkeypatch constructor contracts.
  - MongoDB/Redis deterministic runtime error-code tests now hit trust-gate prompt path first.

2. Contract ambiguity: trust-gate precedence vs connector runtime error mapping
- For loopback/private targets, trust-gate may intercept before connect/auth/query mapping.
- Existing tests still assert connector runtime codes in these paths.

3. Schema sync drift
- Running `make gen-schemas` changes generated artifacts (`integration.python.schema.json`, `integration.schema.json`) with `connector.options.dialect` enum additions.
- Needs explicit decision: commit drift or adjust source contract/generator for deterministic no-op regeneration.

4. Manual verification debt remains
- Phase 9 requires live PostgreSQL success-path verification.
- Phase 10 requires real dashboard visual/fallback transition verification.

## Regression Scope for Final Release Validation

1. Backend trust and runtime safety
- SQL/MongoDB/Redis trust gate behavior for private/loopback endpoints.
- Interaction payloads, confirm decisions, and trust-rule persistence precedence.
- Deterministic error-code behavior for trust denied / trust required / connector failures.

2. Startup/bootstrap contracts
- `main.create_app` dependency wiring compatibility with test doubles and bootstrap behavior.
- Storage contract and startup resilience tests.

3. Frontend trust interaction UX
- FlowHandler rendering and submit payload for DB risk prompts.
- i18n rendering for trust-related copy in `en` and `zh`.

4. SQL-to-chart end-to-end consistency
- `sql_response` field metadata mapping to chart widgets.
- Chart fallback state transitions in live refresh contexts.

## Release Gate Snapshot (2026-03-29)

- `make test-backend`: PASS
- `make test-frontend`: PASS
- `make test-typecheck`: PASS
- `make test-impacted`: PASS (smoke path)
- `python -m pytest tests -q -k "interaction or auth or encryption"`: FAIL (2 failures)
- `python -m pytest tests -q`: FAIL (13 failures)
- `make gen-schemas`: DRIFT FOUND

## Recommended Release Message (after blockers are closed)

`feat(trust): enforce connector-level DB risk prompts and stabilize allow-once trust decisions`

Suggested body:
- Add unified DB target trust gating for SQL/MongoDB/Redis private and loopback connections.
- Split and normalize network trust interaction payloads across API, executor, and frontend FlowHandler.
- Keep i18n parity for trust prompts (`en`/`zh`) and align deterministic error/interaction contracts.
