---
phase: 03-remediate-critical-security-audit-findings-in-core-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [core/auth/oauth_auth.py, core/api.py, tests/api/test_oauth_interaction_api.py, tests/core/test_oauth_authlib.py, tests/core/test_executor_auth_interactions.py]
autonomous: true
requirements: [SEC-02, INT-02]
user_setup: []
must_haves:
  truths:
    - "OAuth code exchange succeeds only when server-bound state matches source_id, redirect_uri, and verifier."
    - "Cross-source interaction payloads cannot redirect secret writes away from the route source_id."
  artifacts:
    - path: "core/auth/oauth_auth.py"
      provides: "Single-use OAuth state binding with deterministic mismatch rejection."
    - path: "core/api.py"
      provides: "Route-bound interact validation that blocks body source_id override paths."
  key_links:
    - from: "core/api.py"
      to: "core/auth/oauth_auth.py"
      via: "oauth_code_exchange interaction validation before exchange_code execution"
---

<objective>
Harden OAuth callback/exchange integrity and interaction source isolation without breaking existing successful auth flows.

Purpose: Close high-impact validation boundary gaps first so later hardening can rely on trusted auth and interaction state.
Output: Route-bound interaction guardrails, strict OAuth state validation, and regression tests for both paths.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-CONTEXT.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-RESEARCH.md
@core/api.py
@core/auth/oauth_auth.py
@tests/api/test_oauth_interaction_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind OAuth code flow to single-use server state metadata</name>
  <files>core/auth/oauth_auth.py</files>
  <read_first>core/auth/oauth_auth.py, core/secrets_controller.py, core/models.py</read_first>
  <action>In `OAuthAuth.get_authorize_url`, generate a high-entropy state value with `generate_token(48)` and persist it in `oauth_pkce` alongside `verifier`, `source_id`, `redirect_uri`, `created_at`, and `used_at=None`. In code-exchange validation path, require incoming `state` to match stored state, reject missing/expired/reused values, reject `source_id` or `redirect_uri` mismatches, and set `used_at` immediately after successful validation. Keep deterministic failures with `ValueError` messages containing `oauth_state_invalid` or `oauth_state_expired` so API layer can map to stable HTTP errors.</action>
  <verify>pytest tests/core/test_oauth_authlib.py -k "state or exchange"</verify>
  <acceptance_criteria>
    - core/auth/oauth_auth.py contains "generate_token(48)" for OAuth state generation.
    - core/auth/oauth_auth.py contains "oauth_state_invalid" and "oauth_state_expired".
    - Stored OAuth PKCE payload includes source_id and redirect_uri fields.
  </acceptance_criteria>
  <done>OAuth code exchange cannot proceed with stale, reused, or mismatched state binding metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce route source ownership in interaction writes</name>
  <files>core/api.py</files>
  <read_first>core/api.py, core/models.py, tests/api/test_oauth_interaction_api.py</read_first>
  <action>In `interact_source`, treat route `source_id` as the only write target. Ignore or reject request-body `source_id` and enforce pending interaction match by source plus expected interaction type before any `_secrets.set_secrets(...)` or OAuth exchange call. Add a helper named `_validate_interaction_source_binding` that rejects mismatches with HTTP 400 and detail `interaction_source_mismatch`. Restrict accepted payload keys to pending interaction field names plus mandatory protocol keys (`interaction_type`, `code`, `state`, `redirect_uri`) and reject extras with detail `interaction_payload_invalid`.</action>
  <verify>pytest tests/api/test_oauth_interaction_api.py -k "source_mismatch or interaction_payload_invalid"</verify>
  <acceptance_criteria>
    - core/api.py contains "interaction_source_mismatch".
    - core/api.py contains "_validate_interaction_source_binding".
    - core/api.py contains "interaction_payload_invalid".
  </acceptance_criteria>
  <done>Interaction submission can no longer mutate secrets for a different source or inject unapproved keys.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for OAuth state and source isolation</name>
  <files>tests/api/test_oauth_interaction_api.py, tests/core/test_oauth_authlib.py, tests/core/test_executor_auth_interactions.py</files>
  <read_first>tests/api/test_oauth_interaction_api.py, tests/core/test_oauth_authlib.py, tests/core/test_executor_auth_interactions.py, core/api.py, core/auth/oauth_auth.py</read_first>
  <action>Add tests that assert: (1) state mismatch/expired/reused exchange attempts fail deterministically, (2) request-body source_id override is rejected, and (3) valid same-source exchange path still succeeds. Use exact assertion strings `interaction_source_mismatch`, `interaction_payload_invalid`, `oauth_state_invalid`, and `oauth_state_expired` in test expectations so failures are deterministic.</action>
  <verify>pytest tests/api/test_oauth_interaction_api.py tests/core/test_oauth_authlib.py tests/core/test_executor_auth_interactions.py</verify>
  <acceptance_criteria>
    - tests/api/test_oauth_interaction_api.py contains "interaction_source_mismatch".
    - tests/core/test_oauth_authlib.py contains "oauth_state_expired".
    - pytest command for the three files exits 0.
  </acceptance_criteria>
  <done>Auth and interaction validation hardening is protected by explicit regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/core/test_oauth_authlib.py -k "state or exchange"
- [ ] pytest tests/api/test_oauth_interaction_api.py -k "source_mismatch or interaction_payload_invalid"
- [ ] make test-impacted
</verification>

<success_criteria>
- All tasks completed.
- OAuth state-less or mismatched exchange attempts are rejected.
- Cross-source interaction overwrite path is blocked.
- Existing same-source auth flow regression remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-01-SUMMARY.md`
</output>
