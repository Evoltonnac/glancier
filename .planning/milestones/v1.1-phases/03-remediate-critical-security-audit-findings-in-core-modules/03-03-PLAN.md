---
phase: 03-remediate-critical-security-audit-findings-in-core-modules
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified: [core/api.py, core/auth/oauth_auth.py, core/steps/script_step.py, tests/core/test_scraper_internal_api.py, tests/api/test_integration_files_api.py]
autonomous: true
requirements: [SEC-01, INT-01]
user_setup: []
must_haves:
  truths:
    - "Sensitive token/secret fields are redacted consistently in API/auth/script logs."
    - "Internal scraper endpoints require service authentication, not localhost-only trust."
    - "Integration file APIs never expose absolute filesystem paths."
  artifacts:
    - path: "core/api.py"
      provides: "Shared redaction usage and authenticated internal scraper endpoint guard."
    - path: "core/auth/oauth_auth.py"
      provides: "No raw OAuth payload/token logging."
    - path: "core/steps/script_step.py"
      provides: "No raw script source dump in error logs."
  key_links:
    - from: "core/api.py"
      to: "tests/core/test_scraper_internal_api.py"
      via: "internal endpoint auth token requirement"
---

<objective>
Prevent secret leakage in logs and harden internal-only backend surfaces that currently rely on weak trust assumptions.

Purpose: Close confidentiality and error-exposure risks without removing diagnostics needed for support.
Output: Central redaction application, internal endpoint auth enforcement, and path-safe integration file API responses.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-CONTEXT.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-RESEARCH.md
@core/api.py
@core/auth/oauth_auth.py
@core/steps/script_step.py
@tests/core/test_scraper_internal_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply centralized sensitive-field redaction to auth/api/script logs</name>
  <files>core/api.py, core/auth/oauth_auth.py, core/steps/script_step.py</files>
  <read_first>core/api.py, core/auth/oauth_auth.py, core/steps/script_step.py, core/executor.py</read_first>
  <action>Add a reusable redaction utility with field list covering `token`, `access_token`, `refresh_token`, `secret`, `client_secret`, `code`, and `device_code`. Replace raw payload/script logging in these files with redacted structured logs. Remove any error logging that prints full script source (`script_code`) and ensure only step id, source id, and sanitized reason are logged. Use deterministic marker `[REDACTED]` in log payload values.</action>
  <verify>pytest tests/core/test_scraper_internal_api.py -k "redacted or auth"</verify>
  <acceptance_criteria>
    - core/auth/oauth_auth.py contains "[REDACTED]".
    - core/steps/script_step.py does not contain "logger.error(f\"Error executing script in step".
    - core/api.py contains redaction handling for "client_secret" and "device_code".
  </acceptance_criteria>
  <done>Sensitive fields are redacted consistently and raw credential/script leaks are removed from logs.</done>
</task>

<task type="auto">
  <name>Task 2: Require authenticated internal scraper endpoint access and path-safe file responses</name>
  <files>core/api.py</files>
  <read_first>core/api.py, tests/core/test_scraper_internal_api.py, tests/api/test_integration_files_api.py</read_first>
  <action>Strengthen `_verify_internal_request` to require a shared secret header (`X-Glanceus-Internal-Token`) with constant-time comparison to configured internal token, while keeping localhost check as a secondary gate. Return HTTP 403 with detail `internal_auth_required` when header is missing/invalid. Update integration files API serializers to return logical identifiers (`filename`, `integration_id`) and never absolute filesystem paths; add helper `_to_logical_integration_file` for this conversion.</action>
  <verify>pytest tests/core/test_scraper_internal_api.py tests/api/test_integration_files_api.py</verify>
  <acceptance_criteria>
    - core/api.py contains "X-Glanceus-Internal-Token".
    - core/api.py contains "internal_auth_required".
    - core/api.py contains "_to_logical_integration_file".
  </acceptance_criteria>
  <done>Internal scraper endpoints and integration file APIs no longer rely on weak trust or leak local filesystem structure.</done>
</task>

<task type="auto">
  <name>Task 3: Extend confidentiality and internal-endpoint regression tests</name>
  <files>tests/core/test_scraper_internal_api.py, tests/api/test_integration_files_api.py</files>
  <read_first>tests/core/test_scraper_internal_api.py, tests/api/test_integration_files_api.py, core/api.py</read_first>
  <action>Add tests that verify (1) missing/invalid internal token returns `internal_auth_required`, (2) valid token keeps current scraper task flow behavior, and (3) integration file APIs never include absolute paths (`/` rooted, drive-letter, or home dir expansions) in JSON responses. Assert exact response detail strings for deterministic checks.</action>
  <verify>pytest tests/core/test_scraper_internal_api.py tests/api/test_integration_files_api.py -k "internal_auth_required or logical"</verify>
  <acceptance_criteria>
    - tests/core/test_scraper_internal_api.py contains "internal_auth_required".
    - tests/api/test_integration_files_api.py contains assertions for "filename" and "integration_id".
    - pytest tests/core/test_scraper_internal_api.py tests/api/test_integration_files_api.py exits 0.
  </acceptance_criteria>
  <done>Confidentiality and internal endpoint hardening are covered by deterministic regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/core/test_scraper_internal_api.py tests/api/test_integration_files_api.py
- [ ] make test-impacted
- [ ] make test-backend
</verification>

<success_criteria>
- All tasks completed.
- Sensitive data no longer appears in raw auth/api/script logs.
- Internal scraper endpoints reject missing/invalid service authentication.
- Integration file API responses expose logical metadata only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-03-SUMMARY.md`
</output>
