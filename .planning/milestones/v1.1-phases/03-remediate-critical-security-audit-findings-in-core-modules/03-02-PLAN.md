---
phase: 03-remediate-critical-security-audit-findings-in-core-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [core/steps/http_step.py, core/parser.py, core/steps/script_step.py, tests/core/test_http_step.py, tests/core/test_parser_security.py]
autonomous: true
requirements: [SEC-03, INT-02]
user_setup: []
must_haves:
  truths:
    - "HTTP requests reject unsafe local/private/unsupported targets before network execution."
    - "Parser and script execution cannot load arbitrary filesystem paths outside approved integration roots."
  artifacts:
    - path: "core/steps/http_step.py"
      provides: "Outbound URL safety guard and deterministic rejection error codes."
    - path: "core/parser.py"
      provides: "Script parser path allowlist enforcement."
    - path: "core/steps/script_step.py"
      provides: "Script execution guardrails against unsafe local path usage."
  key_links:
    - from: "core/steps/http_step.py"
      to: "tests/core/test_http_step.py"
      via: "blocked-target regression assertions"
---

<objective>
Reduce RCE/SSRF blast radius for script/parser/http execution surfaces while keeping current integration behavior compatible.

Purpose: Eliminate critical unsafe-input expansion paths that can target internal/local resources or arbitrary scripts.
Output: URL policy enforcement, script path allowlist checks, and security-focused regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-CONTEXT.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-RESEARCH.md
@core/steps/http_step.py
@core/parser.py
@core/steps/script_step.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic outbound URL policy checks in HTTP step</name>
  <files>core/steps/http_step.py</files>
  <read_first>core/steps/http_step.py, core/executor.py, tests/core/test_http_step.py</read_first>
  <action>Create a preflight validator named `_validate_http_target_url` in `core/steps/http_step.py` that parses final request URL and blocks loopback, link-local, RFC1918 private IP ranges, and unsupported schemes (`file`, `ftp`, empty). Raise `RuntimeError` with exact code strings `http_target_blocked_private` or `http_target_blocked_scheme` and include sanitized URL host only (no credentials/query). Run this validator before creating `httpx.AsyncClient` request.</action>
  <verify>pytest tests/core/test_http_step.py -k "target_blocked_private or target_blocked_scheme"</verify>
  <acceptance_criteria>
    - core/steps/http_step.py contains "_validate_http_target_url".
    - core/steps/http_step.py contains "http_target_blocked_private".
    - core/steps/http_step.py contains "http_target_blocked_scheme".
  </acceptance_criteria>
  <done>Unsafe local/private or unsupported URL targets are rejected before network dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce parser/script local path allowlist boundaries</name>
  <files>core/parser.py, core/steps/script_step.py</files>
  <read_first>core/parser.py, core/steps/script_step.py, core/resource_manager.py, core/config_loader.py</read_first>
  <action>Implement shared path policy helper(s) in parser and script step that resolve candidate paths to absolute paths, deny parent traversal, and require path prefix under approved integration roots (`config/integrations/` and active data dir integration store). In `core/parser.py::_parse_script`, reject missing/unsafe path with `_error` values `script_path_not_allowed` or `script_path_missing`. In `core/steps/script_step.py`, reject unsafe script inputs with raised `RuntimeError(\"script_path_not_allowed\")` and avoid logging raw script code on errors.</action>
  <verify>pytest tests/core/test_parser_security.py -k "script_path_not_allowed or script_path_missing"</verify>
  <acceptance_criteria>
    - core/parser.py contains "script_path_not_allowed".
    - core/parser.py contains "script_path_missing".
    - core/steps/script_step.py contains "RuntimeError(\"script_path_not_allowed\")".
  </acceptance_criteria>
  <done>Script and parser execution no longer accept arbitrary filesystem paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add security regression tests for URL and path policy</name>
  <files>tests/core/test_http_step.py, tests/core/test_parser_security.py</files>
  <read_first>tests/core/test_http_step.py, core/steps/http_step.py, core/parser.py, core/steps/script_step.py</read_first>
  <action>Extend `tests/core/test_http_step.py` with blocking cases for `http://127.0.0.1`, `http://169.254.169.254`, and `file:///tmp/a` targets. Create `tests/core/test_parser_security.py` with parser/script path allowlist tests covering traversal (`../`), absolute outside-root paths, and allowed in-root scripts. Assertions must match exact error identifiers introduced in this plan.</action>
  <verify>pytest tests/core/test_http_step.py tests/core/test_parser_security.py</verify>
  <acceptance_criteria>
    - tests/core/test_http_step.py contains "http_target_blocked_private".
    - tests/core/test_parser_security.py contains "script_path_not_allowed".
    - pytest tests/core/test_http_step.py tests/core/test_parser_security.py exits 0.
  </acceptance_criteria>
  <done>Security policies for outbound targets and parser/script paths are continuously enforced by tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/core/test_http_step.py -k "target_blocked_private or target_blocked_scheme"
- [ ] pytest tests/core/test_http_step.py tests/core/test_parser_security.py
- [ ] make test-impacted
</verification>

<success_criteria>
- All tasks completed.
- Unsafe URL targets are blocked with deterministic error codes.
- Parser/script path traversal and outside-root access are blocked.
- Existing supported script/http behavior remains compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-02-SUMMARY.md`
</output>
