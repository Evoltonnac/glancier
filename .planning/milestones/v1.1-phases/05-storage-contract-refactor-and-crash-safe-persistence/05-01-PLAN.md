---
phase: 05-storage-contract-refactor-and-crash-safe-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/storage/contract.py
  - core/storage/sqlite_connection.py
  - core/storage/sqlite_runtime_repo.py
  - core/storage/sqlite_resource_repo.py
  - core/storage/settings_adapter.py
  - core/data_controller.py
  - core/resource_manager.py
  - main.py
  - tests/core/test_storage_contract_sqlite.py
  - tests/core/test_data_controller.py
autonomous: true
requirements: [STOR-01]
user_setup: []
must_haves:
  truths:
    - "Runtime Integration Data and resource entities persist through one backend-owned storage contract with explicit schema version."
    - "System settings remain on settings.json but are surfaced through the same storage contract boundary."
    - "Integration YAML and secrets.json behavior remains unchanged."
  artifacts:
    - path: "core/storage/contract.py"
      provides: "StorageContract protocol and schema version constants."
    - path: "core/storage/sqlite_runtime_repo.py"
      provides: "SQLite persistence for latest_by_source/history_by_source contracts."
    - path: "core/storage/sqlite_resource_repo.py"
      provides: "SQLite persistence for StoredSource/StoredView contracts."
    - path: "core/storage/settings_adapter.py"
      provides: "Settings JSON adapter exposed via unified storage contract."
    - path: "tests/core/test_storage_contract_sqlite.py"
      provides: "Contract-level regression coverage for schema version and CRUD parity."
  key_links:
    - from: "core/data_controller.py"
      to: "core/storage/sqlite_runtime_repo.py"
      via: "DataController delegates runtime read/write methods to SQLite repository methods."
    - from: "core/resource_manager.py"
      to: "core/storage/sqlite_resource_repo.py"
      via: "ResourceManager delegates source/view CRUD and source-reference cleanup to SQLite repository methods."
    - from: "main.py"
      to: "core/storage/contract.py"
      via: "create_app builds one storage contract instance and injects runtime/resource SQLite repositories plus a settings adapter backed by SettingsManager."
---

<objective>
Build Phase 5 storage foundation: a unified, versioned storage contract that moves runtime/resources persistence to SQLite while preserving JSON-backed settings/secrets contracts.

Purpose: Deliver STOR-01 with minimal blast radius by keeping API/executor call sites stable and swapping storage internals behind manager interfaces.
Output: Storage contract modules, SQLite repositories, controller adapters, and schema-version regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-RESEARCH.md
@core/data_controller.py
@core/resource_manager.py
@core/settings_manager.py
@core/models.py
@main.py
@tests/core/test_data_controller.py
@tests/api/test_source_delete_api.py

<interfaces>
From core/data_controller.py:
```python
def upsert(self, source_id: str, data: dict[str, Any]) -> None
def set_state(self, source_id: str, status: str, message: str | None = None, interaction: dict | None = None, error: str | None = None, error_code: str | None = None) -> None
def set_retry_metadata(self, source_id: str, metadata: dict[str, Any] | None) -> None
def clear_retry_metadata(self, source_id: str) -> None
def get_latest(self, source_id: str) -> dict | None
def get_all_latest(self) -> list[dict]
def get_history(self, source_id: str, limit: int = 100) -> list[dict]
def clear_source(self, source_id: str) -> None
```

From core/resource_manager.py:
```python
def load_sources(self) -> list[StoredSource]
def save_source(self, source: StoredSource) -> StoredSource
def delete_source(self, source_id: str) -> bool
def load_views(self) -> list[StoredView]
def save_view(self, view: StoredView) -> StoredView
def delete_view(self, view_id: str) -> bool
def remove_source_references_from_views(self, source_id: str) -> list[str]
```

From main.py:
```python
resource_manager = ResourceManager()
data_controller = DataController()
settings_manager = SettingsManager()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unified storage contract and schema-versioned SQLite repositories</name>
  <files>core/storage/contract.py, core/storage/sqlite_connection.py, core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, core/storage/settings_adapter.py, tests/core/test_storage_contract_sqlite.py</files>
  <read_first>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md, core/data_controller.py, core/resource_manager.py, core/settings_manager.py, core/models.py</read_first>
  <behavior>
    - Test 1: New storage bootstrap sets `PRAGMA user_version=1` and creates required runtime/resource tables.
    - Test 2: Runtime repository supports `upsert/get_latest/get_all_latest/get_history/clear_source` with the same payload fields currently consumed by API/scheduler.
    - Test 3: Resource repository supports `load/save/delete` for sources/views and source-reference removal in views.
  </behavior>
  <action>Create a `core/storage` package with a `StorageContract` protocol and explicit `STORAGE_SCHEMA_VERSION = 1`. Implement SQLite connection factory settings `foreign_keys=ON`, `journal_mode=WAL`, `busy_timeout=5000`, and schema bootstrap that applies table DDL then sets `PRAGMA user_version`. Implement runtime/resource repositories with deterministic primary keys (`source_id`, `view_id`) and JSON text columns for complex payloads. Add a settings adapter that delegates to existing `SettingsManager` so settings stay JSON-backed while exposed through the contract. Add `tests/core/test_storage_contract_sqlite.py` covering schema version and repository CRUD parity.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_contract_sqlite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_contract_sqlite.py -q` exits 0.
    - `rg -n "STORAGE_SCHEMA_VERSION\\s*=\\s*1" core/storage/contract.py` returns one match.
    - `rg -n "PRAGMA user_version" core/storage/sqlite_connection.py` returns schema-version enforcement code.
    - `rg -n "journal_mode\\s*=\\s*WAL|foreign_keys\\s*=\\s*ON|busy_timeout\\s*=\\s*5000" core/storage/sqlite_connection.py` returns all durability pragmas.
  </acceptance_criteria>
  <done>Unified storage contract and SQLite repositories exist with explicit schema-version behavior and passing contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor DataController and ResourceManager to delegate to storage contract</name>
  <files>core/data_controller.py, core/resource_manager.py, tests/core/test_data_controller.py, tests/api/test_source_delete_api.py</files>
  <read_first>core/data_controller.py, core/resource_manager.py, core/api.py, tests/core/test_data_controller.py, tests/api/test_source_delete_api.py</read_first>
  <behavior>
    - Test 1: Existing DataController tests continue passing with no API-level contract break.
    - Test 2: Source deletion still cascades runtime data cleanup and view reference cleanup through ResourceManager methods.
    - Test 3: Retry metadata persistence methods remain callable for RefreshScheduler compatibility.
  </behavior>
  <action>Replace direct JSON file persistence logic inside `DataController` and `ResourceManager` with storage-contract delegation while preserving all current public method names, arguments, and returned shapes. Keep backward-compatible constructor defaults so existing tests and bootstrap code continue to instantiate managers without caller-side changes. Preserve runtime fields (`status`, `message`, `interaction`, `error`, `error_code`, `retry_metadata`, timestamps) exactly as they are surfaced today.</action>
  <verify>
    <automated>python -m pytest tests/core/test_data_controller.py tests/api/test_source_delete_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_data_controller.py tests/api/test_source_delete_api.py -q` exits 0.
    - `rg -n "def set_retry_metadata|def clear_retry_metadata" core/data_controller.py` confirms scheduler contract methods are preserved.
    - `rg -n "def remove_source_references_from_views" core/resource_manager.py` confirms source-delete cleanup API remains.
  </acceptance_criteria>
  <done>Data and resource managers use the new storage backend internally without changing API/executor call contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire unified storage in app bootstrap while preserving settings/secrets JSON boundaries</name>
  <files>main.py, core/storage/contract.py, core/storage/settings_adapter.py, tests/core/test_storage_contract_sqlite.py, tests/api/test_settings_api.py</files>
  <read_first>main.py, core/settings_manager.py, core/secrets_controller.py, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md, tests/api/test_settings_api.py</read_first>
  <behavior>
    - Test 1: `main.create_app()` creates one shared storage contract instance that wires runtime/resources repositories plus a settings adapter.
    - Test 2: Settings API behavior and encryption-toggle migrations remain unchanged because settings/secrets stay JSON-based.
    - Test 3: Settings adapter methods delegate to `SettingsManager` JSON read/write paths rather than SQLite tables.
  </behavior>
  <action>Update `main.py` to initialize the storage contract once and inject it into `DataController` and `ResourceManager`, explicitly wiring `SettingsAdapter(SettingsManager(...))` through the same contract boundary. Keep `SettingsManager` and `SecretsController` storage backends unchanged (`data/settings.json`, `data/secrets.json`) and do not migrate secrets in this plan. Extend contract/API tests to assert settings access flows through the adapter while persisting to JSON.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_contract_sqlite.py tests/api/test_settings_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_contract_sqlite.py tests/api/test_settings_api.py -q` exits 0.
    - `rg -n "SettingsManager\\(|SecretsController\\(" main.py` confirms JSON settings/secrets managers remain in bootstrap.
    - `rg -n "SettingsAdapter|settings_adapter|settings_store" main.py core/storage/contract.py core/storage/settings_adapter.py` confirms settings-through-contract wiring exists.
  </acceptance_criteria>
  <done>App bootstrap uses one storage contract for runtime/resources/settings wiring while settings/secrets JSON contracts remain intact per locked phase decisions.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_data_controller.py -q
- [ ] python -m pytest tests/api/test_source_delete_api.py tests/api/test_settings_api.py -q
- [ ] make test-backend
</verification>

<success_criteria>
- STOR-01 behavior is implemented via a single backend-owned storage contract with explicit schema version.
- Runtime/resources persistence no longer depends on direct JSON file CRUD code paths.
- Settings and secrets remain JSON-backed and unchanged in behavior for Phase 5 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-01-SUMMARY.md`
</output>
