---
phase: 05-storage-contract-refactor-and-crash-safe-persistence
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - core/storage/migration.py
  - core/storage/errors.py
  - main.py
  - core/api.py
  - tests/core/test_storage_startup_migration.py
  - tests/core/test_storage_error_mapping.py
  - tests/api/test_storage_error_codes_api.py
  - .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md
  - .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md
  - docs/flow/06_storage_contract_and_migration.md
autonomous: true
requirements: [STOR-03, STOR-04]
user_setup: []
must_haves:
  truths:
    - "Startup automatically migrates legacy data.json/sources.json/views.json into SQLite without manual steps."
    - "Migration is idempotent and chunk-scoped; successfully migrated JSON chunks are renamed with Deprecated markers and skipped on later startups."
    - "Storage failures surface deterministic error_code diagnostics and release verification commands are repeatable."
  artifacts:
    - path: "core/storage/migration.py"
      provides: "Chunked idempotent startup migrator with per-chunk validation and deprecated-file marker behavior."
    - path: "tests/core/test_storage_startup_migration.py"
      provides: "Regression coverage for chunk migration, idempotency, validation, and deprecated-marker skip logic."
    - path: "tests/api/test_storage_error_codes_api.py"
      provides: "API-level verification for deterministic storage error_code responses."
    - path: ".planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md"
      provides: "Repeatable release gate checklist for STOR-01..STOR-04 verification."
  key_links:
    - from: "main.py"
      to: "core/storage/migration.py"
      via: "create_app executes startup migration before API/executor begin serving runtime calls."
    - from: "core/storage/errors.py"
      to: "core/api.py"
      via: "StorageContractError codes map to deterministic API error_code diagnostics."
    - from: "core/storage/migration.py"
      to: "core/storage/sqlite_runtime_repo.py"
      via: "Migrator writes chunks using repository upsert APIs and validates committed row counts."
---

<objective>
Finalize Phase 5 with startup migration and deterministic diagnostics: migrate legacy JSON data into SQLite safely and prove storage readiness with repeatable error-code verification gates.

Purpose: Deliver STOR-03 and STOR-04 while preserving locked compatibility constraints (settings/secrets JSON, no manual migration steps, no silent fallback on storage failures).
Output: Startup migrator, storage error-code API mapping tests, and phase release gate documentation.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-RESEARCH.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-02-PLAN.md
@core/storage/errors.py
@core/storage/sqlite_runtime_repo.py
@core/storage/sqlite_resource_repo.py
@core/api.py
@main.py

<interfaces>
From locked migration policy (05-CONTEXT.md):
```text
Chunk order: data.json -> sources.json -> views.json
Per chunk: import -> immediate validation -> mark done
Post-validation: rename source file to Deprecated marker (do not delete)
Idempotent startup: skip already-migrated/deprecated chunks
```

From core/api.py:
```python
@router.get("/sources")
@router.get("/sources/{source_id}/data")
@router.get("/sources/{source_id}/history")
@router.post("/sources")
@router.put("/sources/{source_id}")
@router.delete("/sources/{source_id}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build startup chunk migrator with idempotent deprecated-marker workflow</name>
  <files>core/storage/migration.py, main.py, tests/core/test_storage_startup_migration.py</files>
  <read_first>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md, core/data_controller.py, core/resource_manager.py, main.py, tests/core/test_data_controller.py</read_first>
  <behavior>
    - Test 1: On startup, migrator imports legacy `data.json`, `sources.json`, and `views.json` into SQLite in file-chunk order.
    - Test 2: After each chunk import, validation runs immediately and only then renames the source file to `<name>.deprecated.v1.json`.
    - Test 3: Re-running startup after successful migration does not duplicate rows and skips deprecated-marked files.
  </behavior>
  <action>Create `core/storage/migration.py` with explicit chunk handlers for `data.json`, `sources.json`, and `views.json`. Each handler must: parse JSON, execute UPSERT writes in one transaction, run chunk-level validation (row count and key presence), and rename the chunk file to `<original>.deprecated.v1.json` only after validation passes. Add a startup entrypoint called from `main.create_app()` before API initialization. On startup, skip files already carrying the `.deprecated.v1.json` suffix and treat missing files as no-op. Keep migration failure behavior deterministic by raising `StorageContractError` instead of silently continuing.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_startup_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_startup_migration.py -q` exits 0.
    - `rg -n "data\\.json|sources\\.json|views\\.json" core/storage/migration.py` shows explicit per-file chunk handlers.
    - `rg -n "\\.deprecated\\.v1\\.json" core/storage/migration.py` confirms rename marker convention.
    - `rg -n "run_startup_migration|migrate_on_startup" main.py` confirms startup hook exists before API init.
  </acceptance_criteria>
  <done>Legacy JSON chunk migration runs automatically at startup, validates each chunk, and is idempotent across restarts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire deterministic storage error_code diagnostics into API responses</name>
  <files>core/storage/errors.py, core/api.py, tests/core/test_storage_error_mapping.py, tests/api/test_storage_error_codes_api.py</files>
  <read_first>core/storage/errors.py, core/api.py, core/error_formatter.py, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Storage read/write/integrity failures map to stable API-level `error_code` values.
    - Test 2: API responses preserve deterministic machine-readable diagnostics instead of generic-only error strings.
    - Test 3: Existing non-storage endpoint success contracts remain unchanged.
  </behavior>
  <action>Extend `StorageContractError` mapping with canonical codes and a helper that converts storage exceptions into API-safe error envelopes. In `core/api.py`, wrap storage-dependent endpoints (`/sources`, `/views`, `/sources/{id}/data`, `/sources/{id}/history`, source delete cleanup paths) to catch storage exceptions and return deterministic payloads containing `error_code` plus stable summary text. Keep HTTP status mapping fixed (`storage.read_failed` -> 500, `storage.integrity_violation` -> 500, `storage.schema_mismatch` -> 503, `storage.write_failed` -> 500) unless endpoint semantics already require another status. Add unit/API tests asserting exact `error_code` fields.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_error_mapping.py tests/api/test_storage_error_codes_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_error_mapping.py tests/api/test_storage_error_codes_api.py -q` exits 0.
    - `rg -n "error_code" core/api.py` includes new storage-failure response paths.
    - `rg -n "storage\\.read_failed|storage\\.write_failed|storage\\.integrity_violation|storage\\.schema_mismatch" tests/api/test_storage_error_codes_api.py` confirms deterministic code assertions.
  </acceptance_criteria>
  <done>Storage failures are surfaced through deterministic API `error_code` diagnostics with regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Phase 5 verification and architecture documentation</name>
  <files>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md, docs/flow/06_storage_contract_and_migration.md</files>
  <read_first>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md, .planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-SECURITY-GATE.md, docs/flow/05_refresh_scheduler_and_retry.md, AGENTS.md</read_first>
  <action>Replace placeholder content in `05-VALIDATION.md` with Nyquist-compliant Phase 5 command matrix, concrete quick/full backend commands, and requirement-to-test mapping for STOR-01..STOR-04. Add `05-STORAGE-GATE.md` documenting repeatable PASS/FAIL release gate commands and required evidence capture. Add `docs/flow/06_storage_contract_and_migration.md` describing storage boundaries (SQLite vs JSON), migration marker lifecycle (`.deprecated.v1.json`), crash-recovery semantics, and deterministic storage error_code contract for operators.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_startup_migration.py tests/core/test_storage_error_mapping.py tests/api/test_storage_error_codes_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "STOR-01|STOR-02|STOR-03|STOR-04" .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md` returns all requirement IDs.
    - `rg -n "deprecated\\.v1\\.json|storage\\.write_failed|storage\\.schema_mismatch" docs/flow/06_storage_contract_and_migration.md` confirms contract details are documented.
    - `python -m pytest tests/core/test_storage_startup_migration.py tests/core/test_storage_error_mapping.py tests/api/test_storage_error_codes_api.py -q` exits 0.
  </acceptance_criteria>
  <done>Phase 5 has release-ready verification docs and updated flow documentation aligned with implemented storage contracts.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_storage_startup_migration.py tests/core/test_storage_error_mapping.py tests/api/test_storage_error_codes_api.py -q
- [ ] make test-backend
- [ ] make test-impacted
</verification>

<success_criteria>
- STOR-03 migration behavior is automatic, chunked, validated, and idempotent at startup.
- STOR-04 diagnostics expose deterministic storage `error_code` values through backend APIs and verification gates.
- Phase 5 validation and flow docs are synchronized with the new storage architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-03-SUMMARY.md`
</output>
