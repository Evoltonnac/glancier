---
phase: 04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [core/executor.py, core/refresh_scheduler.py, tests/core/test_scraper_states.py, tests/core/test_refresh_scheduler.py]
autonomous: true
requirements: [PH4-01, PH4-02]
user_setup: []
must_haves:
  truths:
    - "WebView blocked/auth-required interactions do not rely on implicit foreground forcing metadata."
    - "Uncertain runtime failures are retry-eligible with bounded automatic retries."
    - "Manual-required auth failures are excluded from automatic retry loops."
  artifacts:
    - path: "core/executor.py"
      provides: "Deterministic failure classification for manual-required vs retryable uncertain outcomes."
    - path: "core/refresh_scheduler.py"
      provides: "Status + error_code-based retry policy with explicit backoff and retry cap."
    - path: "tests/core/test_refresh_scheduler.py"
      provides: "Regression coverage for retry allowlist, backoff cadence, and retry cap."
  key_links:
    - from: "core/executor.py"
      to: "core/refresh_scheduler.py"
      via: "Persisted error_code values consumed by scheduler retry eligibility checks."
    - from: "core/refresh_scheduler.py"
      to: "core/executor.py"
      via: "_refresh_source queues bounded auto retries through executor.fetch_source()."
---

<objective>
Define backend failure-policy contracts for Phase 4: remove implicit foreground fallback metadata from automatic WebView failure handling and add bounded auto-retry for uncertain failures.

Purpose: Stabilize hidden/auxiliary scraping by preventing focus-stealing fallback assumptions and recovering transient failures automatically.
Output: Updated executor classification logic + scheduler retry policy + deterministic backend tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-RESEARCH.md
@.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-VALIDATION.md
@core/executor.py
@core/refresh_scheduler.py
@tests/core/test_scraper_states.py
@tests/core/test_refresh_scheduler.py

<interfaces>
From core/executor.py:
```python
def _normalize_interaction_error(self, source: SourceConfig, error: Exception) -> Exception
def _classify_http_status_error(self, source: SourceConfig, step: StepConfig | None, error: httpx.HTTPStatusError) -> Exception
def _exception_to_interaction(self, source: SourceConfig, error: Exception) -> InteractionRequest | None
```

From core/refresh_scheduler.py:
```python
async def _tick_once(self) -> None
async def _enqueue(self, source_id: str, reason: str) -> bool
async def _refresh_source(self, source_id: str, reason: str) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic WebView failure classification without implicit foreground forcing</name>
  <files>core/executor.py, tests/core/test_scraper_states.py</files>
  <read_first>core/executor.py, core/source_state.py, tests/core/test_scraper_states.py</read_first>
  <behavior>
    - Test 1: WebView auth-wall/captcha/login signals map to `InteractionType.WEBVIEW_SCRAPE` with `manual_only=true` and no `force_foreground` default.
    - Test 2: WebView uncertain/transient failures map to deterministic retryable code (`runtime.retry_required`) instead of manual foreground interaction.
    - Test 3: Existing `runtime.network_timeout` handling remains deterministic and persisted.
  </behavior>
  <action>Refactor executor error classification to explicit policy buckets. Add a retryable-runtime path for uncertain failures (for example generic 403 without auth challenge, timeout-like transient failure text) and keep manual-required path only for clear auth-wall signals (`captcha`, `login`, `auth required`, `verification`). Remove default `force_foreground` writes from automatic WebView interaction payload construction while preserving `manual_only=true` for manual-required cases. Extend scraper-state regression tests to assert the new payload and error_code contracts.</action>
  <verify>
    <automated>python -m pytest tests/core/test_scraper_states.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/core/test_scraper_states.py` asserts no automatic `force_foreground` contract on manual-required payloads.
    - Uncertain WebView failures now produce a retryable deterministic runtime code.
    - Existing timeout classification still emits `runtime.network_timeout`.
  </acceptance_criteria>
  <done>Executor produces deterministic manual-required vs retryable-uncertain classification aligned with Phase 4 policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace status-only scheduler gating with bounded retry policy for uncertain failures</name>
  <files>core/refresh_scheduler.py, tests/core/test_refresh_scheduler.py</files>
  <read_first>core/refresh_scheduler.py, tests/core/test_refresh_scheduler.py, core/data_controller.py</read_first>
  <behavior>
    - Test 1: `status=error` + `error_code=runtime.network_timeout` is retry-enqueued after configured backoff.
    - Test 2: `status=suspended`/`error` with non-retryable codes (for example `auth.manual_webview_required`) is not auto-enqueued.
    - Test 3: Retry attempts cap at exactly 3 with deterministic backoff windows (60s, 180s, 600s).
  </behavior>
  <action>In `RefreshScheduler`, keep existing active interval refresh path unchanged, and add retry branch keyed by `(status, error_code)`. Implement allowlist `runtime.network_timeout` + `runtime.retry_required` (and keep extension slot for future transient runtime codes), with explicit per-source retry state, backoff schedule `[60, 180, 600]` seconds, and hard attempt cap `3`. Retry attempts must increment only when enqueue succeeds, reset when source returns `active` or error signature changes, and never run for manual-required/auth-deterministic codes. Expand scheduler unit tests to cover allowlist, backoff, and cap behavior.</action>
  <verify>
    <automated>python -m pytest tests/core/test_refresh_scheduler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Non-active transient runtime failures are retry-eligible by `error_code`, not blocked by status-only filtering.
    - Retry budget is bounded to 3 attempts with deterministic backoff intervals 60/180/600 seconds.
    - Manual-required/auth failures remain excluded from auto retry.
  </acceptance_criteria>
  <done>Scheduler supports bounded automatic retries for uncertain failures without regressing deterministic/manual failure handling.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_scraper_states.py -q
- [ ] python -m pytest tests/core/test_refresh_scheduler.py -q
- [ ] make test-backend
</verification>

<success_criteria>
- Backend classification no longer depends on implicit foreground forcing metadata.
- Uncertain failure retries are automatic, bounded, and deterministic.
- Existing deterministic timeout/manual-required behavior remains covered by regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-01-SUMMARY.md`
</output>
