---
phase: 04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures
plan: 04
type: execute
mode: gap_closure
wave: 4
depends_on: [03]
files_modified:
  - ui-react/src/hooks/useScraper.ts
  - ui-react/src-tauri/src/scraper.rs
  - core/api.py
  - core/executor.py
  - tests/core/test_scraper_internal_api.py
  - tests/core/test_scraper_states.py
  - ui-react/src/hooks/useScraper.test.ts
autonomous: false
requirements: []
source_gaps: [1, 2]
user_setup: []
must_haves:
  truths:
    - "Auth-required scraper failures stay in manual-required state and are not overwritten by timeout/cancel side effects."
    - "Ambiguous webview failures can surface manual recovery interaction instead of forced retry-required error-only state."
  artifacts:
    - path: "ui-react/src/hooks/useScraper.ts"
      provides: "Timeout lifecycle that does not auto-cancel tasks after auth-required handoff."
    - path: "ui-react/src-tauri/src/scraper.rs"
      provides: "Auth-required handoff with explicit lifecycle termination semantics for watchdog compatibility."
    - path: "core/api.py"
      provides: "Deterministic fail classification that supports ambiguous/manual-recovery branch."
---

<objective>
Close diagnosed Gap 1 and Gap 2 by stabilizing auth-required handoff and replacing strict keyword fallback with explicit ambiguous-manual recovery behavior.

Purpose: prevent manual-required state regression and ensure uncertain webview failures do not force users into opaque retry-only errors.
Output: backend+frontend+tauri contract updates with regression tests for auth handoff, cancel semantics, and uncertain classification.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
@.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-UAT.md
@.planning/debug/phase04-auth-wall-state-overwritten-by-timeout-cancel.md
@.planning/debug/phase04-uncertain-failure-classification-too-aggressive.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prevent auth-required handoff from being overwritten by timeout/cancel path</name>
  <files>ui-react/src/hooks/useScraper.ts, ui-react/src-tauri/src/scraper.rs, core/api.py, ui-react/src/hooks/useScraper.test.ts, tests/core/test_scraper_internal_api.py</files>
  <action>
    Add explicit lifecycle transition for auth-required handoff so frontend timeout tracking stops for that task. Ensure `cancel_scraper_task` and related fail callbacks cannot regress a source already moved to manual-required state for the same task. Keep deterministic error_code behavior and idempotent internal callback handling.
  </action>
  <verify>
    <automated>make test-frontend</automated>
    <automated>make test-backend</automated>
  </verify>
  <acceptance_criteria>
    - Source remains `suspended` + `auth.manual_webview_required` after auth-required handoff.
    - No subsequent "Scraper task cancelled by user" state overwrite for the same auth-required event.
    - Existing idempotency behavior for repeated fail callbacks remains intact.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ambiguous webview failure branch with manual recovery guidance</name>
  <files>core/api.py, core/executor.py, tests/core/test_scraper_internal_api.py, tests/core/test_scraper_states.py</files>
  <action>
    Introduce explicit ambiguous-failure classification for webview failures where auth cannot be confidently ruled out (including timeout-like uncertainty). Persist deterministic error_code while surfacing manual webview interaction guidance instead of hard retry-only error where policy requires user intervention.
  </action>
  <verify>
    <automated>make test-backend</automated>
  </verify>
  <acceptance_criteria>
    - Ambiguous failures produce actionable manual-recovery interaction payload.
    - Error-code contract remains deterministic and stable.
    - No regression to existing manual keyword detection coverage.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify 401/403 auth-wall UAT behavior in desktop runtime</name>
  <files>ui-react/src/hooks/useScraper.ts, ui-react/src-tauri/src/scraper.rs, core/api.py</files>
  <action>Run phase-04 UAT scenario for auth wall: trigger scraper auth-required condition and confirm state does not regress to cancel error; confirm manual foreground action remains available.</action>
  <how-to-verify>
    1. Trigger a 401/403 source scrape via backend-managed queue flow.
    2. Confirm source becomes `suspended` with manual-required diagnostics.
    3. Wait longer than configured scraper timeout and verify state does not flip to cancellation error.
    4. Trigger explicit foreground/manual recovery action and verify it still works.
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass; otherwise provide observed state transitions.</resume-signal>
</task>

</tasks>

<verification>
- [ ] make test-backend
- [ ] make test-frontend
- [ ] make test-impacted
</verification>

<success_criteria>
- Gap 1 and Gap 2 root causes are removed with regression tests.
- Manual-required auth handoff is stable and not overwritten by timeout side effects.
- Ambiguous webview failures provide manual recovery path with deterministic diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-04-SUMMARY.md`.
</output>
