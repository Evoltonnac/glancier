---
phase: 04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures
plan: 05
type: execute
mode: gap_closure
wave: 4
depends_on: [03]
files_modified:
  - core/refresh_scheduler.py
  - core/data_controller.py
  - tests/core/test_refresh_scheduler.py
autonomous: true
requirements: []
source_gaps: [3, 4]
user_setup: []
must_haves:
  truths:
    - "Retry budget for runtime.retry_required and runtime.network_timeout is bounded and cannot loop infinitely."
    - "Backoff timing is decoupled from generic updated_at mutations."
  artifacts:
    - path: "core/refresh_scheduler.py"
      provides: "Stable retry metadata and bounded retry progression logic."
    - path: "tests/core/test_refresh_scheduler.py"
      provides: "Regression coverage for no-reset budget and cap enforcement across state churn."
---

<objective>
Close diagnosed Gap 3 and Gap 4 by redesigning retry budget/backoff bookkeeping to avoid active-state resets and updated_at coupling.

Purpose: enforce deterministic bounded retries and eliminate observed ~80s infinite retry loops.
Output: scheduler retry-state redesign plus regression tests that model transient status updates and cap behavior.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
@.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-UAT.md
@.planning/debug/phase04-retry-budget-resets-and-backoff-coupled-to-updated-at.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce stable retry metadata independent of updated_at</name>
  <files>core/refresh_scheduler.py, core/data_controller.py, tests/core/test_refresh_scheduler.py</files>
  <action>
    Track retry progression with explicit metadata (`attempts`, `first_failed_at`, `next_retry_at` or equivalent) keyed by source+error signature. Ensure metadata persists through transient non-terminal state updates and is reset only on success/signature change.
  </action>
  <verify>
    <automated>make test-backend</automated>
  </verify>
  <acceptance_criteria>
    - Retry attempts stop at configured cap for both allowlisted runtime codes.
    - Transient `active` transitions do not reset retry budget.
    - Backoff gating does not depend on generic `updated_at` mutations.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for loop-prevention scenario from UAT</name>
  <files>tests/core/test_refresh_scheduler.py</files>
  <action>
    Add focused tests reproducing observed failure mode: repeated retries with periodic status churn and updated_at changes. Verify scheduler terminates retries at cap and does not re-enter first-window loop.
  </action>
  <verify>
    <automated>make test-backend</automated>
  </verify>
  <acceptance_criteria>
    - New tests fail on pre-fix behavior and pass with fix.
    - Tests cover both `runtime.retry_required` and `runtime.network_timeout`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] make test-backend
- [ ] make test-impacted
</verification>

<success_criteria>
- Runtime retry loops are capped deterministically.
- Scheduler behavior matches UAT expectation under real state churn.
</success_criteria>

<output>
After completion, create `.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-05-SUMMARY.md`.
</output>
