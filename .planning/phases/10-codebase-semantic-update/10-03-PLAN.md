---
phase: 10-codebase-semantic-update
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified: ["ui-react/package.json", "ui-react/src/App.tsx", "ui-react/src/pages/Dashboard.tsx", "ui-react/src/hooks/useSidebar.ts", "ui-react/src/hooks/useScraper.ts"]
autonomous: false
requirements: ["UI Refactoring"]
must_haves:
  truths:
    - "Dashboard loads correctly without errors"
    - "Sidebar toggles smoothly without lag"
    - "Scraper status updates seamlessly in the UI"
  artifacts:
    - path: "ui-react/src/pages/Dashboard.tsx"
      provides: "Main dashboard layout"
    - path: "ui-react/package.json"
      provides: "zustand dependency"
  key_links:
    - from: "ui-react/src/pages/Dashboard.tsx"
      to: "ui-react/src/hooks/useSidebar.ts"
      via: "Zustand state connection"
---

<objective>
Refactor App.tsx by introducing Zustand for global state and extracting hooks.
Purpose: Pay down UI technical debt and improve component boundaries for v1.0.
Output: Modular React architecture with Zustand state management.
</objective>

<context>
@.planning/phases/10-codebase-semantic-update/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zustand and Create State Store</name>
  <files>ui-react/package.json, ui-react/src/store/index.ts</files>
  <action>Install `zustand` as a dependency. Create a central store in `src/store/index.ts` to hold global state previously managed in App.tsx (e.g., sidebar toggle state, scraper status).</action>
  <verify>
    <automated>grep "zustand" ui-react/package.json</automated>
  </verify>
  <done>Zustand store is initialized.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Hooks and Refactor App.tsx</name>
  <files>ui-react/src/App.tsx, ui-react/src/pages/Dashboard.tsx, ui-react/src/hooks/useSidebar.ts, ui-react/src/hooks/useScraper.ts</files>
  <action>Create `Dashboard.tsx` to handle the main grid layout. Extract Sidebar logic to `useSidebar.ts` and Scraper status polling to `useScraper.ts`, both connecting to the Zustand store. Refactor `App.tsx` to simply render providers, routing (if any), and the `Dashboard` page.</action>
  <verify>
    <automated>ls ui-react/src/pages/Dashboard.tsx</automated>
  </verify>
  <done>App.tsx is split into modular components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Refactoring</name>
  <what-built>Refactored App.tsx with Zustand</what-built>
  <how-to-verify>
    1. Run the frontend dev server (`cd ui-react && npm run dev`)
    2. Verify the dashboard loads without React errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>
