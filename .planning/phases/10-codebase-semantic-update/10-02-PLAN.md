---
phase: 10-codebase-semantic-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["ui-react/src-tauri/tauri.conf.json", "ui-react/index.html", "ui-react/src/components/TopNav.tsx", "ui-react/src/components/widgets/QuotaBar.tsx", "ui-react/src/components/widgets/WidgetRenderer.tsx", "ui-react/src/index.css", "ui-react/public/vite.svg"]
autonomous: true
requirements: ["Brand & Terminology Update", "UI Refactoring", "ICON/品牌色更新"]
must_haves:
  truths:
    - "UI displays Glancier branding in title and navigation"
    - "Widgets use updated terminology in components"
    - "App uses new Glancier brand colors and icons"
  artifacts:
    - path: "ui-react/src-tauri/tauri.conf.json"
      provides: "Tauri app metadata"
    - path: "ui-react/src/components/widgets/ProgressBar.tsx"
      provides: "Renamed widget component"
  key_links:
    - from: "ui-react/src/components/widgets/WidgetRenderer.tsx"
      to: "ui-react/src/components/widgets/ProgressBar.tsx"
      via: "component mapping"
---

<objective>
Update UI branding artifacts, HTML titles, and rename legacy widget components.
Purpose: Visually align the frontend client with the new Glancier brand identity.
Output: Updated UI branding and renamed widget files.
</objective>

<context>
@.planning/phases/10-codebase-semantic-update/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App Branding and Meta</name>
  <files>ui-react/src-tauri/tauri.conf.json, ui-react/index.html, ui-react/src/components/TopNav.tsx</files>
  <action>Change `productName` and `title` from "Quota Board" to "Glancier" in `tauri.conf.json`. Update the `<title>` tag in `index.html`. Update branding text in `TopNav.tsx`.</action>
  <verify>
    <automated>grep -i "Glancier" ui-react/index.html</automated>
  </verify>
  <done>Frontend displays new brand name.</done>
</task>

<task type="auto">
  <name>Task 2: Rename Widget Components</name>
  <files>ui-react/src/components/widgets/QuotaBar.tsx, ui-react/src/components/widgets/ProgressBar.tsx, ui-react/src/components/widgets/WidgetRenderer.tsx</files>
  <action>Rename `QuotaBar.tsx` to `ProgressBar.tsx` (using file moving/creation). Update `WidgetRenderer.tsx` component map to import and render `ProgressBar` instead of `QuotaBar`.</action>
  <verify>
    <automated>ls ui-react/src/components/widgets/ProgressBar.tsx</automated>
  </verify>
  <done>Widget components semantically match new schema.</done>
</task>

<task type="auto">
  <name>Task 3: Update App Icons and Theme Colors</name>
  <files>ui-react/src/index.css, ui-react/public/vite.svg</files>
  <action>Update primary and secondary CSS variables in `index.css` to reflect the new Glancier brand colors. Replace the default or legacy favicon (`vite.svg` or `favicon.ico`) and Tauri app icons with the new Glancier logo/icon assets.</action>
  <verify>
    <automated>grep -i "primary" ui-react/src/index.css</automated>
  </verify>
  <done>Brand colors and icons are fully updated.</done>
</task>

</tasks>