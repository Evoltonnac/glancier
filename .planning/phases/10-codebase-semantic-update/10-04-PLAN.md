---
phase: 10-codebase-semantic-update
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified: ["scripts/build.sh", "ui-react/src-tauri/tauri.conf.json", "ui-react/src-tauri/src/main.rs", "ui-react/src-tauri/src/lib.rs", "ui-react/src/api/client.ts", "ui-react/src/hooks/useScraper.ts"]
autonomous: true
requirements: ["Brand & Terminology Update"]
must_haves:
  truths:
    - "Application launches successfully with the new backend"
    - "Tauri IPC events use glancier terminology"
  artifacts:
    - path: "scripts/build.sh"
      provides: "Updated build script"
    - path: "ui-react/src-tauri/src/main.rs"
      provides: "Updated Rust IPC logic"
  key_links:
    - from: "ui-react/src-tauri/tauri.conf.json"
      to: "scripts/build.sh"
      via: "sidecar binary naming"
---

<objective>
Update build scripts, sidecar binary configurations, and Rust IPC events to reflect the new brand.
Purpose: Ensure the packaged Tauri application completely eliminates "quota-board" artifacts.
Output: Fully rebranded Tauri build pipeline.
</objective>

<context>
@.planning/phases/10-codebase-semantic-update/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Build Scripts and Tauri Config</name>
  <files>scripts/build.sh, ui-react/src-tauri/tauri.conf.json</files>
  <action>Update `build.sh` to name the generated Python sidecar binary `glancier-server` instead of `quota-board-server`. Update `tauri.conf.json` externalBin configuration to point to `glancier-server`. Update any CLI arguments passed to it.</action>
  <verify>
    <automated>! grep -r "quota-board-server" scripts/build.sh</automated>
  </verify>
  <done>Build process targets glancier-server.</done>
</task>

<task type="auto">
  <name>Task 2: Update Rust Source IPC and TypeScript Strings</name>
  <files>ui-react/src-tauri/src/main.rs, ui-react/src-tauri/src/lib.rs, ui-react/src/api/client.ts, ui-react/src/hooks/useScraper.ts</files>
  <action>Search for and replace all instances of "quota-board" and "quota_board" in the Rust codebase. Update IPC event names and logging to use "glancier". Ensure command sidecar spawning refers to the new binary name. In TypeScript files calling IPC events (e.g., useScraper.ts, client.ts), explicitly update the `invoke()` calls to match the new Rust IPC event names.</action>
  <verify>
    <automated>! grep -r "quota_board" ui-react/src-tauri/src/</automated>
  </verify>
  <done>Rust backend and frontend IPC calls fully updated.</done>
</task>

</tasks>