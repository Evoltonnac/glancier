---
phase: 10-codebase-semantic-update
plan: 05
type: execute
wave: 2
depends_on: ["03"]
files_modified: ["ui-react/src/components/ui/MetricCard.tsx", "ui-react/src/components/ui/BentoWidget.tsx", "ui-react/src/types/index.ts"]
autonomous: false
requirements: ["抽象业务组件"]
must_haves:
  truths:
    - "UI components are modular and decoupled"
    - "TypeScript interfaces are centrally organized"
  artifacts:
    - path: "ui-react/src/components/ui/MetricCard.tsx"
      provides: "Reusable metric card component"
    - path: "ui-react/src/types/index.ts"
      provides: "Centralized types"
  key_links: []
---

<objective>
Extract reusable UI components and organize TypeScript definitions.
Purpose: Implement a mixed type organization approach and standardize UI blocks.
Output: Abstract business components and organized types.
</objective>

<context>
@.planning/phases/10-codebase-semantic-update/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abstract Business Components</name>
  <files>ui-react/src/components/ui/MetricCard.tsx, ui-react/src/components/ui/BentoWidget.tsx</files>
  <action>Implement generic, reusable business components `MetricCard` and `BentoWidget` to standardize UI presentation across the dashboard. Extract these from existing monolithic widgets or create them as foundational UI blocks for the new design system.</action>
  <verify>
    <automated>ls ui-react/src/components/ui/MetricCard.tsx</automated>
  </verify>
  <done>MetricCard and BentoWidget components exist and are ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Organize Types using Mixed Approach</name>
  <files>ui-react/src/types/index.ts</files>
  <action>Move globally shared types and interfaces (e.g., core configuration schemas, API response types) to the central `src/types/` directory. Keep component-specific types (e.g., props) colocated within their respective component files, adhering to the mixed approach decision in CONTEXT.md.</action>
  <verify>
    <automated>ls ui-react/src/types/</automated>
  </verify>
  <done>Types are organized according to the mixed approach.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Components and Types</name>
  <what-built>Abstract components and organized types</what-built>
  <how-to-verify>
    1. Run the frontend dev server (`cd ui-react && npm run dev`)
    2. Verify the application type checks successfully and UI renders properly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>
