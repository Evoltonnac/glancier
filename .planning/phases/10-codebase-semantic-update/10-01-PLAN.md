---
phase: 10-codebase-semantic-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["core/config_loader.py", "core/integration_manager.py", "core/data_controller.py", "core/secrets_controller.py", "core/settings_manager.py", "main.py"]
autonomous: true
requirements: ["UI Refactoring", "Brand & Terminology Update"]
must_haves:
  truths:
    - "Backend uses GLANCIER_DATA_DIR for data path"
    - "Application strictly enforces new widget keys and fails on legacy keys"
    - "Main application server identity is Glancier"
  artifacts:
    - path: "core/config_loader.py"
      provides: "Strict config validation without legacy aliases"
    - path: "main.py"
      provides: "Server title updated"
  key_links:
    - from: "main.py"
      to: "core/config_loader.py"
      via: "startup config validation"
---

<objective>
Update backend environment variables, configuration schemas, and application identity to strictly use Glancier terminology.
Purpose: Enforce new branding and drop legacy technical debt without backward compatibility, per context decisions.
Output: Updated core configuration and data management modules.
</objective>

<context>
@.planning/phases/10-codebase-semantic-update/10-CONTEXT.md
@.planning/phases/10-codebase-semantic-update/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Environment Variables and Data Paths</name>
  <files>core/settings_manager.py, core/secrets_controller.py, core/data_controller.py, core/integration_manager.py, core/config_loader.py</files>
  <action>Replace `QUOTA_BOARD_ROOT` with `GLANCIER_DATA_DIR` across all path resolutions. Remove any fallback to `QUOTA_BOARD_ROOT`. Update the hardcoded `.quota-board` data directories to `.glancier` if any exist. Drop all support for legacy environments.</action>
  <verify>
    <automated>! grep -r "QUOTA_BOARD_ROOT" core/</automated>
  </verify>
  <done>All backend paths resolve using GLANCIER_DATA_DIR.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce Strict Config Schema</name>
  <files>core/config_loader.py</files>
  <action>Remove `QUOTA_CARD` from `ViewComponentType` and ensure `SOURCE_CARD` is used. Remove any Pydantic aliases or fallback mapping that translates `quota_card` or `quota_bar` to new keys. Strict validation must fail on old keys.</action>
  <verify>
    <automated>! grep -r "QUOTA_CARD" core/config_loader.py</automated>
  </verify>
  <done>Config loader strictly requires source_card and progress_bar.</done>
</task>

<task type="auto">
  <name>Task 3: Update Main Server Identity and Startup Validation</name>
  <files>main.py</files>
  <action>Update the FastAPI `title` and any startup logging messages from "Quota Board" to "Glancier". Explicitly call `config_loader.py` logic to validate the configuration schema at startup, ensuring any legacy configs fail early.</action>
  <verify>
    <automated>grep -i "Glancier" main.py</automated>
  </verify>
  <done>Server identity reflects new brand and validates config on startup.</done>
</task>

</tasks>