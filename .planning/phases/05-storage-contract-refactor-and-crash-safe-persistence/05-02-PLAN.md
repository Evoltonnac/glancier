---
phase: 05-storage-contract-refactor-and-crash-safe-persistence
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - core/storage/errors.py
  - core/storage/sqlite_connection.py
  - core/storage/sqlite_runtime_repo.py
  - core/storage/sqlite_resource_repo.py
  - core/data_controller.py
  - core/resource_manager.py
  - core/scraper_task_store.py
  - core/api.py
  - tests/core/test_storage_crash_recovery.py
  - tests/core/test_scraper_task_store.py
autonomous: true
requirements: [STOR-02]
user_setup: []
must_haves:
  truths:
    - "Interrupted writes do not expose partial runtime/resource state."
    - "Last known-good Integration Data remains readable after simulated write failure."
    - "Scraper queue state is runtime-memory only and does not rely on durable JSON task files."
  artifacts:
    - path: "core/storage/sqlite_runtime_repo.py"
      provides: "Transaction-guarded runtime writes with rollback semantics."
    - path: "core/storage/sqlite_resource_repo.py"
      provides: "Transaction-guarded source/view writes and cleanup operations."
    - path: "core/storage/errors.py"
      provides: "Canonical storage exception taxonomy with stable error_code values."
    - path: "core/scraper_task_store.py"
      provides: "In-memory scraper task lifecycle store."
    - path: "tests/core/test_storage_crash_recovery.py"
      provides: "Fault-injection tests proving last-known-good persistence after interrupted writes."
  key_links:
    - from: "core/storage/sqlite_runtime_repo.py"
      to: "core/data_controller.py"
      via: "DataController delegates state writes that must run inside transaction wrappers."
    - from: "core/storage/sqlite_resource_repo.py"
      to: "core/resource_manager.py"
      via: "ResourceManager CRUD operations become transaction-backed and rollback-safe."
    - from: "core/scraper_task_store.py"
      to: "core/api.py"
      via: "Internal scraper endpoints preserve task contract while queue persistence becomes memory-only."
---

<objective>
Harden Phase 5 write semantics for crash safety and replace durable scraper-task storage with memory-only runtime behavior.

Purpose: Deliver STOR-02 by guaranteeing atomic write/rollback behavior for migrated entities and removing disk-backed scraper queue persistence per locked decisions.
Output: Transaction-safe SQLite write path, storage error taxonomy, in-memory scraper task store, and crash-recovery regressions.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-RESEARCH.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-01-PLAN.md
@core/storage/sqlite_runtime_repo.py
@core/storage/sqlite_resource_repo.py
@core/data_controller.py
@core/resource_manager.py
@core/scraper_task_store.py
@core/api.py
@tests/core/test_scraper_task_store.py

<interfaces>
From core/refresh_scheduler.py (consumer contract):
```python
def set_retry_metadata(self, source_id: str, metadata: dict[str, Any] | None) -> None
def clear_retry_metadata(self, source_id: str) -> None
def get_all_latest(self) -> list[dict]
```

From core/api.py (scraper contract):
```python
task = _scraper_task_store.claim_next_task(worker_id=..., lease_seconds=...)
updated = _scraper_task_store.heartbeat_task(task_id=..., worker_id=..., lease_seconds=...)
updated, changed = _scraper_task_store.complete_task(task_id=..., worker_id=..., attempt=...)
updated, changed = _scraper_task_store.fail_task(task_id=..., worker_id=..., error=..., attempt=...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add crash-recovery and rollback regression tests for storage writes</name>
  <files>tests/core/test_storage_crash_recovery.py, tests/core/test_storage_contract_sqlite.py</files>
  <read_first>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-RESEARCH.md, core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, tests/core/test_storage_contract_sqlite.py</read_first>
  <behavior>
    - Test 1: Runtime write failure inside transaction leaves previous committed `latest_by_source` row unchanged.
    - Test 2: Resource write failure inside transaction leaves existing source/view rows unchanged.
    - Test 3: Retry metadata writes are atomic with source state updates (no partially updated retry fields).
  </behavior>
  <action>Create `tests/core/test_storage_crash_recovery.py` with fault-injection cases that force exceptions between `BEGIN IMMEDIATE` and commit, then assert rollback keeps previously committed rows readable. Extend storage contract tests to assert transaction boundaries on both runtime and resource repositories. Keep assertions explicit on stable fields (`source_id`, `status`, `error_code`, `retry_metadata`, timestamps).</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_crash_recovery.py tests/core/test_storage_contract_sqlite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_crash_recovery.py tests/core/test_storage_contract_sqlite.py -q` exits 0.
    - `rg -n "rollback|BEGIN IMMEDIATE|last known-good|last_known_good" tests/core/test_storage_crash_recovery.py` returns crash-recovery assertions.
  </acceptance_criteria>
  <done>Automated tests prove interrupted writes do not corrupt committed runtime/resource state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce transaction-only writes and canonical storage error taxonomy</name>
  <files>core/storage/errors.py, core/storage/sqlite_connection.py, core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, core/data_controller.py, core/resource_manager.py, tests/core/test_storage_crash_recovery.py</files>
  <read_first>core/error_formatter.py, core/storage/sqlite_connection.py, core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, core/data_controller.py, core/resource_manager.py</read_first>
  <behavior>
    - Test 1: All SQLite mutations run in explicit write transactions with rollback-on-exception.
    - Test 2: Storage-layer failures raise deterministic internal codes (`storage.write_failed`, `storage.integrity_violation`, `storage.read_failed`).
    - Test 3: DataController/ResourceManager callers continue receiving same data shapes on successful reads.
  </behavior>
  <action>Add `core/storage/errors.py` with a small `StorageContractError` hierarchy and stable `error_code` values (`storage.read_failed`, `storage.write_failed`, `storage.integrity_violation`, `storage.schema_mismatch`). Refactor SQLite repositories to execute every mutation in `BEGIN IMMEDIATE` transaction wrappers, commit on success, and rollback on failure. Ensure controller adapters catch low-level sqlite exceptions and rethrow as `StorageContractError` with preserved deterministic code, while read APIs still return existing dict/model shapes unchanged on success.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_storage_crash_recovery.py tests/core/test_data_controller.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_storage_crash_recovery.py tests/core/test_data_controller.py -q` exits 0.
    - `rg -n "class StorageContractError|storage\\.write_failed|storage\\.integrity_violation|storage\\.read_failed" core/storage/errors.py` returns defined taxonomy.
    - `rg -n "BEGIN IMMEDIATE|rollback\\(|commit\\(" core/storage/sqlite_runtime_repo.py core/storage/sqlite_resource_repo.py` returns explicit transaction control code.
  </acceptance_criteria>
  <done>Runtime/resource writes are transaction-guarded and storage failures are classified with deterministic internal error codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace durable scraper task file with in-memory queue semantics</name>
  <files>core/scraper_task_store.py, core/api.py, tests/core/test_scraper_task_store.py</files>
  <read_first>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md, core/scraper_task_store.py, core/api.py, tests/core/test_scraper_task_store.py</read_first>
  <behavior>
    - Test 1: Scraper task lifecycle (`upsert/claim/heartbeat/complete/fail`) still works with existing API contracts.
    - Test 2: Creating or mutating tasks does not create or write any durable `scraper_tasks.json` file.
    - Test 3: `core/api.py` keeps existing internal scraper endpoint call patterns while using the in-memory task store.
  </behavior>
  <action>Refactor `ScraperTaskStore` to hold task state in process memory only (dictionary + lock) and remove JSON file load/save code paths entirely. Keep public method names and returned payload fields unchanged so `/api/internal/scraper/*` endpoints remain contract-compatible. Add debug/info logs for task lifecycle transitions (`claimed`, `running`, `completed`, `failed`, `cleared`) to preserve operational visibility now that queue state is not durable. Update scraper store tests to assert no file IO behavior and unchanged lifecycle semantics.</action>
  <verify>
    <automated>python -m pytest tests/core/test_scraper_task_store.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_scraper_task_store.py -q` exits 0.
    - `rg -n "read_text|write_text|json\\.loads|json\\.dumps|scraper_tasks\\.json" core/scraper_task_store.py` returns no persistence-path usage.
    - `rg -n "upsert_pending_task|claim_next_task|heartbeat_task|complete_task|fail_task" core/scraper_task_store.py core/api.py` confirms lifecycle API remains intact across store and endpoint wiring.
  </acceptance_criteria>
  <done>Scraper queue behavior is memory-only with preserved endpoint/task contracts and no durable task-file persistence.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_storage_crash_recovery.py tests/core/test_scraper_task_store.py -q
- [ ] python -m pytest tests/core/test_data_controller.py -q
- [ ] make test-backend
</verification>

<success_criteria>
- STOR-02 crash-safety behavior is demonstrated by rollback tests and transaction-only write paths.
- Last known-good runtime/resource state remains readable after simulated write interruption.
- Scraper task queue persistence is in-memory only, matching locked phase decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-02-SUMMARY.md`
</output>
