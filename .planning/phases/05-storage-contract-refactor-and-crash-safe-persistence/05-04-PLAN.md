---
phase: 05-storage-contract-refactor-and-crash-safe-persistence
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - core/storage/sqlite_runtime_repo.py
  - core/storage/sqlite_resource_repo.py
  - core/storage/migration.py
  - tests/core/test_storage_contract_sqlite.py
  - tests/core/test_storage_startup_migration.py
  - .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md
  - .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md
autonomous: true
requirements: [STOR-01, STOR-02, STOR-03, STOR-04]
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "Startup migration writes runtime/resource chunks through repository upsert APIs instead of direct SQL in the migrator module."
    - "Chunk migration remains idempotent and preserves `.deprecated.v1.json` marker behavior after per-chunk validation."
    - "Repository-backed migration writes keep transaction boundaries so interrupted writes do not expose partial state."
    - "Phase 5 verification includes repeatable commands that prove the migration key-link contract."
  artifacts:
    - path: "core/storage/sqlite_runtime_repo.py"
      provides: "Repository API for migration-time runtime upserts with one explicit write transaction."
    - path: "core/storage/sqlite_resource_repo.py"
      provides: "Repository APIs for migration-time source/view upserts with explicit write transactions."
    - path: "core/storage/migration.py"
      provides: "Startup migrator orchestrating chunk import via repository APIs plus chunk validation and marker rename."
    - path: "tests/core/test_storage_startup_migration.py"
      provides: "Regression coverage that startup migration calls repository APIs and preserves idempotent marker semantics."
    - path: ".planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md"
      provides: "Repeatable key-link verification commands for migration abstraction compliance."
  key_links:
    - from: "core/storage/migration.py"
      to: "core/storage/sqlite_runtime_repo.py"
      via: "data.json chunk calls runtime repository migration upsert API; migrator no longer embeds runtime INSERT/UPDATE SQL."
    - from: "core/storage/migration.py"
      to: "core/storage/sqlite_resource_repo.py"
      via: "sources.json/views.json chunks call resource repository migration upsert APIs."
    - from: "core/storage/sqlite_runtime_repo.py"
      to: "core/storage/sqlite_connection.py"
      via: "migration upsert API executes under BEGIN IMMEDIATE write transaction semantics."
---

<objective>
Close the Phase 5 verification gap by aligning startup migration writes with the storage-contract repository boundary and proving the key-link via repeatable checks.

Purpose: Keep STOR-03 migration behavior intact while making STOR-01/STOR-02 contract boundaries explicit and ensuring STOR-04 verification evidence is deterministic.
Output: Repository migration upsert APIs, migrator wiring update, regression tests, and updated Phase 5 validation/gate checks.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-CONTEXT.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-RESEARCH.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VERIFICATION.md
@.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-03-SUMMARY.md
@core/storage/migration.py
@core/storage/sqlite_runtime_repo.py
@core/storage/sqlite_resource_repo.py
@tests/core/test_storage_startup_migration.py

<interfaces>
From core/storage/contract.py:
```python
class RuntimeStore(Protocol):
    def upsert(self, source_id: str, data: dict[str, Any]) -> None: ...
    def set_state(...): ...
    def set_retry_metadata(...): ...

class ResourceStore(Protocol):
    def save_source(self, source: StoredSource) -> StoredSource: ...
    def save_view(self, view: StoredView) -> StoredView: ...
```

From core/storage/migration.py:
```python
def run_startup_migration(storage: StorageContract, *, data_dir: str | Path | None = None) -> None
```

Locked migration policy from 05-CONTEXT.md:
```text
Chunk order is fixed: data.json -> sources.json -> views.json
Each chunk must validate before rename marker is applied
Marker format must remain <name>.deprecated.v1.json
Migration is idempotent on restart
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repository migration-upsert APIs with explicit transaction boundaries</name>
  <files>core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, tests/core/test_storage_contract_sqlite.py</files>
  <read_first>core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, core/storage/sqlite_connection.py, core/storage/contract.py, tests/core/test_storage_contract_sqlite.py</read_first>
  <behavior>
    - Test 1: Runtime repository exposes a migration upsert API that accepts normalized legacy runtime records and preserves `updated_at`/`last_success_at` fields.
    - Test 2: Resource repository exposes migration upsert APIs for source/view batches and writes all rows in one explicit write transaction per batch.
    - Test 3: New migration upsert APIs keep BEGIN IMMEDIATE transaction behavior (no partial-write path).
  </behavior>
  <action>Implement `SqliteRuntimeRepository.upsert_migration_latest(records: list[dict[str, Any]]) -> list[str]` using one `_write("runtime.upsert_migration_latest", ...)` call that UPSERTs `runtime_latest` rows and returns unique migrated `source_id` values. Implement `SqliteResourceRepository.upsert_migration_sources(sources: list[StoredSource]) -> list[str]` and `SqliteResourceRepository.upsert_migration_views(views: list[StoredView]) -> list[str]` using one `_write(...)` call per chunk that UPSERTs `stored_sources`/`stored_views` and returns migrated IDs. Add/extend tests in `tests/core/test_storage_contract_sqlite.py` to assert these APIs exist, commit rows correctly, and execute `BEGIN IMMEDIATE` for migration batch writes.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_contract_sqlite.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_contract_sqlite.py -q` exits 0.
    - `rg -n "def upsert_migration_latest\\(|def upsert_migration_sources\\(|def upsert_migration_views\\(" core/storage/sqlite_runtime_repo.py core/storage/sqlite_resource_repo.py` finds all three APIs.
    - `rg -n "runtime\\.upsert_migration_latest|resource\\.upsert_migration_sources|resource\\.upsert_migration_views" core/storage/sqlite_runtime_repo.py core/storage/sqlite_resource_repo.py` confirms explicit operation names for transaction wrappers.
  </acceptance_criteria>
  <done>Repository layer has dedicated migration upsert APIs with transaction-guarded semantics and regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor startup migration to call repository APIs instead of direct table writes</name>
  <files>core/storage/migration.py, tests/core/test_storage_startup_migration.py</files>
  <read_first>core/storage/migration.py, core/storage/sqlite_runtime_repo.py, core/storage/sqlite_resource_repo.py, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VERIFICATION.md, tests/core/test_storage_startup_migration.py</read_first>
  <behavior>
    - Test 1: `run_startup_migration` writes `data.json` through runtime repository migration API and writes `sources.json`/`views.json` through resource repository migration APIs.
    - Test 2: Existing migration behavior remains intact: chunk order, per-chunk validation, rename-after-validation, and idempotent re-runs.
    - Test 3: Migrator module no longer contains direct runtime/resource INSERT/UPDATE SQL statements.
  </behavior>
  <action>Replace direct SQL mutation logic inside `core/storage/migration.py` with repository API calls: resolve typed sqlite repositories from `StorageContract`, call `runtime.upsert_migration_latest(...)` for data chunk, call `resources.upsert_migration_sources(...)` for sources chunk, and call `resources.upsert_migration_views(...)` for views chunk. Keep existing validation helpers and marker rename timing unchanged. Add regression tests in `tests/core/test_storage_startup_migration.py` using spy repositories/call counters that fail if migration bypasses repository APIs. Keep read-only validation SQL in migration module, but remove direct mutation SQL for `runtime_latest`, `stored_sources`, and `stored_views`.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_startup_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_storage_startup_migration.py -q` exits 0.
    - `rg -n "upsert_migration_latest|upsert_migration_sources|upsert_migration_views" core/storage/migration.py` confirms repository API usage in migrator.
    - `rg -n "INSERT INTO runtime_latest|INSERT INTO stored_sources|INSERT INTO stored_views|UPDATE stored_views SET payload_json" core/storage/migration.py` returns no matches.
    - `rg -n "_CHUNK_ORDER = \\(\"data\\.json\", \"sources\\.json\", \"views\\.json\"\\)" core/storage/migration.py` confirms chunk order remains unchanged.
  </acceptance_criteria>
  <done>Startup migration writes are routed through repository upsert APIs while preserving existing chunk/idempotency behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit gap-closure verification checks to Phase 5 validation gate docs</name>
  <files>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md</files>
  <read_first>.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md, .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VERIFICATION.md</read_first>
  <action>Add a "Gap Closure: Migration Key-Link" subsection in both docs with exact commands and expected outcomes: (1) `python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_storage_startup_migration.py -q` must pass; (2) `rg -n "upsert_migration_latest|upsert_migration_sources|upsert_migration_views" core/storage/migration.py` must return matches; (3) `rg -n "INSERT INTO runtime_latest|INSERT INTO stored_sources|INSERT INTO stored_views|UPDATE stored_views SET payload_json" core/storage/migration.py` must return no matches. Include a PASS criterion that this section must pass before Phase 5 is marked `verified`.</action>
  <verify>
    <automated>python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_storage_startup_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Gap Closure: Migration Key-Link" .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md` returns 2 matches (one per file).
    - `rg -n "upsert_migration_latest|upsert_migration_sources|upsert_migration_views" .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-VALIDATION.md .planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-STORAGE-GATE.md` returns command references.
    - `python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_storage_startup_migration.py -q` exits 0.
  </acceptance_criteria>
  <done>Phase 5 gate documentation includes deterministic commands to verify the migration abstraction gap is closed.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_storage_contract_sqlite.py tests/core/test_storage_startup_migration.py -q
- [ ] rg -n "upsert_migration_latest|upsert_migration_sources|upsert_migration_views" core/storage/migration.py
- [ ] rg -n "INSERT INTO runtime_latest|INSERT INTO stored_sources|INSERT INTO stored_views|UPDATE stored_views SET payload_json" core/storage/migration.py
</verification>

<success_criteria>
- Startup migration no longer performs direct runtime/resource mutation SQL in `core/storage/migration.py`.
- Repository migration upsert APIs enforce explicit write transactions and keep crash-safe semantics.
- Migration remains chunked, idempotent, and marker-based (`.deprecated.v1.json`).
- Phase 5 verification artifacts include a repeatable key-link gap-closure section and commands.
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-contract-refactor-and-crash-safe-persistence/05-04-SUMMARY.md`
</output>
