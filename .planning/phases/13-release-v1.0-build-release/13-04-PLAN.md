---
phase: 13-release-v1.0-build-release
plan: 04
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - tests/smoke/test_phase13_e2e.py
  - ui-react/tests/e2e/test_ui.spec.ts
autonomous: true
requirements:
  - "Release v1.0 稳定版发版 (Build & Release)"
must_haves:
  truths:
    - "Core user flows are verified end-to-end using automated testing."
    - "App can simulate Integration creation and source fetching seamlessly."
    - "Frontend-only micro-component logic is tested."
  artifacts:
    - path: "tests/smoke/test_phase13_e2e.py"
      provides: "E2E testing suite"
  key_links: []
---

<objective>
Implement UI and E2E test scenarios to verify critical flows before release.

Purpose: Ensure the application core pipelines work synchronously and the UI logic is robust.
Output: Frontend Playwright tests and Tauri/Python E2E tests.
</objective>

<context>
@.planning/phases/13-release-v1.0-build-release/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Frontend-only Playwright UI Tests</name>
  <files>ui-react/tests/e2e/test_ui.spec.ts</files>
  <action>Write Playwright tests focusing purely on Frontend micro-components and page logic using mocked backend responses.</action>
  <verify>
    <automated>cd ui-react && npx playwright test tests/e2e/test_ui.spec.ts</automated>
  </verify>
  <done>Frontend-only Playwright tests are running.</done>
</task>

<task type="auto">
  <name>Task 2: Create Playwright+Tauri E2E Integration Smoke Tests</name>
  <files>tests/smoke/test_phase13_e2e.py</files>
  <action>Write Python-based E2E smoke tests for Playwright+Tauri simulating core flows (macOS only): Integration creation -> Source creation -> Auto-load -> Success. Also cover OAuth redirect and Webview background execution. Use mock external services for CI to prevent network failures.</action>
  <verify>
    <automated>pytest tests/smoke/test_phase13_e2e.py</automated>
  </verify>
  <done>E2E tests verify full stack functionality with mock external dependencies.</done>
</task>

</tasks>

<verification>
E2E tests run successfully locally on macOS.
</verification>

<success_criteria>
Core E2E flow and UI components are validated through tests, increasing confidence for v1.0 release.
</success_criteria>