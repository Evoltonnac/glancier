---
phase: 13-release-v1.0-build-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-react/src-tauri/tauri.conf.json
  - package.json
  - ui-react/package.json
  - core/encryption.py
autonomous: true
requirements:
  - "Release v1.0 稳定版发版 (Build & Release)"
must_haves:
  truths:
    - "Application passes security audits without known critical vulnerabilities."
    - "Tauri IPC is restricted to explicit allowlisted commands."
    - "WebView CSP strictly isolates Scraper context from main UI."
    - "React rendering performance is optimized."
    - "Key storage security utilizes AES/Fernet and system-level keychain."
  artifacts:
    - path: "ui-react/src-tauri/tauri.conf.json"
      provides: "Strict security settings"
  key_links: []
---

<objective>
Establish security baseline, optimize performance, and prepare environment for production builds.

Purpose: Prevent XSS, restrict IPC calls, ensure zero known CVEs, ensure secure secret storage, and optimize UI rendering.
Output: Updated Tauri configuration, encryption updates, and passing security audits.
</objective>

<context>
@.planning/phases/13-release-v1.0-build-release/CONTEXT.md
@.planning/phases/13-release-v1.0-build-release/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce Tauri IPC Allowlist and CSP</name>
  <files>ui-react/src-tauri/tauri.conf.json</files>
  <action>Update `tauri.conf.json` security settings. Add a strict CSP isolating the scraper. Configure `plugins` or `invoke` allowlists to only allow explicitly defined rust commands.</action>
  <verify>
    <automated>cat ui-react/src-tauri/tauri.conf.json | grep -i csp</automated>
  </verify>
  <done>CSP is configured and IPC is explicitly restricted.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AES/Fernet and Keychain Security</name>
  <files>core/encryption.py</files>
  <action>Ensure `.env` and local credentials use AES/Fernet encryption, prioritizing system-level keychain integration via keyring or similar library.</action>
  <verify>
    <automated>pytest tests/core/test_encryption.py</automated>
  </verify>
  <done>Secret storage is secured with AES/Fernet and keychain.</done>
</task>

<task type="auto">
  <name>Task 3: Optimize React Render Performance</name>
  <files>ui-react/src/App.tsx, ui-react/src/components/WidgetRenderer.tsx</files>
  <action>Audit and optimize React rendering performance. Implement React.memo, useMemo, and useCallback for high-frequency updates like widget dragging or scraping state updates.</action>
  <verify>
    <automated>npm --prefix ui-react run typecheck</automated>
  </verify>
  <done>React components are optimized to reduce unnecessary re-renders.</done>
</task>

<task type="auto">
  <name>Task 4: Dependency Security Audit</name>
  <files>ui-react/package.json, Cargo.toml</files>
  <action>Run `npm audit` and `cargo audit` to resolve high/critical vulnerabilities. Update dependencies if necessary.</action>
  <verify>
    <automated>cd ui-react && npm audit --audit-level=high && cd ../src-tauri && cargo audit || true</automated>
  </verify>
  <done>Zero high/critical CVEs in frontend and rust dependencies.</done>
</task>

</tasks>

<verification>
Security configuration must be valid, performance improved, and app should boot securely.
</verification>

<success_criteria>
Tauri config enforce CSP, IPC restricted, keys encrypted, UI optimized, dependency trees clean.
</success_criteria>