---
phase: 13-release-v1.0-build-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-react/src/pages/Integrations.tsx
  - ui-react/src/components/Settings.tsx
  - core/source_state.py
autonomous: true
requirements:
  - "Release v1.0 稳定版发版 (Build & Release)"
must_haves:
  truths:
    - "Users see predefined templates when creating a new integration."
    - "First-time users have example data populated automatically."
    - "Users can access a manual bug report link in case of issues."
  artifacts:
    - path: "ui-react/src/pages/Integrations.tsx"
      provides: "Presets UI"
  key_links: []
---

<objective>
Polish UX for first-time users by adding integration templates, auto-populating examples, and adding bug report links.

Purpose: Lower the barrier to entry, guide users, and provide support channels.
Output: Preset buttons, default data generation logic, and bug report UI.
</objective>

<context>
@.planning/phases/13-release-v1.0-build-release/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Integration Presets UI</name>
  <files>ui-react/src/pages/Integrations.tsx</files>
  <action>Add a preset selection area when creating a new integration. Include templates for API Key, OAuth, Web Scraper, and cURL. Clicking a preset auto-fills the YAML editor with boilerplate.</action>
  <verify>
    <automated>npm --prefix ui-react run typecheck</automated>
  </verify>
  <done>Presets are visible and populate the editor.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-insert Example Data on First Launch</name>
  <files>core/source_state.py</files>
  <action>Modify app startup logic. If no integrations/sources exist, automatically create a basic public integration, a corresponding source, and a widget configuration.</action>
  <verify>
    <automated>pytest tests/core/test_app_startup_resilience.py</automated>
  </verify>
  <done>Example data is seeded when app is launched with empty configurations.</done>
</task>

<task type="auto">
  <name>Task 3: Add Manual Bug Report Link</name>
  <files>ui-react/src/components/Settings.tsx</files>
  <action>Add a 'Report Bug' button in the settings or help menu that directs the user to open a GitHub issue.</action>
  <verify>
    <automated>npm --prefix ui-react run typecheck</automated>
  </verify>
  <done>Bug report link is available in the UI.</done>
</task>

</tasks>

<verification>
UI shows presets, empty workspace gets seeded, bug report link works.
</verification>

<success_criteria>
Improved initial user experience with templates, example data, and support link.
</success_criteria>