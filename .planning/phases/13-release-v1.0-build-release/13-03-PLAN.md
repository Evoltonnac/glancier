---
phase: 13-release-v1.0-build-release
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - .github/workflows/ci.yml
  - ui-react/src-tauri/tauri.conf.json
autonomous: false
requirements:
  - "Release v1.0 稳定版发版 (Build & Release)"
must_haves:
  truths:
    - "GitHub Actions can build and package macOS and Windows binaries."
    - "Release workflow is triggerable manually."
    - "Application branding, icons, and DMG backgrounds are configured."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Release build jobs"
  key_links: []
---

<objective>
Configure CI/CD release pipeline, update app branding, and configure DMG artifacts for v1.0.

Purpose: Automate packaging of Tauri apps into branded DMG and Exe installers.
Output: Updated CI workflow, app icons, DMG backgrounds, and configured updater.
</objective>

<context>
@.planning/phases/13-release-v1.0-build-release/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure App Metadata and Branding</name>
  <files>ui-react/src-tauri/tauri.conf.json, ui-react/src-tauri/icons/</files>
  <action>Update app icon to the new Glancier branding. Set correct versioning in package.json and tauri.conf.json. Configure macOS bundle settings to include an instructional DMG background image (e.g., drag to Applications, right-click to open).</action>
  <verify>
    <automated>cat ui-react/src-tauri/tauri.conf.json | grep macos</automated>
  </verify>
  <done>App metadata and DMG branding are correctly set.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Release GitHub Action</name>
  <files>.github/workflows/ci.yml</files>
  <action>Add jobs to `ci.yml` for building release assets using `tauri-apps/tauri-action`. Enable `workflow_dispatch` trigger. Set up matrix for `macos-latest` and `windows-latest`. Configure it to run `scripts/build.sh` before running the tauri build.</action>
  <verify>
    <automated>cat .github/workflows/ci.yml | grep workflow_dispatch</automated>
  </verify>
  <done>CI workflow includes release build steps.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Tauri Updater</name>
  <files>ui-react/src-tauri/tauri.conf.json</files>
  <action>Enable the updater in `tauri.conf.json` under `app.updater` if applicable for v1.0. Set it up with dummy/placeholder endpoints if needed.</action>
  <verify>
    <automated>cat ui-react/src-tauri/tauri.conf.json | grep updater || echo "Updater optional"</automated>
  </verify>
  <done>Updater configuration is present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify CI Builds</name>
  <what-built>CI/CD and Branding Configuration</what-built>
  <how-to-verify>
    1. Trigger the workflow manually in GitHub Actions.
    2. Confirm it successfully builds and uploads artifact DMGs/EXEs.
    3. Verify the macOS DMG shows the instructional background and correct app icon.
  </how-to-verify>
  <resume-signal>Type "approved" if builds succeed and branding is correct.</resume-signal>
</task>

</tasks>

<verification>
GitHub Actions should output branded, downloadable artifacts.
</verification>

<success_criteria>
Release workflow is fully functional with correct branding.
</success_criteria>