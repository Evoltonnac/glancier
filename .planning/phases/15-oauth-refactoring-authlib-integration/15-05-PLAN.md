---
phase: 15-oauth-refactoring-authlib-integration
plan: 05
type: execute
wave: 4
depends_on: [15-04]
files_modified: [tests/integration/test_oauth_providers.py]
autonomous: true
requirements: [OAUTH-TAURI-EVENT]

must_haves:
  truths:
    - "Integration tests confirm compatibility with real-world OAuth scenarios"
    - "Full E2E flow for Code and Device flows is verified"
  artifacts:
    - path: "tests/integration/test_oauth_providers.py"
      provides: "E2E OAuth flow verification"
  key_links:
    - from: "tests/integration/test_oauth_providers.py"
      to: "core/auth/oauth_auth.py"
      via: "API-level and class-level integration tests"
---

<objective>
Verify the implementation with representative real-world scenarios and polish the final user experience.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/auth/oauth_auth.py
@ui-react/src/components/auth/DeviceFlowModal.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration Testing with Mock/Real Providers</name>
  <files>tests/integration/test_oauth_providers.py</files>
  <behavior>
    - Mock GitHub (Code Flow) server should pass.
    - Mock Twitch (Device Flow) server should pass.
    - Verify token persistence in SecretsController after exchange.
  </behavior>
  <action>
    - Build a test suite using `respx` or `pytest-httpx` to mock external OAuth provider endpoints.
    - Test edge cases like invalid codes, expired device codes, and slow polling.
    - Confirm the full cycle from authorization start to token persistence.
  </action>
  <verify>
    <automated>pytest tests/integration/test_oauth_providers.py</automated>
  </verify>
  <done>Comprehensive integration tests verify the system's reliability.</done>
</task>

<task type="auto">
  <name>Task 2: UI/UX Polishing and Final Documentation</name>
  <files>ui-react/src/components/auth/DeviceFlowModal.tsx, core/auth/oauth_auth.py</files>
  <action>
    - Improve error messages for common OAuth failures (e.g., user cancellation).
    - Add loading indicators to UI when token exchange or polling is in progress.
    - Update internal documentation and PROJECT.md to reflect the new OAuth architecture.
  </action>
  <verify>
    <automated>grep -r "AsyncOAuth2Client" core/auth/</automated>
  </verify>
  <done>Architecture is fully integrated, verified, and documented.</done>
</task>

</tasks>

<success_criteria>
- Zero regressions in existing OAuth integrations.
- New device flow works seamlessly.
- Integration test suite is stable and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/15-oauth-refactoring-authlib-integration/15-05-SUMMARY.md`
</output>
