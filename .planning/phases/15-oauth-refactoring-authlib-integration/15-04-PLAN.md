---
phase: 15-oauth-refactoring-authlib-integration
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified: [core/auth/oauth_auth.py, core/auth/manager.py]
autonomous: true
requirements: [OAUTH-BACKEND-AUTHLIB]

must_haves:
  truths:
    - "Client Credentials flow is supported by the backend"
    - "Token refreshing is automated via Authlib's built-in mechanism"
  artifacts:
    - path: "core/auth/oauth_auth.py"
      provides: "Client Credentials support and auto-refresh"
  key_links:
    - from: "core/auth/oauth_auth.py"
      to: "authlib.integrations.httpx_client.AsyncOAuth2Client.fetch_token"
      via: "grant_type='client_credentials'"
---

<objective>
Support Client Credentials flow and harden token management using Authlib's automated features.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/auth/oauth_auth.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Support Client Credentials Flow</name>
  <files>core/auth/oauth_auth.py</files>
  <behavior>
    - fetch_token(grant_type='client_credentials') should work.
    - Token should be persisted like other flows.
  </behavior>
  <action>
    - Update `OAuthAuth.ensure_valid_token` to support `client_credentials` if configured.
    - Implement the logic to use `AsyncOAuth2Client.fetch_token(grant_type='client_credentials')`.
    - Ensure tokens obtained this way are stored and refreshed appropriately.
  </action>
  <verify>
    <automated>pytest tests/core/test_oauth_client_credentials.py</automated>
  </verify>
  <done>Backend supports Client Credentials flow.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Token Refresh Mechanism</name>
  <files>core/auth/oauth_auth.py</files>
  <action>
    - Ensure the `update_token` callback is robustly integrated.
    - Implement a method `ensure_fresh_token` that uses `AsyncOAuth2Client`'s automatic refresh (by making a no-op call or checking expiry).
    - Leverage Authlib's `leeway` for more reliable expiration detection.
  </action>
  <verify>
    <automated>pytest tests/core/test_oauth_token_refresh.py</automated>
  </verify>
  <done>Token refreshing is standardized and automated.</done>
</task>

</tasks>

<success_criteria>
- Backend seamlessly obtains and refreshes Client Credentials tokens.
- No manual expiration calculations are needed.
- Unified token persistence logic across all flows.
</success_criteria>

<output>
After completion, create `.planning/phases/15-oauth-refactoring-authlib-integration/15-04-SUMMARY.md`
</output>
