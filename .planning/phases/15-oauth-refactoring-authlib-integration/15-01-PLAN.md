---
phase: 15-oauth-refactoring-authlib-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [core/auth/oauth_auth.py, core/auth/manager.py, core/api.py]
autonomous: true
requirements: [OAUTH-CODE-PKCE, OAUTH-BACKEND-AUTHLIB, OAUTH-BACKEND-POLYMORPHISM]

must_haves:
  truths:
    - "Backend uses Authlib AsyncOAuth2Client for OAuth operations"
    - "Authorization Code Flow with PKCE works as before but through Authlib"
    - "SecretsController still persists tokens correctly"
  artifacts:
    - path: "core/auth/oauth_auth.py"
      provides: "Authlib-based OAuth implementation"
  key_links:
    - from: "core/auth/oauth_auth.py"
      to: "authlib.integrations.httpx_client.AsyncOAuth2Client"
      via: "instantiation and method calls"
---

<objective>
Refactor the existing manual OAuth 2.0 implementation to use the Authlib library. This provides a standards-compliant foundation for all OAuth flows, including automatic PKCE handling and token refreshing.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/auth/oauth_auth.py
@core/auth/manager.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor OAuthAuth using Authlib</name>
  <files>core/auth/oauth_auth.py</files>
  <behavior>
    - Initialize AsyncOAuth2Client with client_id, client_secret, and configuration.
    - create_authorization_url() should handle PKCE automatically if configured.
    - fetch_token() should replace manual code exchange.
    - update_token callback should be used to persist tokens via SecretsController.
  </behavior>
  <action>
    - Replace manual URL building and HTTP requests with Authlib's AsyncOAuth2Client.
    - Implement `get_authorize_url` using `client.create_authorization_url`.
    - Implement `exchange_code` using `client.fetch_token`.
    - Ensure PKCE is handled by Authlib (passing `code_challenge_method`).
    - Integrate `update_token` callback to save tokens using existing `_save_token` logic.
    - Remove `core/auth/pkce.py` if no longer needed.
  </action>
  <verify>
    <automated>pytest tests/core/test_oauth_authlib.py</automated>
  </verify>
  <done>OAuthAuth uses Authlib and passes basic Auth Code flow tests.</done>
</task>

<task type="auto">
  <name>Task 2: Update OAuth Manager and API for Polymorphism</name>
  <files>core/auth/manager.py, core/api.py</files>
  <action>
    - Update `OAuthManager` to pass necessary configuration to the new `OAuthAuth`.
    - Modify `/api/oauth/authorize/{source_id}` to return a structured response:
      ```json
      {
        "flow": "code",
        "authorize_url": "..."
      }
      ```
      This allows future flows (like Device Flow) to return different structures.
  </action>
  <verify>
    <automated>curl http://localhost:8000/api/oauth/authorize/test_source</automated>
  </verify>
  <done>API returns polymorphic OAuth start response.</done>
</task>

</tasks>

<success_criteria>
- Backend successfully exchanges code for token using Authlib.
- PKCE is correctly handled for public clients.
- Existing integrations (like OpenRouter) still work.
</success_criteria>

<output>
After completion, create `.planning/phases/15-oauth-refactoring-authlib-integration/15-01-SUMMARY.md`
</output>
