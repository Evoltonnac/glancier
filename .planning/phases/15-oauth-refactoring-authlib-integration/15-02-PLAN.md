---
phase: 15-oauth-refactoring-authlib-integration
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [core/auth/oauth_auth.py, core/auth/manager.py, core/api.py, core/models.py]
autonomous: true
requirements: [OAUTH-DEVICE-FLOW, OAUTH-BACKEND-AUTHLIB, OAUTH-TAURI-EVENT]

must_haves:
  truths:
    - "Backend supports requesting device codes for RFC 8628 flows"
    - "Backend can poll for device tokens asynchronously"
    - "Tauri events are used for device flow state notifications (optional but recommended)"
  artifacts:
    - path: "core/auth/oauth_auth.py"
      provides: "Device Flow implementation"
    - path: "core/models.py"
      provides: "InteractionRequest with device_flow type"
  key_links:
    - from: "core/auth/oauth_auth.py"
      to: "authlib.integrations.httpx_client.AsyncOAuth2Client.register_device_authorization"
      via: "API call"
---

<objective>
Implement the Device Authorization Grant flow (RFC 8628) in the backend. This is crucial for devices and apps with limited input, like Twitch on a desktop client without a direct browser intercept.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core/auth/oauth_auth.py
@core/api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Device Flow Logic in OAuthAuth</name>
  <files>core/auth/oauth_auth.py</files>
  <behavior>
    - request_device_code() should call the device_authorization_endpoint and return verification data.
    - poll_device_token() should handle the polling logic with interval backoff support from Authlib.
  </behavior>
  <action>
    - Add `start_device_flow` method to `OAuthAuth` which calls `client.register_device_authorization`.
    - Implement `poll_device_token` which wraps Authlib's polling logic.
    - Handle the `authorization_pending` and `slow_down` errors by following standard intervals.
  </action>
  <verify>
    <automated>pytest tests/core/test_oauth_device_flow.py</automated>
  </verify>
  <done>Backend can initiate and poll for device flow tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Expose Device Flow via API and Interaction System</name>
  <files>core/api.py, core/models.py</files>
  <action>
    - Update `core/models.py` to include a new `InteractionType.OAUTH_DEVICE_FLOW`.
    - Modify `core/api.py` `/api/oauth/authorize/{source_id}` to detect the source's flow type and return device flow data if needed.
    - Ensure the backend starts a polling background task or provides an endpoint for the frontend to trigger polling.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8000/api/oauth/authorize/twitch_source</automated>
  </verify>
  <done>Frontend can receive device flow data to display to the user.</done>
</task>

</tasks>

<success_criteria>
- Backend successfully starts device flow and receives user code/URL.
- Polling mechanism retrieves token once user approves.
- Integration with Authlib is standard-compliant.
</success_criteria>

<output>
After completion, create `.planning/phases/15-oauth-refactoring-authlib-integration/15-02-SUMMARY.md`
</output>
