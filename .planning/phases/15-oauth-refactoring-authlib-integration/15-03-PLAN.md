---
phase: 15-oauth-refactoring-authlib-integration
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified: [ui-react/src/components/auth/DeviceFlowModal.tsx, ui-react/src/components/auth/FlowHandler.tsx, ui-react/src/components/auth/OAuthCallback.tsx]
autonomous: false
requirements: [OAUTH-UI-DEVICE, OAUTH-UI-CALLBACK]

must_haves:
  truths:
    - "User can see device flow verification URL and user code in a modal"
    - "OAuth callback page can correctly handle #access_token fragments for Implicit flow"
  artifacts:
    - path: "ui-react/src/components/auth/DeviceFlowModal.tsx"
      provides: "UI for device flow authorization"
  key_links:
    - from: "ui-react/src/components/auth/FlowHandler.tsx"
      to: "ui-react/src/components/auth/DeviceFlowModal.tsx"
      via: "conditional rendering"
---

<objective>
Refactor the frontend to support the new device authorization flow and unify callback interception.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ui-react/src/components/auth/FlowHandler.tsx
@ui-react/src/components/auth/OAuthCallback.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeviceFlowModal Component</name>
  <files>ui-react/src/components/auth/DeviceFlowModal.tsx, ui-react/src/components/auth/FlowHandler.tsx</files>
  <action>
    - Create a new component `DeviceFlowModal.tsx` displaying the verification URI, user code, and a countdown.
    - Provide an "Open Link" button and a way to copy the user code.
    - Update `FlowHandler.tsx` to listen for `oauth_device_flow` interaction requests and show the modal.
    - Implement polling feedback (status indicator) if the backend provides polling updates via events or a repeated API call.
  </action>
  <verify>
    <automated>npm run test -- --filter=DeviceFlowModal</automated>
  </verify>
  <done>Frontend displays device flow modal when requested.</done>
</task>

<task type="auto">
  <name>Task 2: Unify OAuth Callback Interceptor</name>
  <files>ui-react/src/components/auth/OAuthCallback.tsx</files>
  <action>
    - Update `OAuthCallback.tsx` logic to parse the URL hash (window.location.hash) for Implicit Flow tokens.
    - Ensure it still handles `code` and `state` query parameters correctly.
    - Forward parsed data to the backend for final processing and persistence.
  </action>
  <verify>
    <automated>npm run test -- --filter=OAuthCallback</automated>
  </verify>
  <done>Unified callback handles both query strings and hash fragments.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Human Verification of OAuth UI</name>
  <action>Verify the device flow modal and the unified callback behavior manually.</action>
  <verify>
    - Trigger a device flow authorization (simulated or real).
    - Verify the modal appears and contains the correct code/URL.
    - Simulate a redirect with #access_token and verify it's captured correctly.
  </verify>
  <done>UI interactions are confirmed to be correct by the user.</done>
</task>

</tasks>

<success_criteria>
- Device flow UI is intuitive and informative.
- Implicit flow (fragment-based) works end-to-end.
- Callback handling is robust against different OAuth provider styles.
</success_criteria>

<output>
After completion, create `.planning/phases/15-oauth-refactoring-authlib-integration/15-03-SUMMARY.md`
</output>
