---
phase: 10-sql-chart-widgets-and-sdui-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui-react/src/components/widgets/shared/chartSchemas.ts
  - ui-react/src/components/widgets/shared/chartFieldValidation.ts
  - ui-react/src/components/widgets/shared/chartState.ts
  - ui-react/src/components/widgets/charts/ChartFrame.tsx
  - ui-react/src/components/widgets/chartSchemas.test.ts
  - ui-react/src/components/widgets/chartState.test.ts
  - ui-react/src/components/widgets/ChartWidgets.test.tsx
  - ui-react/src/components/widgets/ChartTable.test.tsx
autonomous: true
requirements:
  - CHART-06
  - CHART-07
must_haves:
  truths:
    - "Invalid chart encoding never crashes the dashboard and always renders a deterministic in-card config error state."
    - "Chart widgets classify loading, empty, runtime error, config error, and ready states deterministically from normalized SQL data."
    - "x/y/series/value/label mapping is validated against sql_response.fields before any chart renderer mounts."
  artifacts:
    - path: "ui-react/src/components/widgets/shared/chartSchemas.ts"
      provides: "Discriminated chart schemas for Chart.Line/Chart.Bar/Chart.Area/Chart.Pie/Chart.Table with shared data_source + encoding contract"
    - path: "ui-react/src/components/widgets/shared/chartFieldValidation.ts"
      provides: "Runtime encoding validator against sql_response.fields metadata"
    - path: "ui-react/src/components/widgets/shared/chartState.ts"
      provides: "Deterministic chart state classifier"
    - path: "ui-react/src/components/widgets/charts/ChartFrame.tsx"
      provides: "Unified empty/config-error/runtime-error card content"
  key_links:
    - from: "ui-react/src/components/widgets/shared/chartFieldValidation.ts"
      to: "sql_response.fields"
      via: "field metadata lookup"
      pattern: "sql_response\\.fields"
    - from: "ui-react/src/components/widgets/shared/chartState.ts"
      to: "ui-react/src/components/widgets/charts/ChartFrame.tsx"
      via: "state kind to fallback renderer"
      pattern: "loading|empty|config_error|runtime_error|ready"
---

<objective>
Define the Phase 10 chart contract and deterministic state/fallback foundation before chart-specific rendering work.

Purpose: Lock a library-agnostic SDUI contract (`data_source` + `encoding`) and deterministic fallback behavior so all chart types implement against one validated interface.
Output: Shared chart schemas, field-validation/state modules, chart fallback frame, and Wave 0 test files.
</objective>

<execution_context>
@/Users/xingminghua/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-RESEARCH.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-UI-SPEC.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-VALIDATION.md
@docs/sdui/01_architecture_and_guidelines.md
@docs/flow/02_step_reference.md
@ui-react/src/components/widgets/WidgetRenderer.tsx
@ui-react/src/components/widgets/shared/commonProps.ts

<interfaces>
From ui-react/src/components/widgets/shared/commonProps.ts:
```typescript
export const SpacingSchema = z.enum(["none", "sm", "md", "lg"]);
export const SizeSchema = z.enum(["sm", "md", "lg", "xl"]);
export const ToneSchema = z.enum(["default", "muted", "info", "success", "warning", "danger"]);
```

From docs/flow/02_step_reference.md (SQL output contract):
```yaml
sql_response.rows
sql_response.fields
sql_response.row_count
sql_response.duration_ms
sql_response.truncated
sql_response.columns        # compatibility alias
sql_response.execution_ms   # compatibility alias
```

Locked chart type names from 10-CONTEXT.md:
```text
Chart.Line
Chart.Bar
Chart.Area
Chart.Pie
Chart.Table
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 chart test files and fixture data contracts</name>
  <files>
    ui-react/src/components/widgets/chartSchemas.test.ts
    ui-react/src/components/widgets/chartState.test.ts
    ui-react/src/components/widgets/ChartWidgets.test.tsx
    ui-react/src/components/widgets/ChartTable.test.tsx
  </files>
  <read_first>
    ui-react/src/components/widgets/WidgetRenderer.test.tsx
    ui-react/src/components/widgets/WidgetRenderer.tsx
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: schema validation rejects missing required channels and unknown fields with deterministic issue paths.
    - Test 2: state classifier returns loading/runtime_error/config_error/empty/ready for explicit input fixtures.
    - Test 3: chart widget tests include named cases for line/bar/area/pie rendering and fallback state assertions.
    - Test 4: table test includes sorting, limiting, column selection, and title override coverage.
  </behavior>
  <action>
    Create the four missing Wave 0 test files at the exact paths listed in 10-VALIDATION.md and include concrete fixtures rooted at:
    - `sql_response.rows` array with at least 4 rows containing `ts`, `category`, `amount`, `label`, `count` keys.
    - `sql_response.fields` array with metadata objects `{ name, type }` where type values include `integer`, `float`, `text`, `datetime`.

    In `chartSchemas.test.ts`, add explicit failing/then-passing expectations for:
    - `Chart.Line` requiring `encoding.x.field` and `encoding.y.field`.
    - `Chart.Pie` requiring `encoding.label.field` and `encoding.value.field`.
    - `Chart.Table` accepting `columns` entries with `{ field, title?, format? }` and rejecting unknown field references.

    In `chartState.test.ts`, assert exact classifier outputs: `loading`, `runtime_error`, `config_error`, `empty`, `ready`.

    Keep test names exactly aligned with validation strategy rows:
    - `renders line chart from sql_response rows`
    - `renders bar chart from sql_response rows`
    - `renders area chart from sql_response rows`
    - `renders pie chart from sql_response rows`
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/chartSchemas.test.ts src/components/widgets/chartState.test.ts src/components/widgets/ChartWidgets.test.tsx src/components/widgets/ChartTable.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `ui-react/src/components/widgets/chartSchemas.test.ts` exists and contains string `Chart.Line` and `encoding.x.field`.
    - `ui-react/src/components/widgets/chartState.test.ts` exists and contains all literals: `loading`, `runtime_error`, `config_error`, `empty`, `ready`.
    - `ui-react/src/components/widgets/ChartWidgets.test.tsx` contains the exact test names from 10-VALIDATION.md for line/bar/area/pie.
    - `ui-react/src/components/widgets/ChartTable.test.tsx` contains assertions for sorting and limit behavior.
  </acceptance_criteria>
  <done>Wave 0 chart test files exist with concrete fixtures and executable test cases tied to CHART-06/07 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared chart schemas and SQL field-aware encoding validation</name>
  <files>
    ui-react/src/components/widgets/shared/chartSchemas.ts
    ui-react/src/components/widgets/shared/chartFieldValidation.ts
    ui-react/src/components/widgets/chartSchemas.test.ts
  </files>
  <read_first>
    ui-react/src/components/widgets/shared/commonProps.ts
    ui-react/src/components/widgets/containers/List.tsx
    docs/flow/02_step_reference.md
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: each chart schema enforces locked `Chart.*` type names and shared `data_source` + `encoding` contract.
    - Test 2: encoding validator rejects missing channels and field names absent from `sql_response.fields`.
    - Test 3: numeric channels (`y`, `value`) reject incompatible field types.
  </behavior>
  <action>
    Create `chartSchemas.ts` exporting concrete Zod schemas with these exact public properties on base schema:
    - `type`
    - `data_source`
    - `encoding`
    - `title`
    - `description`
    - `legend`
    - `colors`
    - `format`
    - `empty_state`
    - `size` (reuse `SizeSchema`)

    Add discriminated chart schemas for exact type literals:
    - `Chart.Line` with required channels `encoding.x.field`, `encoding.y.field`, optional `encoding.series.field`.
    - `Chart.Bar` with same channel rules as line.
    - `Chart.Area` with same channel rules as line.
    - `Chart.Pie` with required `encoding.label.field`, `encoding.value.field`, optional `donut` boolean default `false`.
    - `Chart.Table` with optional `columns`, `sort_by`, `sort_order`, `limit`.

    Create `chartFieldValidation.ts` with exported function signature:
    `validateChartEncoding(chartType, encoding, sqlFields)`
    that returns `{ ok: true }` or `{ ok: false, kind: "config_error", code, channel, field }`.

    Implement concrete type buckets from Phase 9 metadata:
    - numeric: `integer`, `float`, `decimal`, `number`
    - temporal: `datetime`, `date`, `timestamp`
    - categorical: `text`, `string`, `boolean`

    Enforce channel constraints:
    - `x` allows temporal or categorical
    - `y` requires numeric
    - `value` requires numeric
    - `series`/`label` allow categorical or temporal
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/chartSchemas.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `chartSchemas.ts` contains exact literals `Chart.Line`, `Chart.Bar`, `Chart.Area`, `Chart.Pie`, `Chart.Table`.
    - `chartSchemas.ts` contains keys `data_source` and `encoding` in shared chart schema.
    - `chartFieldValidation.ts` contains exported function name `validateChartEncoding`.
    - `chartFieldValidation.ts` includes exact type tokens `integer`, `float`, `decimal`, `datetime`, `text`.
  </acceptance_criteria>
  <done>Chart contract is library-agnostic, schema-validated, and enforces SQL metadata-aware mapping rules before rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic chart state classifier and unified fallback frame</name>
  <files>
    ui-react/src/components/widgets/shared/chartState.ts
    ui-react/src/components/widgets/charts/ChartFrame.tsx
    ui-react/src/components/widgets/chartState.test.ts
  </files>
  <read_first>
    ui-react/src/components/BaseSourceCard.tsx
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-UI-SPEC.md
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: classifier emits `loading` when source status is refreshing.
    - Test 2: classifier emits `runtime_error` when source status is error or sql runtime error is present.
    - Test 3: classifier emits `config_error` when encoding validator fails.
    - Test 4: classifier emits `empty` on empty datasets and `ready` only when data and mapping are valid.
  </behavior>
  <action>
    Implement `chartState.ts` exporting `classifyChartState` with exact precedence order:
    1. `loading`
    2. `runtime_error`
    3. `config_error`
    4. `empty`
    5. `ready`

    Create `ChartFrame.tsx` with deterministic fallback copy (exact strings from UI-SPEC):
    - Empty heading: `No chart data available`
    - Empty body: `This widget has no rows to visualize yet. Refresh the source or adjust the query to return chartable results.`
    - Config error heading: `Invalid chart configuration`
    - Config error body: `One or more required fields are missing or incompatible for this chart type. Update the field mapping and try again.`
    - Runtime error body includes: `This chart cannot be shown right now. Check the SQL source status or fix the widget field mapping, then refresh the card.`

    Keep loading placeholders chart-specific by passing widget type into `ChartFrame` and rendering different loading labels per chart type.

    Do not register chart switch branches in `WidgetRenderer.tsx` in this plan. Plan 10-02 and 10-03 own runtime component wiring after chart runtime components exist.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/chartState.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `chartState.ts` contains all exact state literals `loading`, `runtime_error`, `config_error`, `empty`, `ready`.
    - `ChartFrame.tsx` contains exact heading `Invalid chart configuration`.
    - `ChartFrame.tsx` contains exact heading `No chart data available`.
    - `ChartFrame.tsx` exposes chart-specific loading labels keyed by widget type.
  </acceptance_criteria>
  <done>Chart state gating and unified fallback card behavior are ready for chart runtime components to consume without white-screen behavior.</done>
</task>

</tasks>

<verification>
Run chart foundation checks:
- `npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/chartSchemas.test.ts src/components/widgets/chartState.test.ts --run`
</verification>

<success_criteria>
- Chart schemas and runtime validator enforce channel/field contracts against normalized SQL metadata.
- Deterministic fallback classifier exists and is covered by automated tests.
- Shared chart runtime state and fallback primitives are ready for downstream renderer wiring plans.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-01-SUMMARY.md`
</output>
