---
phase: 10-sql-chart-widgets-and-sdui-rendering
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ui-react/src/components/widgets/charts/ChartTable.tsx
  - ui-react/src/components/widgets/shared/chartFormatting.ts
  - ui-react/src/components/widgets/WidgetRenderer.tsx
  - ui-react/src/components/widgets/ChartTable.test.tsx
  - docs/sdui/01_architecture_and_guidelines.md
  - docs/sdui/02_component_map_and_categories.md
  - docs/sdui/03_template_expression_spec.md
autonomous: true
requirements:
  - CHART-05
must_haves:
  truths:
    - "Users can render a SQL-backed table widget in Bento cards for dense result inspection."
    - "Table supports first-release features only: column selection, title overrides, simple formatting, sorting, limiting."
    - "Table shares deterministic chart fallback semantics and does not crash on empty/invalid data."
  artifacts:
    - path: "ui-react/src/components/widgets/charts/ChartTable.tsx"
      provides: "Chart.Table widget implementation"
    - path: "ui-react/src/components/widgets/shared/chartFormatting.ts"
      provides: "Simple deterministic value formatters used by Chart.Table"
    - path: "docs/sdui/02_component_map_and_categories.md"
      provides: "Canonical registration of Chart.Table in SDUI component taxonomy"
  key_links:
    - from: "ui-react/src/components/widgets/WidgetRenderer.tsx"
      to: "ui-react/src/components/widgets/charts/ChartTable.tsx"
      via: "switch case render"
      pattern: "case \"Chart\\.Table\""
    - from: "ui-react/src/components/widgets/charts/ChartTable.tsx"
      to: "ui-react/src/components/widgets/shared/chartFormatting.ts"
      via: "column format application"
      pattern: "format"
    - from: "docs/sdui/01_architecture_and_guidelines.md"
      to: "docs/sdui/02_component_map_and_categories.md"
      via: "component contract documentation sync"
      pattern: "Chart\\.Table"
---

<objective>
Deliver the Chart.Table widget and documentation updates as a chart-compatible dense inspection surface.

Purpose: Complete Phase 10 first-release table scope with deterministic behavior and no drift between implementation and SDUI docs.
Output: Runtime Chart.Table component, formatting helper, renderer wiring, tests, and SDUI documentation updates.
</objective>

<execution_context>
@/Users/xingminghua/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-UI-SPEC.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-01-SUMMARY.md
@docs/sdui/01_architecture_and_guidelines.md
@docs/sdui/02_component_map_and_categories.md
@docs/sdui/03_template_expression_spec.md
@ui-react/src/components/widgets/WidgetRenderer.tsx

<interfaces>
Locked first-release Chart.Table scope (10-CONTEXT.md):
```text
- column selection
- column title overrides
- simple formatting
- sorting
- limiting
```

Deferred (must not implement in this plan):
```text
- pagination grid product behavior
- pinned headers
- complex cell templates
- advanced interactions
```

Existing list sorting/limit pattern reference (WidgetRenderer.tsx):
```typescript
if (widget.sort_by) { ... }
if (widget.limit) { processedData = processedData.slice(0, widget.limit); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Chart.Table runtime component with deterministic sort/limit/column behavior</name>
  <files>
    ui-react/src/components/widgets/charts/ChartTable.tsx
    ui-react/src/components/widgets/shared/chartFormatting.ts
    ui-react/src/components/widgets/ChartTable.test.tsx
  </files>
  <read_first>
    ui-react/src/components/widgets/containers/List.tsx
    ui-react/src/components/widgets/shared/chartSchemas.ts
    ui-react/src/components/widgets/shared/chartState.ts
    ui-react/src/components/widgets/charts/ChartFrame.tsx
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: selected columns render in declared order with title overrides.
    - Test 2: sorting honors `sort_by` + `sort_order` and is deterministic.
    - Test 3: limit applies after sorting and before render.
    - Test 4: simple formatter outputs deterministic display values.
  </behavior>
  <action>
    Create `ChartTable.tsx` with these concrete props consumed from schema:
    - `data_source`
    - `columns` (array of `{ field, title?, format? }`)
    - `sort_by`
    - `sort_order` (`asc` | `desc`)
    - `limit`
    - shared props (`title`, `description`, `empty_state`)

    Implement deterministic row processing order:
    1. start from `data_source`
    2. apply sort if `sort_by` provided
    3. apply limit if `limit` provided
    4. render final rows

    Create `chartFormatting.ts` with simple format handlers only:
    - `number` -> fixed 2 decimals when numeric
    - `percent` -> multiply by 100 then fixed 1 decimal with `%`
    - `datetime` -> ISO string or locale-safe short string from Date parse
    - `text` -> string conversion

    Keep table behavior in scope: no pagination, no pinned header, no custom cell templates.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartTable.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `ChartTable.tsx` exists and contains tokens `sort_by`, `sort_order`, `limit`, `columns`.
    - `chartFormatting.ts` exists and contains formatter keys `number`, `percent`, `datetime`, `text`.
    - `ChartTable.test.tsx` includes assertions for title overrides and sorted/limited output ordering.
    - `ChartTable.test.tsx` does not include pagination assertions.
  </acceptance_criteria>
  <done>Chart.Table provides first-release dense inspection capabilities exactly within locked scope and passes dedicated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Chart.Table into WidgetRenderer and enforce shared fallback semantics</name>
  <files>
    ui-react/src/components/widgets/WidgetRenderer.tsx
    ui-react/src/components/widgets/charts/ChartTable.tsx
    ui-react/src/components/widgets/ChartTable.test.tsx
  </files>
  <read_first>
    ui-react/src/components/widgets/WidgetRenderer.tsx
    ui-react/src/components/widgets/shared/chartSchemas.ts
    ui-react/src/components/widgets/shared/chartFieldValidation.ts
    ui-react/src/components/widgets/charts/ChartFrame.tsx
  </read_first>
  <behavior>
    - Test 1: WidgetRenderer accepts `Chart.Table` schema branch.
    - Test 2: invalid table mapping yields deterministic config-error fallback inside card.
    - Test 3: empty dataset yields unified empty state copy.
  </behavior>
  <action>
    Update `WidgetRenderer.tsx` to register and render `Chart.Table` via explicit branch:
    - `case "Chart.Table"` -> render `ChartTable` component.

    Ensure Chart.Table uses same validation + state path as other charts:
    - invalid/missing column field references -> config error state in card
    - empty rows -> `No chart data available` state
    - upstream runtime error flag -> runtime error state

    Keep fallback wording identical to UI-SPEC strings already used by ChartFrame; do not introduce table-specific alternative copy.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartTable.test.tsx src/components/widgets/WidgetRenderer.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `WidgetRenderer.tsx` contains `case "Chart.Table"`.
    - Chart.Table invalid mapping tests assert heading `Invalid chart configuration`.
    - Chart.Table empty-data tests assert heading `No chart data available`.
    - Existing WidgetRenderer regression tests still pass.
  </acceptance_criteria>
  <done>Chart.Table is fully integrated into the SDUI renderer and inherits deterministic non-crashing fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update SDUI documentation to canonize chart widget contracts</name>
  <files>
    docs/sdui/01_architecture_and_guidelines.md
    docs/sdui/02_component_map_and_categories.md
    docs/sdui/03_template_expression_spec.md
  </files>
  <read_first>
    docs/sdui/01_architecture_and_guidelines.md
    docs/sdui/02_component_map_and_categories.md
    docs/sdui/03_template_expression_spec.md
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-UI-SPEC.md
  </read_first>
  <action>
    Update SDUI docs to include exact new chart types and contract shape:
    - Add `Chart.Line`, `Chart.Bar`, `Chart.Area`, `Chart.Pie`, `Chart.Table` as supported widget types.
    - Document shared chart props exactly:
      `data_source`, `encoding`, `title`, `description`, `legend`, `colors`, `format`, `empty_state`.
    - Document per-chart required encoding channels:
      - Line/Bar/Area: `x`, `y` (+ optional `series`)
      - Pie: `label`, `value`
      - Table: `columns.field` references against dataset fields
    - Document deterministic fallback states: `loading`, `empty`, `config_error`, `runtime_error`.
    - Add expression examples for chart field mappings that reference normalized SQL paths (e.g., `sql_response.rows`, `sql_response.fields`).

    Remove or adjust any line that would classify chart types as unsupported legacy names after this phase.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `docs/sdui/02_component_map_and_categories.md` contains all five `Chart.*` types.
    - docs explicitly list `data_source` + `encoding` as chart contract core.
    - docs include fallback state names `loading`, `empty`, `config_error`, `runtime_error`.
    - no doc section still marks `line_chart`/`bar_chart` as future when `Chart.Line`/`Chart.Bar` are now supported.
  </acceptance_criteria>
  <done>Documentation is synchronized with shipped chart contracts so authoring and implementation references stay aligned.</done>
</task>

</tasks>

<verification>
- `npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartTable.test.tsx --run`
- `npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test:core`
</verification>

<success_criteria>
- Chart.Table renders normalized SQL rows with first-release scoped features.
- Chart.Table wiring preserves deterministic in-card fallback behavior.
- SDUI docs canonicalize the new chart contracts and no longer drift from implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-03-SUMMARY.md`
</output>
