---
phase: 10-sql-chart-widgets-and-sdui-rendering
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - ui-react/src/components/widgets/charts/adapters/rechartsAdapter.tsx
  - ui-react/src/components/widgets/charts/ChartLine.tsx
  - ui-react/src/components/widgets/charts/ChartBar.tsx
  - ui-react/src/components/widgets/charts/ChartArea.tsx
  - ui-react/src/components/widgets/charts/ChartPie.tsx
  - ui-react/src/components/widgets/WidgetRenderer.tsx
  - ui-react/src/components/widgets/ChartWidgets.test.tsx
autonomous: true
requirements:
  - CHART-01
  - CHART-02
  - CHART-03
  - CHART-04
  - CHART-06
must_haves:
  truths:
    - "Users can render SQL-backed line, bar, area, and pie charts in Bento cards from normalized sql_response data."
    - "Public SDUI chart contract remains library-agnostic while internal renderer uses Recharts."
    - "Pie chart supports donut mode in first release."
    - "Each concrete chart widget (line/bar/area/pie) explicitly integrates deterministic loading/empty/config-error/runtime-error fallbacks before rendering chart primitives."
  artifacts:
    - path: "ui-react/src/components/widgets/charts/adapters/rechartsAdapter.tsx"
      provides: "Single internal mapping layer from SDUI encoding to Recharts props"
    - path: "ui-react/src/components/widgets/charts/ChartLine.tsx"
      provides: "Line chart runtime component"
    - path: "ui-react/src/components/widgets/charts/ChartBar.tsx"
      provides: "Bar chart runtime component"
    - path: "ui-react/src/components/widgets/charts/ChartArea.tsx"
      provides: "Area chart runtime component"
    - path: "ui-react/src/components/widgets/charts/ChartPie.tsx"
      provides: "Pie/donut runtime component"
  key_links:
    - from: "ui-react/src/components/widgets/WidgetRenderer.tsx"
      to: "ui-react/src/components/widgets/charts/ChartLine.tsx"
      via: "switch case render"
      pattern: "case \"Chart\\.Line\""
    - from: "ui-react/src/components/widgets/charts/adapters/rechartsAdapter.tsx"
      to: "recharts"
      via: "internal component mapping"
      pattern: "ResponsiveContainer|LineChart|BarChart|AreaChart|PieChart"
    - from: "ui-react/src/components/widgets/charts/ChartPie.tsx"
      to: "encoding.value.field / encoding.label.field"
      via: "Pie dataKey and nameKey"
      pattern: "dataKey|nameKey|innerRadius"
---

<objective>
Implement render-ready cartesian and pie chart widgets on top of the validated chart foundation from 10-01.

Purpose: Deliver user-visible SQL chart rendering for line/bar/area/pie while preserving the locked library-agnostic SDUI contract.
Output: Recharts adapter and chart components wired through WidgetRenderer with regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.claude/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-RESEARCH.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-UI-SPEC.md
@.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-01-SUMMARY.md
@ui-react/src/components/widgets/WidgetRenderer.tsx
@ui-react/package.json

<interfaces>
From ui-react/package.json:
```json
"recharts": "^2.15.0"
```

From phase locked decisions (10-CONTEXT.md):
```text
- Public contract must be library-agnostic
- Internal renderer may use Recharts
- Direct chart component types only: Chart.Line, Chart.Bar, Chart.Area, Chart.Pie
- Pie supports donut in first release
```

From WidgetRenderer pattern:
```typescript
const parseResult = RuntimeWidgetSchema.safeParse(evaluatedWidget);
if (!parseResult.success) {
  return <div>Invalid widget configuration ...</div>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build internal Recharts adapter for cartesian and pie mappings</name>
  <files>
    ui-react/src/components/widgets/charts/adapters/rechartsAdapter.tsx
    ui-react/src/components/widgets/ChartWidgets.test.tsx
  </files>
  <read_first>
    ui-react/package.json
    ui-react/src/components/widgets/shared/chartSchemas.ts
    ui-react/src/components/widgets/shared/chartFieldValidation.ts
    /Users/xingminghua/Coding/evoltonnac/glanceus/.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: adapter maps cartesian encoding x/y/(optional series) to Recharts dataKey props.
    - Test 2: adapter maps pie encoding label/value and donut flag to Pie nameKey/dataKey/innerRadius.
    - Test 3: adapter applies deterministic palette order when colors are not provided.
  </behavior>
  <action>
    Create `rechartsAdapter.tsx` as the only file that imports Recharts chart primitives and map SDUI contract to internal props.

    Import and use these exact Recharts components:
    - `ResponsiveContainer`
    - `LineChart`, `Line`
    - `BarChart`, `Bar`
    - `AreaChart`, `Area`
    - `PieChart`, `Pie`, `Cell`
    - `XAxis`, `YAxis`, `CartesianGrid`, `Tooltip`, `Legend`

    Implement exported adapter functions:
    - `renderLineChart({ rows, encoding, legend, colors })`
    - `renderBarChart({ rows, encoding, legend, colors })`
    - `renderAreaChart({ rows, encoding, legend, colors })`
    - `renderPieChart({ rows, encoding, legend, colors, donut })`

    Apply default palette order exactly as UI-SPEC: `blue`, `teal`, `green`, `amber`, `violet`, `pink`, `slate`, `red`.

    Keep all Recharts-specific keys (`dataKey`, `nameKey`, `innerRadius`) internal to this adapter and do not expose them in SDUI schema props.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartWidgets.test.tsx --run -t "renders line chart from sql_response rows"</automated>
  </verify>
  <acceptance_criteria>
    - `rechartsAdapter.tsx` exists and contains `ResponsiveContainer` import.
    - `rechartsAdapter.tsx` contains symbols `LineChart`, `BarChart`, `AreaChart`, `PieChart`.
    - `rechartsAdapter.tsx` contains token `innerRadius` for donut support.
    - `rechartsAdapter.tsx` contains color token sequence including `blue` and `slate`.
  </acceptance_criteria>
  <done>Renderer-specific mapping is isolated to one adapter file and supports all required chart families without leaking Recharts props to SDUI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Chart.Line, Chart.Bar, Chart.Area, and Chart.Pie runtime components</name>
  <files>
    ui-react/src/components/widgets/charts/ChartLine.tsx
    ui-react/src/components/widgets/charts/ChartBar.tsx
    ui-react/src/components/widgets/charts/ChartArea.tsx
    ui-react/src/components/widgets/charts/ChartPie.tsx
    ui-react/src/components/widgets/charts/adapters/rechartsAdapter.tsx
    ui-react/src/components/widgets/ChartWidgets.test.tsx
  </files>
  <read_first>
    ui-react/src/components/widgets/charts/ChartFrame.tsx
    ui-react/src/components/widgets/shared/chartState.ts
    ui-react/src/components/widgets/shared/chartFieldValidation.ts
    ui-react/src/components/widgets/shared/chartSchemas.ts
  </read_first>
  <behavior>
    - Test 1: each chart component delegates pre-render state handling to classifier and shows ChartFrame fallbacks.
    - Test 2: each component renders Recharts output only when state is `ready`.
    - Test 3: pie chart renders donut when `donut: true`.
    - Test 4: line/bar/area/pie each exercise deterministic `loading`, `runtime_error`, `config_error`, and `empty` fallbacks before `ready` rendering.
  </behavior>
  <action>
    Create four runtime components that all follow the same deterministic sequence:
    1. Resolve dataset from `data_source`.
    2. Validate encoding with `validateChartEncoding` against `sql_response.fields`.
    3. Classify with `classifyChartState`.
    4. Render `ChartFrame` fallback for `loading`, `runtime_error`, `config_error`, `empty`.
    5. Render adapter output for `ready` only.

    `Chart.Line`, `Chart.Bar`, and `Chart.Area` must support optional grouping via `encoding.series.field` in first release.

    `Chart.Pie` must support `donut` with these concrete values:
    - `donut: true` -> `innerRadius` set to a non-zero value (e.g. `48`)
    - `donut: false` -> `innerRadius` set to `0`

    For each of `Chart.Line`, `Chart.Bar`, `Chart.Area`, and `Chart.Pie`, implement explicit fallback integration using `ChartFrame` so each widget path handles:
    - `loading` -> chart-specific loading placeholder
    - `runtime_error` -> unified runtime-error body
    - `config_error` -> unified invalid-configuration body
    - `empty` -> unified empty-state body
    - `ready` -> adapter-rendered chart output

    Keep these components render-only: no business-data repair, no connector-specific branches, no fallback field inference.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartWidgets.test.tsx --run -t "renders pie chart from sql_response rows"</automated>
  </verify>
  <acceptance_criteria>
    - `ChartLine.tsx`, `ChartBar.tsx`, `ChartArea.tsx`, `ChartPie.tsx` all exist.
    - Each chart component file contains `classifyChartState` call.
    - `ChartPie.tsx` contains `donut` handling and passes `innerRadius` via adapter path.
    - Each chart component (`ChartLine.tsx`, `ChartBar.tsx`, `ChartArea.tsx`, `ChartPie.tsx`) contains explicit fallback handling paths for `loading`, `runtime_error`, `config_error`, and `empty` before `ready` render.
    - `ChartWidgets.test.tsx` contains per-widget fallback assertions for line/bar/area/pie config-error and empty-state behavior.
    - No chart component file imports Recharts directly (adapter-only constraint).
  </acceptance_criteria>
  <done>All four chart components render SQL-backed visuals through shared deterministic state gates and adapter internals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register chart components in WidgetRenderer and pass chart widget regression suite</name>
  <files>
    ui-react/src/components/widgets/WidgetRenderer.tsx
    ui-react/src/components/widgets/ChartWidgets.test.tsx
  </files>
  <read_first>
    ui-react/src/components/widgets/WidgetRenderer.tsx
    ui-react/src/components/widgets/shared/chartSchemas.ts
    ui-react/src/components/widgets/charts/ChartLine.tsx
    ui-react/src/components/widgets/charts/ChartBar.tsx
    ui-react/src/components/widgets/charts/ChartArea.tsx
    ui-react/src/components/widgets/charts/ChartPie.tsx
  </read_first>
  <behavior>
    - Test 1: RuntimeWidgetSchema accepts all four chart types.
    - Test 2: switch-case renders each chart component branch.
    - Test 3: invalid chart config still uses existing validation fallback path.
  </behavior>
  <action>
    Update `WidgetRenderer.tsx` to:
    - include chart schemas in `createWidgetSchema` discriminated union.
    - import chart components and add explicit switch branches:
      - `case "Chart.Line"`
      - `case "Chart.Bar"`
      - `case "Chart.Area"`
      - `case "Chart.Pie"`
    - keep existing invalid-widget fallback block unchanged in structure (still shows `Invalid widget configuration: {type}` + `Path` + `Reason`).

    Ensure chart widget branch props include evaluated template values from existing resolver flow and do not bypass `resolveWidgetParams`.
  </action>
  <verify>
    <automated>npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartWidgets.test.tsx src/components/widgets/WidgetRenderer.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `WidgetRenderer.tsx` includes all chart case labels for line/bar/area/pie.
    - `WidgetRenderer.tsx` includes chart schemas in discriminated union.
    - `ChartWidgets.test.tsx` passes named tests for line/bar/area/pie rendering.
    - Existing `WidgetRenderer.test.tsx` remains green.
  </acceptance_criteria>
  <done>Chart widget rendering is fully wired through SDUI runtime validation and component dispatch without regressions to existing widgets.</done>
</task>

</tasks>

<verification>
- `npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/ChartWidgets.test.tsx --run`
- `npm --prefix /Users/xingminghua/Coding/evoltonnac/glanceus/ui-react run test -- src/components/widgets/WidgetRenderer.test.tsx --run`
</verification>

<success_criteria>
- Line/bar/area/pie widgets render from normalized SQL rows in Bento cards.
- Public schema remains library-agnostic while Recharts is internal-only.
- Donut mode works for pie widget and is covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sql-chart-widgets-and-sdui-rendering/10-02-SUMMARY.md`
</output>
