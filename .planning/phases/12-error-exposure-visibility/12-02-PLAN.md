---
phase: 12-error-exposure-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/executor.py
  - ui-react/src/pages/Dashboard.tsx
  - ui-react/src/components/BaseSourceCard.tsx
  - tests/core/test_executor_errors.py
  - core/source_state.py
autonomous: true
requirements: 
  - "Phase 12: 数据源 Flow 普通 Pattern 步骤（Fetch、脚本操作等）错误实现统一结构化返回与前端可视化。"
  - "Phase 12: 认证 Flow 覆盖 OAuth 报错、WebScrap 报错、WebScraper 403、非登录状态触发 cURL/CYL 报错。"
must_haves:
  truths:
    - "User can immediately see when a flow execution halts due to an error."
    - "User can view live execution progress and output for script steps."
    - "User sees a clickable error badge in the UI that can show detailed traces behind a toggle."
  artifacts:
    - path: "core/executor.py"
      provides: "Fail-fast flow execution logic"
    - path: "ui-react/src/pages/Dashboard.tsx"
      provides: "Error state visualization"
    - path: "tests/core/test_executor_errors.py"
      provides: "Tests for fail fast and auth checking logic"
  key_links:
    - from: "core/executor.py"
      to: "core/source_state.py"
      via: "Error status updates"
      pattern: "_update_state.*ERROR"
---

<objective>
Halt execution immediately upon flow step errors and expose these errors visibly in the UI.

Purpose: Prevent cascading failures by failing fast, and provide users with actionable error insights.
Output: Updated executor with fail-fast logic, updated Dashboard components showing error badges.
</objective>

<context>
@core/executor.py
@ui-react/src/pages/Dashboard.tsx
@ui-react/src/components/BaseSourceCard.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement Backend Fail-Fast & Streaming Output</name>
  <files>core/executor.py, core/source_state.py, tests/core/test_executor_errors.py</files>
  <behavior>
    - Test 1: Flow execution stops immediately when a step raises an exception.
    - Test 2: Source state transitions to ERROR and captures standard streams from the script.
    - Test 3: Flow fails early before executing a script if the required authentication is missing.
  </behavior>
  <action>Update `core/executor.py` `_run_flow` to immediately halt on any step error, setting the state to ERROR. Explicitly check authentication status before executing cURL or CYL scripts to intercept failures early. Update script execution tool to stream `stdout`/`stderr` progressively to the frontend (e.g. by capturing logs and storing them in source state message). Ensure non-authenticated states explicitly trigger failure rather than silent hangs.</action>
  <verify>
    <automated>pytest tests/core/test_executor_errors.py</automated>
  </verify>
  <done>Any exception in a flow step stops the execution immediately, unauthenticated scripts fail early, and standard streams are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Frontend Error Badge & Toggle</name>
  <files>ui-react/src/pages/Dashboard.tsx, ui-react/src/components/BaseSourceCard.tsx</files>
  <action>Update source card and dashboard list to display a persistent error badge if `source.error` is present. Clicking the badge opens a modal or panel displaying the error message. Add a "Show Details" toggle to reveal the raw stack trace. Integrate this into `BaseSourceCard.tsx` and the side panel in `Dashboard.tsx`.</action>
  <verify>
    <automated>npm run build --prefix ui-react</automated>
  </verify>
  <done>Errors are visibly exposed in the UI with an option to view detailed traces.</done>
</task>
</tasks>

<success_criteria>
- Failed flow steps immediately stop execution.
- Missing authentication halts flow immediately.
- Frontend shows a red error badge for failed sources.
- Stack trace is hidden by default behind a toggle.
</success_criteria>