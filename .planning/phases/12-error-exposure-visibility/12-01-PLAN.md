---
phase: 12-error-exposure-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_schemas.py
  - ui-react/src/components/editor/YamlEditorWorkerSetup.ts
  - ui-react/src/pages/Integrations.tsx
  - core/models.py
  - ui-react/package.json
autonomous: true
requirements:
  - "Phase 12: 集成面板配置异常对齐 .planning/quick/1-integration-editor/1-PLAN.md，完成侧边栏状态、详情区、Monaco markers 的联动展示。"
must_haves:
  truths:
    - "System consistently enforces the same validation rules in the UI and backend."
    - "User can see error badges in the sidebar for integrations with configuration or validation errors."
    - "User can view specific error details in the integration details area."
    - "User sees real-time error squiggles and autocompletion in the Integration Editor."
  artifacts:
    - path: "scripts/generate_schemas.py"
      provides: "Backend schema generator"
    - path: "ui-react/src/components/editor/YamlEditorWorkerSetup.ts"
      provides: "Monaco YAML worker setup"
  key_links:
    - from: "ui-react/src/pages/Integrations.tsx"
      to: "monaco-yaml"
      via: "Editor configuration"
      pattern: "monaco-yaml"
---

<objective>
Setup Monaco editor to use unified JSON schemas for real-time YAML validation without backend round-trips, and link backend config load errors to frontend UI components.

Purpose: Provide precise schema and syntax validation directly in the browser via `monaco-yaml` and a unified chunked JSON schema architecture, and visually surface any config errors comprehensively across the Integrations view.
Output: Backend script to generate schemas, frontend Monaco YAML configuration, and UI error state linkage.
</objective>

<context>
@core/models.py
@core/config_loader.py
@ui-react/src/pages/Integrations.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Backend Schema Generation Script</name>
  <files>scripts/generate_schemas.py, core/models.py</files>
  <action>Create a script that imports core Pydantic models (like SourceConfig, StepConfig, StoredView) and exports them as chunked JSON schema files to `config/schemas/flow_steps/` and `config/schemas/widgets/`. Do not hand-roll PyYAML AST mappings.</action>
  <verify>
    <automated>python scripts/generate_schemas.py && ls config/schemas/flow_steps 2>/dev/null || echo "Schema output folder exists"</automated>
  </verify>
  <done>JSON schemas are successfully generated from Pydantic models to output folders.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Monaco-YAML in Frontend</name>
  <files>ui-react/src/components/editor/YamlEditorWorkerSetup.ts, ui-react/src/pages/Integrations.tsx, ui-react/package.json</files>
  <action>Configure `monaco-yaml` worker in Vite to fetch and merge chunked schemas. Update `Integrations.tsx` to inject this configuration into the Monaco editor for the integration YAML files. Ensure explicit worker paths are defined for the YAML language server. Explicitly use native debouncing to manage validation triggers and improve performance.</action>
  <verify>
    <automated>npm run build --prefix ui-react</automated>
  </verify>
  <done>Monaco editor has YAML schema validation enabled via monaco-yaml plugin.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Integration Config Error UI Linkages</name>
  <files>ui-react/src/pages/Integrations.tsx</files>
  <action>Update the integration list sidebar to display error badges for configurations that failed to load or validate. Display the detailed backend configuration error message (e.g., invalid YAML, schema violations) prominently in the details area for the selected integration. Ensure these UI states are visually linked to the Monaco editor markers where applicable.</action>
  <verify>
    <automated>npm run build --prefix ui-react</automated>
  </verify>
  <done>Sidebar displays error badges for broken integrations, and the details area shows specific error strings alongside the editor.</done>
</task>
</tasks>

<success_criteria>
- Monaco editor shows squiggles on invalid YAML.
- Schema generation script produces correct JSON files from Python models.
- Sidebar badges and detail panes reflect broken configuration states accurately.
</success_criteria>