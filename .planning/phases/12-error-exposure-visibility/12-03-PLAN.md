---
phase: 12-error-exposure-visibility
plan: 03
type: execute
wave: 2
depends_on: ["12-02"]
files_modified:
  - core/executor.py
  - core/source_state.py
  - ui-react/src/pages/Dashboard.tsx
  - ui-react/src/components/auth/FlowHandler.tsx
  - ui-react/src/hooks/useScraper.ts
  - tests/core/test_scraper_states.py
autonomous: true
requirements:
  - "Phase 12: 认证 Flow 覆盖 OAuth 报错、WebScrap 报错、WebScraper 403"
must_haves:
  truths:
    - "Interactive steps clearly distinguish between SUSPENDED (missing info) and ERROR (bad info)."
    - "WebScraper 403s/captchas trigger SUSPENDED instead of retry loops."
    - "Resuming a SUSPENDED WebScraper forces it into foreground mode."
  artifacts:
    - path: "core/executor.py"
      provides: "State resolution for SUSPENDED vs ERROR"
    - path: "ui-react/src/components/auth/FlowHandler.tsx"
      provides: "Manual foreground trigger handling"
    - path: "tests/core/test_scraper_states.py"
      provides: "Tests for SUSPENDED and ERROR resolution"
  key_links:
    - from: "core/executor.py"
      to: "core/source_state.py"
      via: "Exception to interaction mapping"
      pattern: "SUSPENDED"
---

<objective>
Refine interactive flow step states to differentiate between missing information and invalid information, specifically addressing WebScraper foreground recovery.

Purpose: Prevent scraper infinite retry loops on auth walls and provide clear, distinct interaction paths for OAuth and cURL steps.
Output: Differentiated SUSPENDED/ERROR states, foreground manual scraper trigger.
</objective>

<context>
@core/executor.py
@core/source_state.py
@ui-react/src/pages/Dashboard.tsx
@ui-react/src/components/auth/FlowHandler.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Differentiate SUSPENDED vs ERROR Backend States</name>
  <files>core/executor.py, core/source_state.py, tests/core/test_scraper_states.py</files>
  <behavior>
    - Test 1: WebScraper 403 or captcha response yields SUSPENDED state.
    - Test 2: Invalid credentials yield ERROR state.
    - Test 3: Missing config yields SUSPENDED state.
  </behavior>
  <action>Refactor `_exception_to_interaction` and execution logic to distinguish `SUSPENDED` (missing config, 403 HTTP response, captchas) from `ERROR` (bad credentials provided). Add robust detection for WebScraper 403/redirects/timeouts to yield a `SUSPENDED` state.</action>
  <verify>
    <automated>pytest tests/core/test_scraper_states.py</automated>
  </verify>
  <done>Backend accurately categorizes interactive step failures into SUSPENDED or ERROR.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Manual Trigger & Re-entry</name>
  <files>ui-react/src/pages/Dashboard.tsx, ui-react/src/components/auth/FlowHandler.tsx, ui-react/src/hooks/useScraper.ts</files>
  <action>Update FlowHandler and related UI to present both `SUSPENDED` and `ERROR` states distinctly. For WebScraper `SUSPENDED` tasks, provide a manual trigger button that explicitly passes a flag to run the webview in the foreground for manual user interaction (logging in, solving captcha).</action>
  <verify>
    <automated>npm run build --prefix ui-react</automated>
  </verify>
  <done>User can manually resume blocked WebScraper tasks in foreground mode directly from the UI.</done>
</task>
</tasks>

<success_criteria>
- WebScraper 403 correctly halts flow as SUSPENDED.
- User can trigger foreground webview from UI to bypass captchas.
- Both SUSPENDED and ERROR states allow form re-entry.
</success_criteria>