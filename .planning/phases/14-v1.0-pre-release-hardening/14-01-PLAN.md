---
phase: 14-v1.0-pre-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-v1.0-pre-release-hardening/14-RISK-REGISTER.md
  - core/integration_manager.py
  - ui-react/src/components/Dashboard.tsx
autonomous: true
requirements:
  - "v1.0 发布前设计稳定性加固"
must_haves:
  truths:
    - "Potentially breaking design risks are explicitly documented before release."
    - "Automated YAML version upgrade runs in memory."
    - "Dashboard widgets degrade silently on breaking schema changes."
    - "Deprecation warnings are logged for old syntax."
  artifacts:
    - path: ".planning/phases/14-v1.0-pre-release-hardening/14-RISK-REGISTER.md"
      provides: "Pre-release breaking-risk register and mitigation actions."
    - path: "core/integration_manager.py"
      provides: "YAML version upgrade and deprecation warning logic."
  key_links:
    - from: "core/integration_manager.py"
      to: "integrations/"
      via: "YAML parsing"
      pattern: "yaml\\.safe_load"
---

<objective>
完成发布前的设计风险审计，并从代码层面实施兼容性保护（YAML 热升级、微件静默降级、旧语法弃用警告）。
</objective>

<context>
@.planning/phases/14-v1.0-pre-release-hardening/14-CONTEXT.md
@.planning/phases/14-v1.0-pre-release-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Risk Register & Compatibility Strategy</name>
  <files>.planning/phases/14-v1.0-pre-release-hardening/14-RISK-REGISTER.md</files>
  <action>梳理 Integration schema、Flow DSL 等关键契约边界的破坏性风险，为每个风险定义版本化、迁移路径、弃用窗口、回滚方案，输出到风险台账文档中。</action>
  <verify>
    <automated>test -f .planning/phases/14-v1.0-pre-release-hardening/14-RISK-REGISTER.md || echo "MISSING"</automated>
  </verify>
  <done>风险与兼容策略形成可执行的 markdown 清单。</done>
</task>

<task type="auto">
  <name>Task 2: Implement Backend YAML Compatibility & Warnings</name>
  <files>core/integration_manager.py</files>
  <action>在后端解析集成配置时，增加 `version` 字段检测。对于旧版本 YAML 结构，在内存中执行自动热升级转换到新结构，同时使用 logger 打印出明确的弃用期警告日志，但不阻断执行。</action>
  <verify>
    <automated>pytest tests/core/test_integration_manager.py -q || echo "MISSING — Wave 0 must create test_integration_manager.py first"</automated>
  </verify>
  <done>旧版 YAML 可以被自动升级解析并在后台打印警告。</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dashboard Widget Silent Fallback</name>
  <files>ui-react/src/components/Dashboard.tsx</files>
  <action>在前端渲染微件 (Widgets) 时增加容错包装。如果遇到微件配置参数 (如组件类型) 发生破坏性改动导致无法匹配或渲染失败，进行静默降级处理（如渲染为默认文本或空状态，保留原配置不崩溃）。</action>
  <verify>
    <automated>npm run test --prefix ui-react -- --passWithNoTests</automated>
  </verify>
  <done>微件在遇到异常配置时不会导致整个 Dashboard 崩溃。</done>
</task>

</tasks>

<success_criteria>
具备完整风险台账文档，并且代码层面已实现 YAML 自动升级和前端微件的静默降级策略。
</success_criteria>
