---
phase: 14-v1.0-pre-release-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 14-02
files_modified:
  - ui-react/src/components/EmptyState.tsx
  - ui-react/src/components/RouteInterceptor.tsx
  - ui-react/src/components/InlineError.tsx
  - ui-react/src/pages/
  - ui-react/src/styles/
  - .planning/phases/14-v1.0-pre-release-hardening/14-UAT.md
autonomous: true
requirements:
  - "UI 和交互细节补全"
must_haves:
  truths:
    - "Release-critical pages have consistent loading/empty/error/success interactions."
    - "Interaction feedback is explicit and discoverable."
  artifacts:
    - path: ".planning/phases/14-v1.0-pre-release-hardening/14-UAT.md"
      provides: "UI/interaction pre-release acceptance checklist."
    - path: "ui-react/src/components/EmptyState.tsx"
      provides: "Reusable empty state component with creation guidance."
    - path: "ui-react/src/components/RouteInterceptor.tsx"
      provides: "Unsaved changes confirmation dialog wrapper."
    - path: "ui-react/src/components/InlineError.tsx"
      provides: "Component for displaying inline form errors."
  key_links: []
---

<objective>
补齐发布前 UI 与交互细节，确保功能可用同时具备稳定、可理解、可反馈的体验。
</objective>

<context>
@.planning/phases/14-v1.0-pre-release-hardening/14-CONTEXT.md
@ui-react/src/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Error and Success Feedback</name>
  <files>ui-react/src/components/InlineError.tsx, ui-react/src/pages/</files>
  <action>实施明确的 UI 错误与成功反馈规范：成功等弱提示使用 Toast（现有库），表单报错等需长时展示信息使用新创建的 InlineError 内联错误组件。</action>
  <verify>
    <automated>cd ui-react && npm run test --passWithNoTests || echo "MISSING"</automated>
  </verify>
  <done>Toast 和 Inline Error 反馈模式已被正确应用到页面中。</done>
</task>

<task type="auto">
  <name>Task 2: Implement Empty and Loading States</name>
  <files>ui-react/src/components/EmptyState.tsx, ui-react/src/pages/</files>
  <action>实施空状态与加载状态设计：Dashboard/列表为空时，展示 EmptyState 空状态组件（含图标、文字说明及“去创建”引导按钮）；明确各组件自身的兜底加载状态（无全局骨架屏）。</action>
  <verify>
    <automated>cd ui-react && npm run test --passWithNoTests || echo "MISSING"</automated>
  </verify>
  <done>页面为空或加载时展示了符合预期的兜底状态。</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dirty State Route Interception</name>
  <files>ui-react/src/components/RouteInterceptor.tsx, ui-react/src/pages/</files>
  <action>实施路由拦截逻辑：在 Integration 等编辑器中如果存在未保存修改，当用户尝试离开页面时，使用 RouteInterceptor 弹出拦截确认框防止数据丢失。</action>
  <verify>
    <automated>cd ui-react && npm run test --passWithNoTests || echo "MISSING"</automated>
  </verify>
  <done>未保存更改的路由离开会被拦截提示。</done>
</task>

<task type="auto">
  <name>Task 4: Add UAT Checklist for UX Polish</name>
  <files>.planning/phases/14-v1.0-pre-release-hardening/14-UAT.md</files>
  <action>整理发布前 UI/交互验收清单，覆盖鼠标操作、键盘可达、错误恢复与提示可见性。</action>
  <verify>
    <automated>test -f .planning/phases/14-v1.0-pre-release-hardening/14-UAT.md</automated>
  </verify>
  <done>团队可按 checklist 快速回归关键体验。</done>
</task>

</tasks>

<success_criteria>
UI 与交互细节达到 v1.0 发布质量线，避免“只可用、不好用”的交付风险。
</success_criteria>
