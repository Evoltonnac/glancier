---
phase: 14-v1.0-pre-release-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-04
  - 14-05
files_modified:
  - README.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/TESTING.md
  - docs/
  - core/api.py
  - ui-react/src/components/
  - core/steps/__init__.py
  - core/steps/http_step.py
  - core/steps/browser_step.py
  - core/executor.py
autonomous: true
requirements:
  - "文档和代码完整性检查"
  - "代码注释强制覆盖"
  - "自动生成配置文档"
must_haves:
  truths:
    - "Documentation and implementation are consistent for release-critical flows."
    - "Commands and examples in docs are executable and current."
    - "Core code files are documented."
    - "Configuration schema docs are auto-generated and up-to-date."
    - "Step execution logic is modularized into independent files."
    - "Step modules are individually documented."
  artifacts:
    - path: "README.md"
      provides: "Updated, executable onboarding and integration setup guidance."
    - path: "docs/integration.schema.json"
      provides: "Auto-generated configuration schema documentation."
    - path: "core/api.py"
      provides: "Core Python API with complete documentation comments."
    - path: "core/steps/http_step.py"
      provides: "Independent module for HTTP steps."
  key_links:
    - from: "core/executor.py"
      to: "core/steps/"
      via: "step execution routing"
      pattern: "step"
---

<objective>
Merge Plan 14-03 into Plan 14-06: complete release-facing documentation and code integrity verification, enforce core comments, and refactor Step execution logic into independent, well-documented modules.
</objective>

<context>
@.planning/phases/14-v1.0-pre-release-hardening/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Documentation-to-Code Consistency</name>
  <files>README.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/TESTING.md</files>
  <action>核验文档中命令、配置和流程说明与当前代码一致，清理过期描述和失效链接。不要盲目覆盖，注意保留对历史决定的记录。</action>
  <verify>
    <automated>! rg -n "TBD" README.md .planning/codebase</automated>
  </verify>
  <done>关键文档一致性问题已关闭，旧名称已被替换。</done>
</task>

<task type="auto">
  <name>Task 2: Run Schema-to-Docs Generation</name>
  <files>docs/</files>
  <action>通过脚本提取 schema，自动生成集成配置项相关的文档。确保在每次配置项修改后文档保持同步。</action>
  <verify>
    <automated>test -f docs/integration.schema.json || ls docs/*.md || echo "MISSING"</automated>
  </verify>
  <done>配置文档已成功自动生成。</done>
</task>

<task type="auto">
  <name>Task 3: Enforce Core Code Comments</name>
  <files>core/api.py, ui-react/src/components/, core/steps/http_step.py, core/steps/browser_step.py</files>
  <action>补充核心代码注释：1) 核心 Python API；2) 关键 release-facing React 微组件；3) Step 模块注释。缩小范围，仅针对核心的、修改过的或直接面向发布的文件进行注释，不要求覆盖所有文件。</action>
  <verify>
    <automated>bash scripts/test_backend_core.sh || echo "MISSING"</automated>
  </verify>
  <done>核心 Python API、关键 React 组件和 Step 模块补充了清晰注释。</done>
</task>

<task type="auto">
  <name>Task 4: Extract Step Modules</name>
  <files>core/steps/__init__.py, core/steps/http_step.py, core/steps/browser_step.py, core/executor.py</files>
  <action>将原本集中在 core/executor.py 等位置的 Step 执行逻辑拆分为独立的模块（如 HTTP 请求、Browser 操作等），每个 Step 类型对应 core/steps/ 下的一个文件。</action>
  <verify>
    <automated>pytest tests/ -k "executor or step" -q || echo "MISSING"</automated>
  </verify>
  <done>Step 逻辑被拆分到独立文件，执行流依然正常。</done>
</task>

<task type="auto">
  <name>Task 5: Document Step Modules</name>
  <files>core/steps/http_step.py, core/steps/browser_step.py</files>
  <action>在每个独立的 Step 模块中补充详细的文档级注释，说明该 Step 的作用、所需参数 schema 以及返回值结构。</action>
  <verify>
    <automated>pytest tests/ -k "executor or step" -q || echo "MISSING"</automated>
  </verify>
  <done>所有拆分后的 Step 模块都包含了完整的文档注释。</done>
</task>

<task type="auto">
  <name>Task 6: Run Release-Facing Integrity Checks</name>
  <files>README.md, docs/, core/, ui-react/</files>
  <action>执行最小可用检查：关键测试入口、lint/typecheck、主要启动命令可运行并与文档一致。</action>
  <verify>
    <automated>bash scripts/test_backend_core.sh && bash scripts/test_frontend_core.sh || echo "MISSING"</automated>
  </verify>
  <done>文档声明的质量门禁可被实际执行。</done>
</task>

</tasks>

<success_criteria>
文档是“可执行说明书”，核心代码拥有清晰注释，Step 逻辑完成模块化拆分并保持可维护性。
</success_criteria>
