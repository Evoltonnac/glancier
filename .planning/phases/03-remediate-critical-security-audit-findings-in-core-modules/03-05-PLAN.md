---
phase: 03-remediate-critical-security-audit-findings-in-core-modules
plan: 05
type: execute
wave: 4
depends_on: [02, 03, 04]
files_modified: [core/settings_manager.py, core/api.py, core/executor.py, core/steps/script_step.py, tests/core/test_script_step.py, tests/api/test_settings_api.py, ui-react/src/api/client.ts, ui-react/src/pages/Settings.tsx, ui-react/src/i18n/messages/en.ts, ui-react/src/i18n/messages/zh.ts, docs/flow/02_step_reference.md]
autonomous: true
requirements: [SEC-03, INT-02]
user_setup: []
must_haves:
  truths:
    - "Script sandbox remains opt-in and off by default to preserve compatibility with existing integrations."
    - "Advanced Settings exposes sandbox toggle with explicit Beta labeling and persisted backend ownership."
    - "Script step enforces a default timeout even when sandbox is disabled."
  artifacts:
    - path: "core/steps/script_step.py"
      provides: "Lightweight high-risk capability restrictions and deterministic script timeout handling."
    - path: "core/settings_manager.py"
      provides: "Persisted system settings for script sandbox toggle and timeout defaults."
    - path: "ui-react/src/pages/Settings.tsx"
      provides: "Advanced settings controls and Beta labeling for script sandbox behavior."
  key_links:
    - from: "core/settings_manager.py"
      to: "ui-react/src/pages/Settings.tsx"
      via: "/api/settings contract for script_sandbox_enabled + script_timeout_seconds"
---

<objective>
Add a compatibility-first lightweight script sandbox as an opt-in Beta feature and enforce default script timeout limits.

Purpose: Reduce high-risk script execution blast radius without breaking existing config-first integrations by default.
Output: Backend-owned advanced settings for sandbox/timeouts, guarded script execution path, and regression coverage.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-CONTEXT.md
@.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-RESEARCH.md
@core/settings_manager.py
@core/api.py
@core/executor.py
@core/steps/script_step.py
@ui-react/src/pages/Settings.tsx
@ui-react/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend-owned script sandbox and timeout settings contract</name>
  <files>core/settings_manager.py, core/api.py, ui-react/src/api/client.ts, tests/api/test_settings_api.py</files>
  <read_first>core/settings_manager.py, core/api.py, ui-react/src/api/client.ts, tests/api/test_settings_api.py</read_first>
  <action>Extend `SystemSettings` with `script_sandbox_enabled` (default `false`) and `script_timeout_seconds` (default `10`, bounded to a safe range such as 1..120). Ensure `/api/settings` GET/PUT round-trips both fields, keeps backward compatibility for older settings files, and preserves backend ownership of persisted language/security preferences.</action>
  <verify>pytest tests/api/test_settings_api.py -k "script_sandbox or script_timeout"</verify>
  <acceptance_criteria>
    - core/settings_manager.py defines `script_sandbox_enabled: bool = False`.
    - core/settings_manager.py defines `script_timeout_seconds` with default 10 seconds.
    - tests/api/test_settings_api.py verifies default + persisted update behavior for both fields.
  </acceptance_criteria>
  <done>Settings API cleanly owns and persists sandbox/timeout controls with deterministic defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lightweight sandbox restrictions and default timeout in script step</name>
  <files>core/steps/script_step.py, core/executor.py, tests/core/test_script_step.py</files>
  <read_first>core/steps/script_step.py, core/executor.py, tests/core/test_executor_errors.py</read_first>
  <action>In script execution path, always apply timeout using `script_timeout_seconds` (fall back to default 10s if settings unavailable). When `script_sandbox_enabled` is true, enable lightweight restrictions that block high-risk capabilities (for example `open`, `exec`, `eval`, dangerous imports like `os`, `subprocess`, `socket`, `pathlib`) while allowing common transformation imports required by existing integrations (`re`, `datetime`, basic math/collections). Return deterministic runtime errors such as `script_timeout_exceeded` and `script_sandbox_blocked` without logging raw script source.</action>
  <verify>pytest tests/core/test_script_step.py tests/core/test_executor_errors.py -k "timeout or sandbox or stream"</verify>
  <acceptance_criteria>
    - script execution times out deterministically with `script_timeout_exceeded`.
    - sandbox-off mode preserves current integration behavior for existing script samples.
    - sandbox-on mode rejects blocked capabilities with `script_sandbox_blocked`.
  </acceptance_criteria>
  <done>Script execution has bounded runtime and optional risk reduction controls with compatibility-first defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Advanced Beta sandbox controls in Settings UI with i18n parity</name>
  <files>ui-react/src/pages/Settings.tsx, ui-react/src/i18n/messages/en.ts, ui-react/src/i18n/messages/zh.ts</files>
  <read_first>ui-react/src/pages/Settings.tsx, ui-react/src/i18n/messages/en.ts, ui-react/src/i18n/messages/zh.ts, AGENTS.md</read_first>
  <action>Add an Advanced section control for `script_sandbox_enabled` with explicit Beta label and concise risk/compatibility note. Expose `script_timeout_seconds` input in the same section with default 10-second hint. Add matching translation keys in both `en.ts` and `zh.ts`, and do not introduce hardcoded UI strings.</action>
  <verify>npm --prefix ui-react run test -- Settings && make test-typecheck</verify>
  <acceptance_criteria>
    - Settings advanced tab includes script sandbox toggle labeled as Beta.
    - UI persists values through `/api/settings` and reloads them on open.
    - New translation keys exist in both en/zh files with identical key names.
  </acceptance_criteria>
  <done>Users can explicitly opt into lightweight sandbox behavior and manage script timeout via backend-backed settings.</done>
</task>

<task type="auto">
  <name>Task 4: Document behavior contract and add regression coverage for concurrent script runs</name>
  <files>docs/flow/02_step_reference.md, tests/core/test_script_step.py, tests/core/test_executor_concurrency.py</files>
  <read_first>docs/flow/02_step_reference.md, tests/core/test_executor_concurrency.py, core/steps/script_step.py</read_first>
  <action>Update script-step documentation to describe: default timeout behavior, optional Beta sandbox mode, and compatibility caveats. Add/extend tests to verify multiple flows can still run under global executor concurrency while script timeout/sandbox checks remain deterministic and isolated per source execution.</action>
  <verify>pytest tests/core/test_script_step.py tests/core/test_executor_concurrency.py -k "script or timeout or sandbox" && make test-impacted</verify>
  <acceptance_criteria>
    - docs/flow/02_step_reference.md documents default script timeout and opt-in Beta sandbox mode.
    - concurrency regression confirms no cross-source state bleed when multiple script steps run.
    - make test-impacted exits 0 after changes.
  </acceptance_criteria>
  <done>Behavior is documented and protected by deterministic regression checks under concurrent flow execution.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/api/test_settings_api.py -k "script_sandbox or script_timeout"
- [ ] pytest tests/core/test_script_step.py tests/core/test_executor_errors.py -k "timeout or sandbox or stream"
- [ ] make test-impacted
- [ ] make test-backend
- [ ] make test-frontend
- [ ] make test-typecheck
</verification>

<success_criteria>
- Optional lightweight sandbox is available and clearly marked Beta in Advanced Settings.
- Sandbox remains disabled by default; existing integrations are not regressed by default behavior.
- Script step has default timeout enforcement with deterministic timeout error behavior.
- Backend and frontend settings contracts are covered by automated regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remediate-critical-security-audit-findings-in-core-modules/03-05-SUMMARY.md`
</output>
