---
phase: 04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [core/api.py, tests/core/test_scraper_internal_api.py, ui-react/src/hooks/useScraper.ts, ui-react/src/hooks/useScraper.test.ts]
autonomous: true
requirements: [PH4-03, PH4-04]
user_setup: []
must_haves:
  truths:
    - "Internal scraper fail callbacks classify manual-required auth walls separately from uncertain runtime failures."
    - "Manual-required WebView interactions stay manual-only and do not carry automatic foreground forcing metadata."
    - "Frontend enters foreground mode only when user explicitly requests it."
  artifacts:
    - path: "core/api.py"
      provides: "Internal scraper fail classification and deterministic error_code/state updates."
    - path: "ui-react/src/hooks/useScraper.ts"
      provides: "Observer/manual control logic that avoids default foreground forcing."
    - path: "tests/core/test_scraper_internal_api.py"
      provides: "Internal API regression coverage for manual-required vs retryable uncertain outcomes."
  key_links:
    - from: "core/api.py"
      to: "core/executor.py"
      via: "_update_state(status, interaction, error_code) with deterministic fail classification."
    - from: "ui-react/src/hooks/useScraper.ts"
      to: "core/api.py /refresh/{source_id}"
      via: "non-foreground retry path requests backend refresh instead of direct foreground queue push."
---

<objective>
Apply Phase 4 fallback policy to internal scraper fail callbacks and frontend observer/manual controls.

Purpose: Keep backend as workflow owner while ensuring uncertain failures remain retryable and foreground transitions are explicit user actions only.
Output: Updated internal fail classification, updated `useScraper` control path, and regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-RESEARCH.md
@core/api.py
@core/source_state.py
@tests/core/test_scraper_internal_api.py
@ui-react/src/hooks/useScraper.ts
@ui-react/src/hooks/useScraper.test.ts

<interfaces>
From core/api.py:
```python
def _build_webview_interaction_from_task(task: dict[str, Any], message: str) -> InteractionRequest
@router.post("/internal/scraper/fail")
async def internal_fail_scraper_task(payload: ScraperTaskFailRequest, request: Request) -> dict[str, Any]
```

From ui-react/src/hooks/useScraper.ts:
```typescript
const handlePushToQueue = useCallback(
  (source: SourceSummary, options?: { foreground?: boolean }): boolean => { ... }
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify internal scraper fail callbacks into manual-required vs retryable-uncertain contracts</name>
  <files>core/api.py, tests/core/test_scraper_internal_api.py</files>
  <read_first>core/api.py, core/source_state.py, tests/core/test_scraper_internal_api.py</read_first>
  <behavior>
    - Test 1: Auth-wall/captcha/login-like fail reasons keep `status=suspended` + `interaction.type=webview_scrape` + `manual_only=true`.
    - Test 2: Transient/internal uncertain fail reasons set retryable deterministic code (`runtime.retry_required`) without foreground forcing payload.
    - Test 3: Idempotent fail callbacks preserve existing accepted/idempotent response semantics.
  </behavior>
  <action>Refactor `/api/internal/scraper/fail` to use an explicit error classification helper keyed by fail reason text. For auth-wall/manual signals, keep suspended `webview_scrape` interaction but remove default `force_foreground` metadata; keep `manual_only=true`. For uncertain/transient signals (for example non-auth navigation/driver/runtime failures), write `SourceStatus.ERROR` with deterministic retry code `runtime.retry_required` and no manual interaction payload. Update tests to cover both paths and assert deterministic status/error_code behavior.</action>
  <verify>
    <automated>python -m pytest tests/core/test_scraper_internal_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Internal fail path no longer writes `force_foreground` by default.
    - Manual-required failures still produce `webview_scrape` suspended interaction with `manual_only=true`.
    - Uncertain failures produce deterministic retryable code for scheduler policy consumption.
  </acceptance_criteria>
  <done>Internal scraper fail endpoint emits deterministic, policy-aligned state/error contracts without implicit foreground forcing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ensure frontend scraper controls only use foreground mode on explicit user intent</name>
  <files>ui-react/src/hooks/useScraper.ts, ui-react/src/hooks/useScraper.test.ts</files>
  <read_first>ui-react/src/hooks/useScraper.ts, ui-react/src/hooks/useScraper.test.ts, ui-react/src/components/auth/FlowHandler.tsx</read_first>
  <behavior>
    - Test 1: `handlePushToQueue(source)` without `{ foreground: true }` routes to backend `api.refreshSource(source.id)`.
    - Test 2: Presence of legacy `interaction.data.force_foreground` does not force foreground behavior.
    - Test 3: Explicit manual action (`{ foreground: true }`) still promotes/pushes foreground scraper task.
  </behavior>
  <action>Update `useScraper` so foreground mode is controlled only by explicit `options.foreground`. Remove fallback logic that reads `source.interaction.data.force_foreground`. Keep manual foreground button behavior unchanged through `FlowHandler` (it passes `{ foreground: true }`). Adjust auth-required listener payload shaping to avoid writing `force_foreground` while preserving `manual_only` and `blocked_target_url`. Extend hook tests to lock non-foreground retry behavior and explicit-foreground behavior.</action>
  <verify>
    <automated>npm --prefix ui-react run test -- --run src/hooks/useScraper.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Non-foreground queue action uses backend refresh path by default.
    - Legacy `force_foreground` payload values no longer alter automatic behavior.
    - Explicit user-triggered foreground action remains functional.
  </acceptance_criteria>
  <done>Frontend observer/manual controls are explicit-intent driven and compatible with backend-owned retry orchestration.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_scraper_internal_api.py -q
- [ ] npm --prefix ui-react run test -- --run src/hooks/useScraper.test.ts
- [ ] make test-frontend
- [ ] make test-typecheck
</verification>

<success_criteria>
- Internal fail callback behavior cleanly separates manual-required and retryable-uncertain outcomes.
- Frontend no longer inherits implicit foreground-forcing defaults.
- Automated backend/frontend regressions verify policy and interaction contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-02-SUMMARY.md`
</output>
