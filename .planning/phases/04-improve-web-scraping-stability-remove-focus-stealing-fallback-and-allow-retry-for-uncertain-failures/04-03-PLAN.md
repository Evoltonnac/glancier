---
phase: 04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: [ui-react/src-tauri/src/scraper.rs, docs/webview-scraper/02_runtime_and_fallback.md, docs/flow/04_step_failure_test_inputs.md]
autonomous: false
requirements: [PH4-05]
user_setup: []
must_haves:
  truths:
    - "Automatic auth-required fallback no longer steals focus from the user workspace."
    - "User-triggered manual foreground actions still open/focus the scraper window when explicitly requested."
    - "Runtime/fallback documentation reflects no-auto-focus behavior and retry policy details."
  artifacts:
    - path: "ui-react/src-tauri/src/scraper.rs"
      provides: "Auth-required runtime path that emits manual-required signal without implicit show/focus."
    - path: "docs/webview-scraper/02_runtime_and_fallback.md"
      provides: "Updated fallback contract including no-auto-focus and explicit manual foreground behavior."
    - path: "docs/flow/04_step_failure_test_inputs.md"
      provides: "Failure expectation updates for manual_only contract and retryable uncertain failures."
  key_links:
    - from: "ui-react/src-tauri/src/scraper.rs"
      to: "ui-react/src/hooks/useScraper.ts"
      via: "`scraper_auth_required` event informs UI; user action drives foreground open."
    - from: "ui-react/src-tauri/src/scraper.rs"
      to: "ui-react/src-tauri/src/lib.rs"
      via: "`show_scraper_window` remains explicit focus path initiated from main window command."
---

<objective>
Remove automatic focus-stealing behavior in Rust scraper auth-required fallback while preserving explicit user-controlled foreground entry and synchronizing docs.

Purpose: Guarantee hidden/auxiliary mode stability and user-focus safety without regressing manual recovery capability.
Output: Rust fallback updates, docs updates, and explicit manual verification checkpoint.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ui-react/src-tauri/src/scraper.rs
@ui-react/src-tauri/src/lib.rs
@docs/webview-scraper/02_runtime_and_fallback.md
@docs/flow/04_step_failure_test_inputs.md

<interfaces>
From ui-react/src-tauri/src/scraper.rs:
```rust
pub async fn handle_scraper_auth(
    window: Window,
    app: AppHandle,
    source_id: String,
    task_id: Option<String>,
    target_url: String,
) -> Result<(), String>

pub async fn show_scraper_window(window: Window, app: AppHandle) -> Result<(), String>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove implicit show/focus from Rust auth-required fallback path</name>
  <files>ui-react/src-tauri/src/scraper.rs</files>
  <read_first>ui-react/src-tauri/src/scraper.rs, ui-react/src-tauri/src/lib.rs</read_first>
  <behavior>
    - Test 1: `handle_scraper_auth` emits auth-required signal and backend fail callback without calling implicit `show`/`set_focus`.
    - Test 2: Explicit `show_scraper_window` and explicit foreground task creation still call `show`/`set_focus`.
  </behavior>
  <action>In `handle_scraper_auth`, remove automatic window reveal/focus operations (`show`, `set_focus`, forced resize/center) from fallback handling. Keep event emission (`scraper_auth_required`) and backend fail callback intact so state remains synchronized. Preserve explicit foreground behavior in user-triggered paths (`show_scraper_window`, foreground task start) with no behavioral changes. Update lifecycle log messaging to reflect pending manual action without auto focus.</action>
  <verify>
    <automated>cargo test --manifest-path ui-react/src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Auth-required fallback no longer auto-focuses or auto-shows scraper window.
    - Explicit user commands still open/focus window as before.
    - Rust build/tests pass for `ui-react/src-tauri`.
  </acceptance_criteria>
  <done>Rust runtime no longer steals focus during automatic fallback, while explicit manual foreground controls remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize runtime fallback documentation with Phase 4 behavior contract</name>
  <files>docs/webview-scraper/02_runtime_and_fallback.md, docs/flow/04_step_failure_test_inputs.md</files>
  <read_first>docs/webview-scraper/02_runtime_and_fallback.md, docs/flow/04_step_failure_test_inputs.md, .planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-RESEARCH.md</read_first>
  <action>Update docs to codify: automatic auth-required fallback does not auto-show/focus windows; manual foreground opening is explicit user action; uncertain failures use retryable deterministic handling with bounded scheduler retries (`3` attempts, backoff `60s/180s/600s`); manual-required payload contract uses `manual_only=true` without implicit `force_foreground` default.</action>
  <verify>
    <automated>rg -n \"manual_only|force_foreground|retry\" docs/webview-scraper/02_runtime_and_fallback.md docs/flow/04_step_failure_test_inputs.md</automated>
  </verify>
  <acceptance_criteria>
    - Docs no longer describe `force_foreground=true` as required fallback payload default.
    - Docs describe explicit manual foreground trigger and no-auto-focus fallback.
    - Retry budget values are documented as `3` attempts with `60/180/600` second backoff.
  </acceptance_criteria>
  <done>Documentation matches implemented fallback and retry policy, preventing contract drift.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify no focus stealing in hidden/auxiliary runtime conditions</name>
  <files>ui-react/src-tauri/src/scraper.rs, docs/webview-scraper/02_runtime_and_fallback.md</files>
  <read_first>docs/webview-scraper/02_runtime_and_fallback.md, ui-react/src-tauri/src/scraper.rs</read_first>
  <action>Run the 5-step manual verification sequence exactly as listed below after Task 1 and Task 2 are merged. Capture pass/fail notes for each step in the plan summary, including observed `source_id`, `status`, and `error_code` for the tested source.</action>
  <verify>Manual checkpoint: complete all 5 verification steps and provide explicit approval/resolution notes.</verify>
  <what-built>Rust auth-required fallback no longer auto-shows/focuses windows; manual window focus is explicit user action.</what-built>
  <how-to-verify>
    1. Launch desktop app and ensure at least one source can trigger webview auth-required path (for example expired session/captcha scenario).
    2. Keep main app window minimized or occluded, then trigger the failing source refresh.
    3. Confirm no scraper window is auto-focused/raised during auth-required fallback.
    4. Open manual recovery via UI (Needs Fix -> manual foreground action) and confirm scraper window opens in foreground.
    5. Confirm source still surfaces deterministic `error_code`/status for the failure in Dashboard state.
  </how-to-verify>
  <resume-signal>Type "approved" when all 5 checks pass, or provide failure notes with exact observed behavior.</resume-signal>
  <acceptance_criteria>
    - Automatic fallback path does not steal focus.
    - Explicit manual action still brings foreground window.
    - Dashboard diagnostics remain available after fallback.
  </acceptance_criteria>
  <done>Manual UAT confirms non-focus fallback behavior and explicit foreground recovery still function correctly.</done>
</task>

</tasks>

<verification>
- [ ] cargo test --manifest-path ui-react/src-tauri/Cargo.toml
- [ ] make test-impacted
- [ ] Human checkpoint completed with explicit approval
</verification>

<success_criteria>
- Focus is never stolen by automatic scraper auth-required fallback.
- Explicit user-driven manual foreground flow remains functional.
- Runtime/fallback docs and failure test-input contracts match implemented behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-improve-web-scraping-stability-remove-focus-stealing-fallback-and-allow-retry-for-uncertain-failures/04-03-SUMMARY.md`
</output>
