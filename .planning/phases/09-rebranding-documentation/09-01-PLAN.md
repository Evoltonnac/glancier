---
phase: 09-rebranding-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/terminology.md
  - Agent.md
autonomous: true
requirements:
  - "确立新的术语标准并体现在文档中"
must_haves:
  truths:
    - "New Glancier terminology is explicitly defined in a central dictionary."
    - "AI Assistant instructions (Agent.md) enforce the new mental model."
  artifacts:
    - path: docs/terminology.md
      provides: "Central terminology dictionary for the rebranding transition."
    - path: Agent.md
      provides: "AI context reflecting the Glancier identity."
  key_links:
    - from: Agent.md
      to: docs/terminology.md
      via: "Reference to the terminology dictionary for definitions."
      pattern: "terminology\.md"
---

<objective>
Establish the "Glancier" terminology definitions and enforce them in the AI Agent instructions.

Purpose: This creates the semantic foundation ("interface") for all subsequent documentation updates. It prevents context switching and confusion by explicitly mapping old "Quota" terms to the new "Metric/Signal/Integration Data" concepts.
Output: A new `docs/terminology.md` file and an updated `Agent.md`.
</objective>

<context>
@.planning/phases/09-rebranding-documentation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Terminology Dictionary</name>
  <files>docs/terminology.md</files>
  <action>
    Create a new file `docs/terminology.md`.
    Extract the "Supporting Terminology Dictionary" from `09-RESEARCH.md` and define the target mental model for Glancier (Personal Data Aggregator & Hub).
    Explicitly document the deprecation of "Quota" as a generic catch-all term and define context-specific replacements:
    - Quota Board -> Glancier
    - Quota -> Metric (numeric), Signal (boolean/status), Integration Data (generic)
    - Quota Bar -> Progress Bar
    - Quota Card -> Base Source Card / Bento Card
    - QUOTA_BOARD_ROOT -> GLANCIER_DATA_DIR
  </action>
  <verify>
    <automated>grep -q "Integration Data" docs/terminology.md</automated>
  </verify>
  <done>The file is created with clear mappings from old terms to new terms.</done>
</task>

<task type="auto">
  <name>Task 2: Update AI Assistant Instructions</name>
  <files>Agent.md</files>
  <action>
    Update `Agent.md` to reflect the new Glancier positioning.
    Remove outdated references that restrict the project to "Quota Board" or strict quota monitoring.
    Add a directive for the AI to consult `docs/terminology.md` when generating new content or code, ensuring it uses the new namespace (e.g., Metric, Signal) instead of "Quota" unless specifically discussing an API limit.
  </action>
  <verify>
    <automated>grep -q "Glancier" Agent.md</automated>
  </verify>
  <done>Agent.md explicitly mentions Glancier and enforces the new terminology.</done>
</task>

</tasks>

<verification>
Verify that `docs/terminology.md` exists and `Agent.md` uses the new branding.
</verification>

<success_criteria>
Terminology is established and AI rules are updated without touching source code.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rebranding-documentation/09-01-SUMMARY.md`
</output>
