---
phase: 09-rebranding-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - CONFIG.md
autonomous: true
requirements:
  - "重写架构说明、PROJECT.md 和 README.md"
must_haves:
  truths:
    - "System architecture reflects a generic 'Flow -> Bento Grid' pipeline instead of a quota-specific pipeline."
    - "Configuration documentation outlines the deprecation and transition plan for legacy `quota_bar` and `quota_card` identifiers."
  artifacts:
    - path: .planning/codebase/ARCHITECTURE.md
      provides: "Updated architecture description aligned with the new Glancier mental model."
    - path: CONFIG.md
      provides: "Configuration reference incorporating new widget naming conventions."
  key_links:
    - from: CONFIG.md
      to: docs/terminology.md
      via: "Explanation of deprecation for old widget names in favor of new terminology."
      pattern: "(progress_bar|metric_bar)"
---

<objective>
Update system architecture and configuration semantic descriptions.

Purpose: Architecture docs must represent the actual capability of the system (a universal data aggregator with a Bento Grid UI) rather than its original narrower purpose. The configuration docs need to communicate the transition path for users from legacy identifier names (e.g., `quota_bar`) to new names (e.g., `progress_bar`).
Output: Scrubbed and rewritten `.planning/codebase/ARCHITECTURE.md` and `CONFIG.md`.
</objective>

<context>
@.planning/phases/09-rebranding-documentation/09-RESEARCH.md
@docs/terminology.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ARCHITECTURE.md Semantics</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <action>
    Scrub and update `.planning/codebase/ARCHITECTURE.md`.
    Replace mentions of the "Quota monitoring pipeline" with the new "Flow -> Bento Grid" pipeline.
    Ensure that descriptions of data fetching, state management, and rendering use the new Glancier terminology (Metric, Signal, Integration Data) rather than "Quota".
    Update the overall narrative to present a flexible Personal Data Aggregator architecture.
  </action>
  <verify>
    <automated>grep -q "Bento Grid" .planning/codebase/ARCHITECTURE.md</automated>
  </verify>
  <done>Architecture documentation fully aligns with the new mental model.</done>
</task>

<task type="auto">
  <name>Task 2: Update CONFIG.md to Include Deprecation Plan</name>
  <files>CONFIG.md</files>
  <action>
    Update `CONFIG.md`.
    Add a section explaining the transition plan from old widget types (`quota_bar`, `quota_card`) to the new equivalents (`progress_bar`, `base_source_card`/`bento_card`).
    Clarify that while the current documentation uses the new terms, backward compatibility is maintained in the v1.0 codebase for existing user configurations.
    Scrub the rest of the document to use "Metric", "Signal", and "Integration Data" where appropriate.
  </action>
  <verify>
    <automated>grep -q "progress_bar" CONFIG.md</automated>
  </verify>
  <done>Configuration documentation explains the terminology shift and deprecation strategy clearly.</done>
</task>

</tasks>

<verification>
Check that both files are properly aligned with the terminology in `docs/terminology.md`.
</verification>

<success_criteria>
Architectural intent and configuration syntax reflect the Phase 09 rebranding.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rebranding-documentation/09-03-SUMMARY.md`
</output>
