---
phase: 08-ui-refactoring
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["ui-react/src/App.tsx", "ui-react/src/pages/Integrations.tsx", "ui-react/src/pages/Settings.tsx"]
autonomous: true
requirements: ["REQ-UI-REF-003"]
must_haves:
  truths:
    - "Sidebar cards use soft-elevation hover and semantic styling"
    - "Buttons and interactive elements use contrast inversion on hover"
    - "Dashboard grid enforces 12px gap and falls back to single-column on narrow screens"
    - "Pages align with High-Density Minimalist spec"
  artifacts:
    - path: "ui-react/src/App.tsx"
      provides: "Dashboard Layout and Sidebar UI"
    - path: "ui-react/src/pages/Integrations.tsx"
      provides: "Integrations Page UI"
    - path: "ui-react/src/pages/Settings.tsx"
      provides: "Settings Page UI"
---

<objective>
Refactor main pages and layout structures.

Purpose: To apply the high-density minimalist design, soft-elevation hover states, and proper responsive behaviors to the overall application layout (App.tsx), Integrations page, and Settings page.
Output: Modernized `App.tsx`, `Integrations.tsx`, and `Settings.tsx`.
</objective>

<execution_context>
@/Users/xingminghua/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-refactoring/08-CONTEXT.md
@.planning/phases/08-ui-refactoring/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize Dashboard Layout and Sidebar (App.tsx)</name>
  <files>ui-react/src/App.tsx</files>
  <action>
    - Ensure the Dashboard grid uses the 12px margin setting and gracefully falls back to a single column on narrow screens.
    - Refactor the left sidebar cards to use `hover:shadow-soft-elevation` and transition effects.
    - Replace text-heavy buttons in the sidebar with icon-only buttons wrapped in Tooltips.
    - Ensure focus states (`focus-visible:ring-brand/50`) are explicit on interactive elements.
  </action>
  <verify>
    <automated>grep -q "shadow-soft-elevation" ui-react/src/App.tsx</automated>
  </verify>
  <done>App.tsx layout is modernized and responsive.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Integrations Page UI</name>
  <files>ui-react/src/pages/Integrations.tsx</files>
  <action>
    - Redesign the integrations list and editor layout to follow the high-density guidelines.
    - Apply contrast inversion hover states (`hover:bg-foreground hover:text-background`) to relevant action buttons.
    - Use `shadow-soft-elevation` on source cards within the integrations view.
    - Standardize focus rings (`ring-2 ring-brand/50`).
  </action>
  <verify>
    <automated>grep -q "shadow-soft-elevation" ui-react/src/pages/Integrations.tsx</automated>
  </verify>
  <done>Integrations view matches the new design system.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Settings Page UI</name>
  <files>ui-react/src/pages/Settings.tsx</files>
  <action>
    - Redesign settings panels and form fields to be compact and minimal.
    - Ensure toggles and buttons have the correct semantic brand colors and focus states.
    - Use soft elevation on interactive container cards.
  </action>
  <verify>
    <automated>grep -q "focus-visible" ui-react/src/pages/Settings.tsx</automated>
  </verify>
  <done>Settings view is consistent with high-density design.</done>
</task>

</tasks>

<verification>
Run `npm run build` in `ui-react` to ensure TypeScript compilation succeeds.
</verification>

<success_criteria>
Main pages and layout shells use the unified design tokens, proper hover/focus states, and provide a high-density, consistent user experience.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-refactoring/08-03-SUMMARY.md`
</output>
