---
phase: 09-sql-runtime-and-integration-data-normalization
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - requirements.txt
  - core/sql/runtime_adapters.py
  - core/steps/sql_step.py
  - tests/core/test_sql_step.py
  - tests/core/test_sql_step_connectors.py
autonomous: true
requirements:
  - SQL-03
  - DATA-03
must_haves:
  truths:
    - "User can execute one `use: sql` contract model using `connector.profile=sqlite` and `connector.profile=postgresql`."
    - "User receives SQL execution metadata (`row_count`, `duration_ms`, `truncated`) in the same normalized envelope for both connectors."
    - "Unsupported connector profiles fail deterministically with `runtime.sql_connect_failed`."
  artifacts:
    - path: "core/sql/runtime_adapters.py"
      provides: "Connector adapter dispatch and raw query execution helpers for sqlite and postgresql."
    - path: "core/steps/sql_step.py"
      provides: "Profile dispatch + trust gate + normalized sql_response envelope wiring."
    - path: "tests/core/test_sql_step_connectors.py"
      provides: "Regression coverage for sqlite/postgresql profile parity and unsupported-profile failures."
  key_links:
    - from: "core/steps/sql_step.py"
      to: "core/sql/runtime_adapters.py"
      via: "adapter dispatch by args.connector.profile"
      pattern: "profile in {\"sqlite\", \"postgresql\"}"
    - from: "core/steps/sql_step.py"
      to: "core/sql/normalization.py"
      via: "build_normalized_sql_response"
      pattern: "build_normalized_sql_response"
---

<objective>
Integrate connector runtime parity so SQL execution supports `sqlite` and `postgresql` under one contract and always emits the canonical normalized SQL envelope with compatibility aliases.

Purpose: Deliver SQL-03 and DATA-03 while preserving trust-gate/error-code stability from Phase 8.
Output: Connector adapters, SQL step runtime integration, and connector parity regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sql-runtime-and-integration-data-normalization/09-CONTEXT.md
@.planning/phases/09-sql-runtime-and-integration-data-normalization/09-RESEARCH.md
@.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-02-SUMMARY.md
@core/steps/sql_step.py
@core/sql/contracts.py
@core/settings_manager.py
@tests/core/test_sql_step.py

<interfaces>
From current core/steps/sql_step.py (pre-Phase 9):
```python
profile = _normalize_connector_profile(args)
if profile != "sqlite":
    raise SqlConnectFailedError(..., code="runtime.sql_connect_failed")
```

From expected Phase 9 envelope constraints (09-CONTEXT.md):
```text
rows, fields, row_count, duration_ms, truncated,
statement_count, statement_types, is_high_risk, risk_reasons,
timeout/max_rows metadata, plus compatibility aliases columns/execution_ms
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SQL connector adapters for sqlite and postgresql profiles</name>
  <files>requirements.txt, core/sql/runtime_adapters.py</files>
  <read_first>
    requirements.txt
    core/steps/sql_step.py
    .planning/phases/09-sql-runtime-and-integration-data-normalization/09-RESEARCH.md
    .planning/phases/09-sql-runtime-and-integration-data-normalization/09-CONTEXT.md
  </read_first>
  <action>
    Add PostgreSQL driver dependency line to `requirements.txt` exactly as:
    `psycopg[binary]>=3.3.3,<3.4`

    Create `core/sql/runtime_adapters.py` with adapter functions:
    - `run_sqlite_query(...)`
    - `run_postgresql_query(...)`
    - `run_sql_query_for_profile(profile: str, ...)`

    `run_sql_query_for_profile` must accept only `sqlite` and `postgresql` and return a normalized raw payload dictionary containing:
    - `columns` (ordered list of SQL projection names)
    - `rows` (list of row dicts)
    - `has_more_rows` (boolean)
    - `db_type_hints` (map of column name to connector-native type hint string)

    PostgreSQL credential resolution rules (in this order):
    1. `credentials.dsn` if non-empty;
    2. keyword connect args from `credentials.host`, `credentials.port` (default `5432`), `credentials.database`, `credentials.user`, `credentials.password`, optional `credentials.sslmode`.

    Adapter module must not log or return credential values.
  </action>
  <verify>
    <automated>python -c "from core.sql.runtime_adapters import run_sqlite_query, run_postgresql_query, run_sql_query_for_profile; print('runtime-adapters-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "psycopg\\[binary\\]>=3\\.3\\.3,<3\\.4" requirements.txt` returns one match.
    - `rg -n "def run_sqlite_query\\(" core/sql/runtime_adapters.py` returns one match.
    - `rg -n "def run_postgresql_query\\(" core/sql/runtime_adapters.py` returns one match.
    - `rg -n "def run_sql_query_for_profile\\(" core/sql/runtime_adapters.py` returns one match.
    - `rg -n "\"postgresql\"" core/sql/runtime_adapters.py` returns at least one match in dispatch logic.
  </acceptance_criteria>
  <done>Connector adapter module exists with explicit sqlite/postgresql support and deterministic raw query payload contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SQL step runtime to adapter dispatch and canonical metadata envelope</name>
  <files>core/steps/sql_step.py</files>
  <read_first>
    core/steps/sql_step.py
    core/sql/normalization.py
    core/sql/runtime_adapters.py
    core/sql/contracts.py
    docs/flow/02_step_reference.md
  </read_first>
  <action>
    Refactor `execute_sql_step` to replace sqlite-only execution with profile-based adapter dispatch:
    - keep trust-gate and SQLGlot classification behavior unchanged;
    - call `run_sql_query_for_profile(profile=..., ...)` for runtime query execution;
    - keep unsupported profile failure code as `runtime.sql_connect_failed` with detail `Unsupported SQL connector profile: {profile_or_unknown}`.

    Use `build_sql_fields(...)` and `build_normalized_sql_response(...)` from `core/sql/normalization.py` so every successful execution returns:
    - canonical keys: `rows`, `fields`, `row_count`, `duration_ms`, `truncated`, `statement_count`, `statement_types`, `is_high_risk`, `risk_reasons`, `timeout_seconds`, `max_rows`
    - compatibility aliases: `columns`, `execution_ms`.

    Preserve deterministic SQL error mappings from Phase 8:
    - `runtime.sql_connect_failed`
    - `runtime.sql_auth_failed`
    - `runtime.sql_query_failed`
    - `runtime.sql_timeout`
    - `runtime.sql_risk_operation_requires_trust`
    - `runtime.sql_risk_operation_denied`

    Ensure runtime details remain sanitized via existing `sanitize_log_reason` path.
  </action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_step.py -q -k "sql_step and (duration_ms or truncated or statement_types or connect_and_auth_failures)"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"duration_ms\"" core/steps/sql_step.py` returns at least one match in `sql_response` construction.
    - `rg -n "\"truncated\"" core/steps/sql_step.py` returns at least one match in `sql_response` construction.
    - `rg -n "\"fields\"" core/steps/sql_step.py` returns at least one match in `sql_response` construction.
    - `rg -n "\"execution_ms\"" core/steps/sql_step.py` and `rg -n "\"columns\"" core/steps/sql_step.py` both return matches.
    - SQL trust-gate error code strings (`runtime.sql_risk_operation_requires_trust` and `runtime.sql_risk_operation_denied`) remain present in file.
  </acceptance_criteria>
  <done>SQL runtime emits one canonical-plus-alias envelope for connector profiles without changing trust-gate/error-code contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add connector parity regression coverage for sqlite/postgresql and unsupported profiles</name>
  <files>tests/core/test_sql_step_connectors.py, tests/core/test_sql_step.py</files>
  <read_first>
    tests/core/test_sql_step.py
    core/steps/sql_step.py
    core/sql/runtime_adapters.py
    .planning/phases/09-sql-runtime-and-integration-data-normalization/09-RESEARCH.md
  </read_first>
  <action>
    Create `tests/core/test_sql_step_connectors.py` with deterministic unit tests:
    - sqlite profile executes and returns `sql_response.fields` + `sql_response.duration_ms`;
    - postgresql profile executes through monkeypatched adapter (no real DB/network) and returns same canonical keys;
    - unsupported profile (`mysql`) raises code `runtime.sql_connect_failed`.

    Update `tests/core/test_sql_step.py` assertions to include canonical metadata key coverage:
    - `sql_response.duration_ms` exists and is `>= 0`;
    - `sql_response.truncated` exists and is boolean;
    - alias consistency assertion: `sql_response.execution_ms == sql_response.duration_ms`.
  </action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_step_connectors.py tests/core/test_sql_step.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_sql_step_connectors.py` exists and contains `runtime.sql_connect_failed` assertion for unsupported profile.
    - `rg -n "sql_response\\[\"fields\"\\]" tests/core/test_sql_step_connectors.py` returns at least one match.
    - `rg -n "sql_response\\[\"duration_ms\"\\]" tests/core/test_sql_step.py` returns at least one match.
    - Pytest command in `<verify>` passes with exit code 0.
  </acceptance_criteria>
  <done>SQL-03 and DATA-03 have passing regression coverage for sqlite/postgresql parity, unsupported profile failure, and metadata envelope stability.</done>
</task>

</tasks>

<verification>
Run connector/runtime subset:
- `python -m pytest tests/core/test_sql_step_connectors.py tests/core/test_sql_step.py -q`
</verification>

<success_criteria>
- `use: sql` runtime dispatch supports both `sqlite` and `postgresql` profiles through one contract model.
- Successful SQL responses include canonical metadata keys and compatibility aliases.
- Unsupported profile behavior remains deterministic with `runtime.sql_connect_failed`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-sql-runtime-and-integration-data-normalization/09-02-SUMMARY.md`
</output>
