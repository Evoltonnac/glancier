---
phase: 08-sql-step-contracts-and-safety-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - core/config_loader.py
  - scripts/generate_schemas.py
  - config/schemas/integration.python.schema.json
  - config/schemas/integration.schema.json
  - core/sql/contracts.py
  - tests/test_generate_schemas.py
  - tests/core/test_sql_contracts.py
autonomous: true
requirements: [SQL-01, SQL-02]
user_setup: []
must_haves:
  truths:
    - "`use: sql` is a first-class flow step contract with schema-validated connector/credential/query arguments."
    - "SQLGlot AST classification marks non-SELECT/high-risk SQL operations before execution paths run."
    - "Schema generation artifacts remain synchronized with StepType/args declarations."
  artifacts:
    - path: "core/sql/contracts.py"
      provides: "Deterministic SQL contract validation (AST risk classification over user-authored query text)."
    - path: "core/config_loader.py"
      provides: "StepType/sql args schema declaration wired into integration validation."
    - path: "tests/core/test_sql_contracts.py"
      provides: "Regression coverage for allowed/blocked SQL contract patterns."
  key_links:
    - from: "core/config_loader.py"
      to: "scripts/generate_schemas.py"
      via: "StepType + args schema declarations must generate StepConfig_sql variant definitions."
    - from: "core/sql/contracts.py"
      to: "core/steps/sql_step.py"
      via: "Runtime SQL step will rely on contract checks to enforce pre-execution safety."
---

<objective>
Build Phase 8 foundation: declare the `use: sql` contract and lock SQLGlot AST risk validation before runtime integration.

Purpose: deliver SQL-01/SQL-02 contract semantics with low blast radius.
Output: SQL step schema + shared safety validator + schema/contract regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-CONTEXT.md
@.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-RESEARCH.md
@core/config_loader.py
@scripts/generate_schemas.py
@tests/test_generate_schemas.py

<interfaces>
SQL step contract requirements (phase-8 baseline):
```text
use: sql
args include connector profile + credentials reference + fully user-authored query text
```

Validation invariants:
```text
- SQLGlot AST classifies SELECT vs high-risk operations
- high-risk operations route to trust authorization wall in runtime phase
- static analysis runs on final SQL text before execution
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `sql` step type and colocated schema declaration</name>
  <files>requirements.txt, core/config_loader.py, scripts/generate_schemas.py, config/schemas/integration.python.schema.json, config/schemas/integration.schema.json, tests/test_generate_schemas.py</files>
  <read_first>core/config_loader.py, scripts/generate_schemas.py, tests/test_generate_schemas.py</read_first>
  <behavior>
    - Test 1: `StepType` includes `sql` and `STEP_ARGS_SCHEMAS_BY_USE` declares `sql` args schema.
    - Test 2: Generated schema includes `StepConfig_sql` variant and keeps oneOf coverage consistent with all step types.
    - Test 3: SQLGlot dependency is declared for AST-based SQL risk classification.
  </behavior>
  <action>Introduce `StepType.SQL` and a deterministic SQL args schema shape in `core/config_loader.py`. Add SQLGlot dependency declaration for AST classification support. Regenerate integration schema artifacts and update schema tests to assert `StepConfig_sql` presence without regressing existing step variants.</action>
  <verify>
    <automated>python -m pytest tests/test_generate_schemas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_generate_schemas.py -q` exits 0.
    - `rg -n "StepType\.SQL|StepConfig_sql|\"sql\"" core/config_loader.py config/schemas/integration.python.schema.json` returns contract wiring.
    - Schema files are regenerated and committed with deterministic changes only.
    - `rg -n "sqlglot" requirements.txt` confirms AST parser dependency declaration.
  </acceptance_criteria>
  <done>`use: sql` is represented in runtime and generated schema contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SQL contract safety validator module</name>
  <files>core/sql/contracts.py, tests/core/test_sql_contracts.py</files>
  <read_first>.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-RESEARCH.md, db_step_design.md</read_first>
  <behavior>
    - Test 1: SELECT/CTE query patterns are classified as non-risk and pass contract checks.
    - Test 2: Mutation/DDL/multi-statement query patterns are classified as high-risk deterministically.
    - Test 3: Query validation fails deterministically on invalid/empty SQL text.
  </behavior>
  <action>Create a focused SQL contract module that uses SQLGlot AST to classify SQL operation risk on final user-authored SQL text before runtime execution. Keep validator pure/testable and return deterministic classification metadata consumed by runtime authorization-wall mapping in later plan.</action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_contracts.py -q</automated>
    <automated>python -m pytest tests/test_generate_schemas.py tests/core/test_sql_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Contract validator classifies high-risk SQL patterns deterministically.
    - `python -m pytest tests/test_generate_schemas.py tests/core/test_sql_contracts.py -q` exits 0.
    - `rg -n "sqlglot|risk|sql_invalid_contract" core/sql/contracts.py tests/core/test_sql_contracts.py` confirms stable semantics.
  </acceptance_criteria>
  <done>SQL contract and AST-based risk checks are codified before runtime wiring.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/test_generate_schemas.py tests/core/test_sql_contracts.py -q
- [ ] make test-backend
</verification>

<success_criteria>
- `use: sql` passes schema generation/validation flow as a first-class step type.
- AST risk classification and static query-text guardrails are enforced by shared contract validator.
- Foundation is ready for runtime execution integration (08-02).
</success_criteria>

<output>
After completion, create `.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-01-SUMMARY.md`
</output>
