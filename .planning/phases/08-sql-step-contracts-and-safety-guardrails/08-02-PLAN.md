---
phase: 08-sql-step-contracts-and-safety-guardrails
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - core/steps/sql_step.py
  - core/steps/__init__.py
  - core/executor.py
  - core/sql/contracts.py
  - core/log_redaction.py
  - tests/core/test_sql_step.py
  - tests/core/test_executor_errors.py
autonomous: true
requirements: [SQL-02, SQL-04, SQL-05, SQL-06]
user_setup: []
must_haves:
  truths:
    - "SQL step query text is fully user-authored (script-like control), and risk checks run on final SQL text before execution."
    - "Non-SELECT/high-risk SQL operations require explicit authorization through existing trust-wall interaction."
    - "Timeout and max-row guardrails are enforced deterministically during SQL execution."
    - "SQL runtime failures emit stable `error_code` values without credential leakage."
  artifacts:
    - path: "core/steps/sql_step.py"
      provides: "SQL runtime handler with guardrail enforcement and deterministic error mapping inputs."
    - path: "core/executor.py"
      provides: "Executor wiring for `StepType.SQL` and SQL error normalization into existing runtime state pipeline."
    - path: "tests/core/test_sql_step.py"
      provides: "Regression coverage for SQL success, trust-gated risk operations, timeout, row-limit, and error-code behavior."
  key_links:
    - from: "core/steps/sql_step.py"
      to: "core/sql/contracts.py"
      via: "Runtime path must call shared contract validator before query execution."
    - from: "core/steps/sql_step.py"
      to: "core/executor.py"
      via: "SQL exceptions propagate stable codes through formatter/state persistence."
    - from: "core/log_redaction.py"
      to: "core/steps/sql_step.py"
      via: "Credential-bearing SQL connection details are sanitized before logs/errors are persisted."
---

<objective>
Implement the SQL runtime execution baseline and deterministic error semantics for Phase 8.

Purpose: deliver SQL-02/04/05/06 runtime behavior with secure diagnostics, guardrails, and authorization-wall routing for risky SQL operations.
Output: executable `sql` step path, guardrail enforcement, stable SQL error-code mapping, and runtime regression tests.
</objective>

<execution_context>
@/Users/xingminghua/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xingminghua/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-CONTEXT.md
@.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-RESEARCH.md
@.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-01-PLAN.md
@core/executor.py
@core/steps/http_step.py
@core/steps/script_step.py
@core/error_formatter.py
@core/log_redaction.py

<interfaces>
Required SQL runtime error-code family:
```text
runtime.sql_invalid_contract
runtime.sql_risk_operation_requires_trust
runtime.sql_risk_operation_denied
runtime.sql_connect_failed
runtime.sql_auth_failed
runtime.sql_query_failed
runtime.sql_timeout
runtime.sql_row_limit_exceeded
```

Phase-8 output contract (pre-normalization baseline):
```text
sql_response contains rows + execution metadata needed for downstream mapping.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SQL step runtime handler and executor branch wiring</name>
  <files>core/steps/sql_step.py, core/steps/__init__.py, core/executor.py, tests/core/test_sql_step.py</files>
  <read_first>core/steps/http_step.py, core/steps/script_step.py, core/executor.py, core/sql/contracts.py</read_first>
  <behavior>
    - Test 1: Valid SELECT SQL executes and returns deterministic output envelope.
    - Test 2: High-risk SQL (mutation/multi-statement) triggers trust-required interaction instead of silent execution.
    - Test 3: Executor handles `StepType.SQL` and updates source state using SQL/trust-gate error-code semantics.
  </behavior>
  <action>Create `execute_sql_step` and wire `StepType.SQL` in executor dispatch. Ensure runtime path invokes shared SQLGlot-based contract classification on final query text and routes high-risk operations to existing authorization-wall interaction protocol (`confirm`) before execution. Preserve backend-owned output/state behavior with no frontend business-logic changes.</action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_step.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/core/test_sql_step.py -q` exits 0.
    - `rg -n "StepType\.SQL|execute_sql_step" core/executor.py core/steps/__init__.py` confirms dispatch wiring.
    - High-risk SQL patterns never execute without explicit trust decision.
  </acceptance_criteria>
  <done>SQL runtime path is wired and enforceable under executor flow semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce timeout/max-row guardrails and deterministic SQL error mapping</name>
  <files>core/steps/sql_step.py, core/executor.py, tests/core/test_sql_step.py, tests/core/test_executor_errors.py</files>
  <read_first>core/error_formatter.py, tests/core/test_executor_errors.py, docs/testing_tdd.md</read_first>
  <behavior>
    - Test 1: Query timeout maps to `runtime.sql_timeout` with deterministic summary/details.
    - Test 2: Max-row guardrail breach maps to `runtime.sql_row_limit_exceeded` and predictable truncation/failure semantics.
    - Test 3: Connect/auth/query/trust-denied failures map to stable SQL error-code family without leaking credentials.
  </behavior>
  <action>Implement SQL guardrail runtime controls (timeout + row limit) and explicit exception mapping to stable SQL error codes. Reuse formatter pipeline conventions so API/runtime state surfaces remain consistent and machine-parseable.</action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_step.py tests/core/test_executor_errors.py -q</automated>
  </verify>
  <acceptance_criteria>
    - SQL failure and trust-gate modes produce stable error codes listed in this plan.
    - `python -m pytest tests/core/test_sql_step.py tests/core/test_executor_errors.py -q` exits 0.
    - `rg -n "runtime\.sql_" core/steps/sql_step.py core/executor.py tests/core/test_sql_step.py` confirms deterministic mapping coverage.
  </acceptance_criteria>
  <done>SQL runtime failures and guardrails are deterministic and test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden SQL log/error redaction for credential safety</name>
  <files>core/log_redaction.py, core/steps/sql_step.py, tests/core/test_sql_step.py</files>
  <read_first>core/log_redaction.py, core/steps/sql_step.py</read_first>
  <behavior>
    - Test 1: SQL connection/auth failures do not expose raw password/token fields in logs or surfaced error details.
    - Test 2: Error details remain actionable while preserving redaction guarantees.
  </behavior>
  <action>Extend redaction coverage for SQL credential-bearing fields/DSN fragments and route SQL-step logging through sanitization helpers. Add tests that assert sensitive fragments are removed from emitted state/log messages.</action>
  <verify>
    <automated>python -m pytest tests/core/test_sql_step.py -q</automated>
  </verify>
  <acceptance_criteria>
    - No plaintext credential fragments are present in SQL runtime error/log surfaces.
    - Redaction tests remain deterministic.
    - Existing non-SQL redaction behavior remains unchanged.
  </acceptance_criteria>
  <done>SQL execution path satisfies SQL-06 credential-safety contracts.</done>
</task>

</tasks>

<verification>
- [ ] python -m pytest tests/core/test_sql_step.py tests/core/test_executor_errors.py -q
- [ ] make test-backend
</verification>

<success_criteria>
- SQL runtime execution is available through `use: sql` with static query-text guardrails and trust-gated high-risk operation handling.
- Timeout/max-row failures and connect/auth/query/trust-gate failures map to stable SQL error codes.
- Credential redaction guarantees hold across SQL logs/runtime failure surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/08-sql-step-contracts-and-safety-guardrails/08-02-SUMMARY.md`
</output>
