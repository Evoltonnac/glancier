This directory stores bundled Python backend artifacts for Tauri release builds.

Build script output:
- `glancier-server-<target-triple>/` (PyInstaller onedir layout)

Do not commit generated platform folders.
